apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterSPIFFEID
metadata:
  name: serviceradar-mapper
spec:
  spiffeIDTemplate: spiffe://carverauto.dev/ns/$(SERVICERADAR_NAMESPACE)/sa/serviceradar-mapper
  namespaceSelector:
    matchLabels:
      kubernetes.io/metadata.name: $(SERVICERADAR_NAMESPACE)
  podSelector:
    matchLabels:
      app: serviceradar-mapper
