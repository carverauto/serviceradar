apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterSPIFFEID
metadata:
  name: serviceradar-datasvc
spec:
  spiffeIDTemplate: spiffe://carverauto.dev/ns/$(SERVICERADAR_NAMESPACE)/sa/serviceradar-datasvc
  namespaceSelector:
    matchLabels:
      kubernetes.io/metadata.name: $(SERVICERADAR_NAMESPACE)
  podSelector:
    matchLabels:
      app: serviceradar-datasvc
