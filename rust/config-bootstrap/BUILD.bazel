load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")
load("@rust_crates//:defs.bzl", "all_crate_deps")

rust_library(
    name = "config_bootstrap",
    srcs = glob(["src/**/*.rs"]),
    crate_root = "src/lib.rs",
    edition = "2021",
    deps = all_crate_deps(normal = True) + ["//rust/kvutil:kvutil"],
    visibility = ["//visibility:public"],
)

rust_test(
    name = "config_bootstrap_test",
    crate = ":config_bootstrap",
    edition = "2021",
)
