[package]
name = "serviceradar-metric-proto"
version = "0.1.0"
edition = "2024"

description = "Generated prost bindings for ServiceRadar's canonical metric envelope."
license = "Apache-2.0"

[lib]
name = "serviceradar_metric_proto"
path = "src/lib.rs"
doctest = false

[dependencies]
prost = { workspace = true }

[build-dependencies]
tonic-build = { workspace = true }
