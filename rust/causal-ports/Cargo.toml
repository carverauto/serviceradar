[package]
name = "serviceradar-causal-ports"
version = "0.1.0"
edition = "2024"

description = "Causal security engine — boundary traits (the isolation seam): ObservationSource, ContextStore, Emitter, MitigationPolicy, ActionExecutor. Phase 0 (OpenSpec: add-causal-security-foundation)."
license = "Apache-2.0"

[lib]
name = "serviceradar_causal_ports"
path = "src/lib.rs"

[dependencies]
serviceradar-causal-model = { path = "../causal-model" }
