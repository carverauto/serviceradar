# serviceradar-bmp-collector

Legacy adapter source retained for reference only.

ServiceRadar now runs upstream `arancini` directly for BMP ingest in Helm, Docker Compose,
and package runtime wiring.
