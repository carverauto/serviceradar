[package]
name = "serviceradar-causal-mitigation"
edition = "2024"

description = "Causal security engine — mitigation authority policy engine (causal_mitigation_policies, shadow/enforce, blast-radius gate). Scaffold only in Phase 0; authored by add-causal-mitigation. (OpenSpec: add-causal-security-foundation)."
license = "Apache-2.0"

[lib]
name = "serviceradar_causal_mitigation"
path = "src/lib.rs"

[dependencies]
serviceradar-causal-model = { path = "../causal-model" }
serviceradar-causal-ports = { path = "../causal-ports" }
