//! The detection code — the security causaloid library (S1-S7) and the kill-chain graph topology.
//! SCAFFOLD ONLY in Phase 0; the causaloids and their fusion nodes are authored by
//! `add-causal-security-detections`. This crate never depends on `causal-reasoning` (they are siblings
//! the binary composes). (OpenSpec `add-causal-security-foundation`.)
#![forbid(unsafe_code)]
