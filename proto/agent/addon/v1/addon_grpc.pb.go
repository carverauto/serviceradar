//
// Copyright 2026 Carver Automation Corporation.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             v7.34.1
// source: agent/addon/v1/addon.proto

package addonpb

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AddonService_Info_FullMethodName             = "/serviceradar.agent.addon.v1.AddonService/Info"
	AddonService_Configure_FullMethodName        = "/serviceradar.agent.addon.v1.AddonService/Configure"
	AddonService_Health_FullMethodName           = "/serviceradar.agent.addon.v1.AddonService/Health"
	AddonService_StreamTelemetry_FullMethodName  = "/serviceradar.agent.addon.v1.AddonService/StreamTelemetry"
	AddonService_StreamArtifacts_FullMethodName  = "/serviceradar.agent.addon.v1.AddonService/StreamArtifacts"
	AddonService_RelayOtlp_FullMethodName        = "/serviceradar.agent.addon.v1.AddonService/RelayOtlp"
	AddonService_StreamMetricFeed_FullMethodName = "/serviceradar.agent.addon.v1.AddonService/StreamMetricFeed"
	AddonService_RunCommand_FullMethodName       = "/serviceradar.agent.addon.v1.AddonService/RunCommand"
)

// AddonServiceClient is the client API for AddonService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// AddonService is the gRPC contract every native agent add-on serves over the
// HashiCorp go-plugin transport (a subprocess + gRPC on a restricted Unix-domain
// socket with AutoMTLS). The agent is the go-plugin client; the add-on is the
// server. The contract is intentionally language-agnostic so add-ons may be
// implemented in Go (via the first-party SDK) or any language with a gRPC stack
// (e.g. Rust via tonic).
type AddonServiceClient interface {
	// Info reports the add-on's stable identity, version, and the capability
	// identifiers it advertises when active.
	Info(ctx context.Context, in *InfoRequest, opts ...grpc.CallOption) (*InfoResponse, error)
	// Configure delivers the operator-selected configuration (already validated
	// against the add-on's config.schema.json by the control plane) and returns a
	// stable hash the agent can use for change detection.
	Configure(ctx context.Context, in *ConfigureRequest, opts ...grpc.CallOption) (*ConfigureResponse, error)
	// Health is the liveness/readiness probe the agent polls on an interval. It
	// doubles as the go-plugin health signal and carries a bounded degradation
	// reason so the agent can report an add-on as degraded rather than absent.
	Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error)
	// StreamTelemetry is an optional add-on-to-agent telemetry stream. The agent
	// only calls this RPC for add-ons that advertise the native-telemetry:v1
	// capability in InfoResponse.capabilities. Add-ons that do not produce
	// telemetry may leave the stream empty.
	StreamTelemetry(ctx context.Context, in *StreamTelemetryRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[TelemetryBatch], error)
	// StreamArtifacts is an optional add-on-to-agent artifact staging stream. The
	// agent only calls this RPC for add-ons that advertise artifact-staging:v1 in
	// InfoResponse.capabilities. Add-ons send bounded chunks; the agent stages the
	// bytes through agent-gateway and never exposes object-store credentials.
	StreamArtifacts(ctx context.Context, in *StreamArtifactsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ArtifactUploadChunk], error)
	// RelayOtlp is the acked add-on-to-agent OTLP relay stream. The agent only
	// calls this RPC for add-ons that advertise the otlp-relay:v1 capability in
	// InfoResponse.capabilities. The add-on is the gRPC server (go-plugin
	// transport), but the AGENT is the RPC client: it opens the stream, reads
	// OtlpRelayFrame messages off the response stream, and writes OtlpRelayAck
	// watermarks on the request stream.
	//
	// Unlike StreamTelemetry (lossy, fire-and-forget), RelayOtlp is an
	// at-least-once delivery contract: every frame carries a persistent,
	// monotonically increasing relay_id assigned by the add-on's durable spool,
	// and the frame remains the add-on's responsibility (spooled on disk) until
	// the agent acks it. The agent forwards each frame to the agent-gateway
	// inside a GatewayServiceStatus envelope with source == "otlp-relay"
	// (GatewayServiceStatus.message = the encoded TelemetryBatch) and acks the
	// add-on only after the gateway confirms the forward succeeded. After a
	// reconnect the add-on re-sends every unacked frame, resuming from its last
	// acked watermark with the original relay_ids.
	RelayOtlp(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[OtlpRelayAck, OtlpRelayFrame], error)
	// StreamMetricFeed is the agent-to-add-on local metric feed. The agent only
	// calls this RPC for add-ons that advertise the metric-feed:v1 capability in
	// InfoResponse.capabilities. It is the data-direction inverse of the upstream
	// streams: the add-on is the gRPC server, but the AGENT is the RPC client and
	// the DATA PRODUCER. The agent writes MetricFeedFrame messages on the request
	// stream BEFORE those samples are published to agent-gateway, and reads
	// MetricFeedAck watermarks on the response stream for flow control.
	//
	// The feed lets a co-located compute add-on (for example per-series anomaly
	// detection) analyze the agent's locally collected samples at the edge so the
	// platform can ship verdicts/rollups instead of every raw point. Acks are
	// cumulative and back-pressure the agent: the agent bounds the number of
	// unacked in-flight frames per add-on so a slow add-on can never stall the
	// agent's own collection or its gateway publishing path. The feed is lossy by
	// contract (it is analysis input, not a durable delivery path): if an add-on
	// falls past the agent's in-flight bound, the agent drops feed frames for that
	// add-on and records the drop rather than blocking.
	StreamMetricFeed(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[MetricFeedFrame, MetricFeedAck], error)
	// RunCommand executes a bounded, platform-scheduled command against a local
	// add-on instance. The control plane records an addon.run_command command,
	// agent-gateway streams it to the agent, and the agent invokes this RPC on the
	// supervised add-on subprocess. Add-ons never receive direct core, web-ng, or
	// object-store connectivity through this contract.
	RunCommand(ctx context.Context, in *RunCommandRequest, opts ...grpc.CallOption) (*RunCommandResponse, error)
}

type addonServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAddonServiceClient(cc grpc.ClientConnInterface) AddonServiceClient {
	return &addonServiceClient{cc}
}

func (c *addonServiceClient) Info(ctx context.Context, in *InfoRequest, opts ...grpc.CallOption) (*InfoResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(InfoResponse)
	err := c.cc.Invoke(ctx, AddonService_Info_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *addonServiceClient) Configure(ctx context.Context, in *ConfigureRequest, opts ...grpc.CallOption) (*ConfigureResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ConfigureResponse)
	err := c.cc.Invoke(ctx, AddonService_Configure_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *addonServiceClient) Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HealthResponse)
	err := c.cc.Invoke(ctx, AddonService_Health_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *addonServiceClient) StreamTelemetry(ctx context.Context, in *StreamTelemetryRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[TelemetryBatch], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AddonService_ServiceDesc.Streams[0], AddonService_StreamTelemetry_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[StreamTelemetryRequest, TelemetryBatch]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_StreamTelemetryClient = grpc.ServerStreamingClient[TelemetryBatch]

func (c *addonServiceClient) StreamArtifacts(ctx context.Context, in *StreamArtifactsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ArtifactUploadChunk], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AddonService_ServiceDesc.Streams[1], AddonService_StreamArtifacts_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[StreamArtifactsRequest, ArtifactUploadChunk]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_StreamArtifactsClient = grpc.ServerStreamingClient[ArtifactUploadChunk]

func (c *addonServiceClient) RelayOtlp(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[OtlpRelayAck, OtlpRelayFrame], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AddonService_ServiceDesc.Streams[2], AddonService_RelayOtlp_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[OtlpRelayAck, OtlpRelayFrame]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_RelayOtlpClient = grpc.BidiStreamingClient[OtlpRelayAck, OtlpRelayFrame]

func (c *addonServiceClient) StreamMetricFeed(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[MetricFeedFrame, MetricFeedAck], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AddonService_ServiceDesc.Streams[3], AddonService_StreamMetricFeed_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[MetricFeedFrame, MetricFeedAck]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_StreamMetricFeedClient = grpc.BidiStreamingClient[MetricFeedFrame, MetricFeedAck]

func (c *addonServiceClient) RunCommand(ctx context.Context, in *RunCommandRequest, opts ...grpc.CallOption) (*RunCommandResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RunCommandResponse)
	err := c.cc.Invoke(ctx, AddonService_RunCommand_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AddonServiceServer is the server API for AddonService service.
// All implementations must embed UnimplementedAddonServiceServer
// for forward compatibility.
//
// AddonService is the gRPC contract every native agent add-on serves over the
// HashiCorp go-plugin transport (a subprocess + gRPC on a restricted Unix-domain
// socket with AutoMTLS). The agent is the go-plugin client; the add-on is the
// server. The contract is intentionally language-agnostic so add-ons may be
// implemented in Go (via the first-party SDK) or any language with a gRPC stack
// (e.g. Rust via tonic).
type AddonServiceServer interface {
	// Info reports the add-on's stable identity, version, and the capability
	// identifiers it advertises when active.
	Info(context.Context, *InfoRequest) (*InfoResponse, error)
	// Configure delivers the operator-selected configuration (already validated
	// against the add-on's config.schema.json by the control plane) and returns a
	// stable hash the agent can use for change detection.
	Configure(context.Context, *ConfigureRequest) (*ConfigureResponse, error)
	// Health is the liveness/readiness probe the agent polls on an interval. It
	// doubles as the go-plugin health signal and carries a bounded degradation
	// reason so the agent can report an add-on as degraded rather than absent.
	Health(context.Context, *HealthRequest) (*HealthResponse, error)
	// StreamTelemetry is an optional add-on-to-agent telemetry stream. The agent
	// only calls this RPC for add-ons that advertise the native-telemetry:v1
	// capability in InfoResponse.capabilities. Add-ons that do not produce
	// telemetry may leave the stream empty.
	StreamTelemetry(*StreamTelemetryRequest, grpc.ServerStreamingServer[TelemetryBatch]) error
	// StreamArtifacts is an optional add-on-to-agent artifact staging stream. The
	// agent only calls this RPC for add-ons that advertise artifact-staging:v1 in
	// InfoResponse.capabilities. Add-ons send bounded chunks; the agent stages the
	// bytes through agent-gateway and never exposes object-store credentials.
	StreamArtifacts(*StreamArtifactsRequest, grpc.ServerStreamingServer[ArtifactUploadChunk]) error
	// RelayOtlp is the acked add-on-to-agent OTLP relay stream. The agent only
	// calls this RPC for add-ons that advertise the otlp-relay:v1 capability in
	// InfoResponse.capabilities. The add-on is the gRPC server (go-plugin
	// transport), but the AGENT is the RPC client: it opens the stream, reads
	// OtlpRelayFrame messages off the response stream, and writes OtlpRelayAck
	// watermarks on the request stream.
	//
	// Unlike StreamTelemetry (lossy, fire-and-forget), RelayOtlp is an
	// at-least-once delivery contract: every frame carries a persistent,
	// monotonically increasing relay_id assigned by the add-on's durable spool,
	// and the frame remains the add-on's responsibility (spooled on disk) until
	// the agent acks it. The agent forwards each frame to the agent-gateway
	// inside a GatewayServiceStatus envelope with source == "otlp-relay"
	// (GatewayServiceStatus.message = the encoded TelemetryBatch) and acks the
	// add-on only after the gateway confirms the forward succeeded. After a
	// reconnect the add-on re-sends every unacked frame, resuming from its last
	// acked watermark with the original relay_ids.
	RelayOtlp(grpc.BidiStreamingServer[OtlpRelayAck, OtlpRelayFrame]) error
	// StreamMetricFeed is the agent-to-add-on local metric feed. The agent only
	// calls this RPC for add-ons that advertise the metric-feed:v1 capability in
	// InfoResponse.capabilities. It is the data-direction inverse of the upstream
	// streams: the add-on is the gRPC server, but the AGENT is the RPC client and
	// the DATA PRODUCER. The agent writes MetricFeedFrame messages on the request
	// stream BEFORE those samples are published to agent-gateway, and reads
	// MetricFeedAck watermarks on the response stream for flow control.
	//
	// The feed lets a co-located compute add-on (for example per-series anomaly
	// detection) analyze the agent's locally collected samples at the edge so the
	// platform can ship verdicts/rollups instead of every raw point. Acks are
	// cumulative and back-pressure the agent: the agent bounds the number of
	// unacked in-flight frames per add-on so a slow add-on can never stall the
	// agent's own collection or its gateway publishing path. The feed is lossy by
	// contract (it is analysis input, not a durable delivery path): if an add-on
	// falls past the agent's in-flight bound, the agent drops feed frames for that
	// add-on and records the drop rather than blocking.
	StreamMetricFeed(grpc.BidiStreamingServer[MetricFeedFrame, MetricFeedAck]) error
	// RunCommand executes a bounded, platform-scheduled command against a local
	// add-on instance. The control plane records an addon.run_command command,
	// agent-gateway streams it to the agent, and the agent invokes this RPC on the
	// supervised add-on subprocess. Add-ons never receive direct core, web-ng, or
	// object-store connectivity through this contract.
	RunCommand(context.Context, *RunCommandRequest) (*RunCommandResponse, error)
	mustEmbedUnimplementedAddonServiceServer()
}

// UnimplementedAddonServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAddonServiceServer struct{}

func (UnimplementedAddonServiceServer) Info(context.Context, *InfoRequest) (*InfoResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Info not implemented")
}
func (UnimplementedAddonServiceServer) Configure(context.Context, *ConfigureRequest) (*ConfigureResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Configure not implemented")
}
func (UnimplementedAddonServiceServer) Health(context.Context, *HealthRequest) (*HealthResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Health not implemented")
}
func (UnimplementedAddonServiceServer) StreamTelemetry(*StreamTelemetryRequest, grpc.ServerStreamingServer[TelemetryBatch]) error {
	return status.Errorf(codes.Unimplemented, "method StreamTelemetry not implemented")
}
func (UnimplementedAddonServiceServer) StreamArtifacts(*StreamArtifactsRequest, grpc.ServerStreamingServer[ArtifactUploadChunk]) error {
	return status.Errorf(codes.Unimplemented, "method StreamArtifacts not implemented")
}
func (UnimplementedAddonServiceServer) RelayOtlp(grpc.BidiStreamingServer[OtlpRelayAck, OtlpRelayFrame]) error {
	return status.Errorf(codes.Unimplemented, "method RelayOtlp not implemented")
}
func (UnimplementedAddonServiceServer) StreamMetricFeed(grpc.BidiStreamingServer[MetricFeedFrame, MetricFeedAck]) error {
	return status.Errorf(codes.Unimplemented, "method StreamMetricFeed not implemented")
}
func (UnimplementedAddonServiceServer) RunCommand(context.Context, *RunCommandRequest) (*RunCommandResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method RunCommand not implemented")
}
func (UnimplementedAddonServiceServer) mustEmbedUnimplementedAddonServiceServer() {}
func (UnimplementedAddonServiceServer) testEmbeddedByValue()                      {}

// UnsafeAddonServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AddonServiceServer will
// result in compilation errors.
type UnsafeAddonServiceServer interface {
	mustEmbedUnimplementedAddonServiceServer()
}

func RegisterAddonServiceServer(s grpc.ServiceRegistrar, srv AddonServiceServer) {
	// If the following call pancis, it indicates UnimplementedAddonServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AddonService_ServiceDesc, srv)
}

func _AddonService_Info_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(InfoRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AddonServiceServer).Info(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AddonService_Info_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AddonServiceServer).Info(ctx, req.(*InfoRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AddonService_Configure_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ConfigureRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AddonServiceServer).Configure(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AddonService_Configure_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AddonServiceServer).Configure(ctx, req.(*ConfigureRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AddonService_Health_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HealthRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AddonServiceServer).Health(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AddonService_Health_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AddonServiceServer).Health(ctx, req.(*HealthRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AddonService_StreamTelemetry_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(StreamTelemetryRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(AddonServiceServer).StreamTelemetry(m, &grpc.GenericServerStream[StreamTelemetryRequest, TelemetryBatch]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_StreamTelemetryServer = grpc.ServerStreamingServer[TelemetryBatch]

func _AddonService_StreamArtifacts_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(StreamArtifactsRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(AddonServiceServer).StreamArtifacts(m, &grpc.GenericServerStream[StreamArtifactsRequest, ArtifactUploadChunk]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_StreamArtifactsServer = grpc.ServerStreamingServer[ArtifactUploadChunk]

func _AddonService_RelayOtlp_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(AddonServiceServer).RelayOtlp(&grpc.GenericServerStream[OtlpRelayAck, OtlpRelayFrame]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_RelayOtlpServer = grpc.BidiStreamingServer[OtlpRelayAck, OtlpRelayFrame]

func _AddonService_StreamMetricFeed_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(AddonServiceServer).StreamMetricFeed(&grpc.GenericServerStream[MetricFeedFrame, MetricFeedAck]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AddonService_StreamMetricFeedServer = grpc.BidiStreamingServer[MetricFeedFrame, MetricFeedAck]

func _AddonService_RunCommand_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RunCommandRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AddonServiceServer).RunCommand(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AddonService_RunCommand_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AddonServiceServer).RunCommand(ctx, req.(*RunCommandRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AddonService_ServiceDesc is the grpc.ServiceDesc for AddonService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AddonService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "serviceradar.agent.addon.v1.AddonService",
	HandlerType: (*AddonServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Info",
			Handler:    _AddonService_Info_Handler,
		},
		{
			MethodName: "Configure",
			Handler:    _AddonService_Configure_Handler,
		},
		{
			MethodName: "Health",
			Handler:    _AddonService_Health_Handler,
		},
		{
			MethodName: "RunCommand",
			Handler:    _AddonService_RunCommand_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "StreamTelemetry",
			Handler:       _AddonService_StreamTelemetry_Handler,
			ServerStreams: true,
		},
		{
			StreamName:    "StreamArtifacts",
			Handler:       _AddonService_StreamArtifacts_Handler,
			ServerStreams: true,
		},
		{
			StreamName:    "RelayOtlp",
			Handler:       _AddonService_RelayOtlp_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
		{
			StreamName:    "StreamMetricFeed",
			Handler:       _AddonService_StreamMetricFeed_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "agent/addon/v1/addon.proto",
}
