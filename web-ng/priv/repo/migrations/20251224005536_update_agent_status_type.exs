defmodule ServiceRadarWebNG.Repo.Migrations.UpdateAgentStatusType do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:ocsf_agents) do
      modify :status, :text, null: false, default: "connecting"
    end
  end

  def down do
    alter table(:ocsf_agents) do
      modify :status, :text, null: true, default: "connected"
    end
  end
end
