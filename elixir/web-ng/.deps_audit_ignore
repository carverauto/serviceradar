# GHSA-4g2h-vm7x-747c / EEF-CVE-2026-28809 / esaml XXE
#
# The advisory is in esaml's own XML decoding (esaml_sp / esaml_util), which
# web-ng never calls. esaml is a direct dependency only for :xmerl_dsig
# signature verification, and every document it sees -- SAML responses and IdP
# metadata alike -- is parsed first by
# lib/serviceradar_web_ng_web/auth/saml_xml.ex with `dtd: :none`, which rejects
# any entity declaration and never fetches an external resource. Covered by
# test/phoenix/auth/saml_response_test.exs and saml_metadata_test.exs.
#
# There is no newer Hex release than esaml 4.6.0 available to upgrade to
# (re-verified 2026-09-25 via mix hex.info), so we track the advisory here until
# upstream publishes a patched package or the dependency is replaced.
GHSA-4g2h-vm7x-747c

# GHSA-rhv4-8758-jx7v / Decimal unbounded exponent DoS
#
# web-ng currently receives Decimal through shared Ash, Ecto, Postgrex, ADBC,
# and schema tooling dependencies that constrain Decimal to the 2.x line. The
# first advisory-listed patched release is Decimal 3.0.0, so this cannot be
# resolved in this Mix project without a broader dependency compatibility
# upgrade across the Elixir workspace.
#
# Keep this waiver local to this quality gate until the shared dependency graph
# accepts Decimal 3.x or the dependency is otherwise removed from this release.
GHSA-rhv4-8758-jx7v

# Cowlib waiver rationale and removal criteria:
# ../serviceradar_core/.deps_audit_ignore (EEF-CVE-2026-43966 block).
EEF-CVE-2026-43966
EEF-CVE-2026-43969
EEF-CVE-2026-43971
GHSA-g2wm-735q-3f56

# mix hex.audit and mix deps.audit report the cowlib response-splitting defect
# under different ids, and the mix_audit ignore file matches on the advisory id
# exactly (MixAudit.CLI.Audit rejects `&1.id in ignored_advisory_ids`), so the
# GitHub id has to be listed alongside the EEF id above. It is the same defect:
# the advisory text is verbatim about cowlib, reported here against the
# dependent package gun. Note that gun cannot be upgraded out of it -- the gun
# row carries cowboy's range data (`< 2.16.0`, first patched `2.16.0`) while
# gun's newest release is 2.5.0, so no gun version can ever satisfy it.
GHSA-w4f7-4cxr-rv3c
