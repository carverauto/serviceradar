# GHSA-rhv4-8758-jx7v / Decimal unbounded exponent DoS
#
# serviceradar_agent_gateway currently receives Decimal through shared Ash,
# Ecto, Postgrex, and schema tooling dependencies that constrain Decimal to
# the 2.x line. The first advisory-listed patched release is Decimal 3.0.0,
# so this cannot be resolved in this Mix project without a broader dependency
# compatibility upgrade across the Elixir workspace.
#
# Keep this waiver local to the gateway quality gate until the shared Ash/Ecto
# dependency graph accepts Decimal 3.x or the dependency is otherwise removed
# from this release.
GHSA-rhv4-8758-jx7v

# EEF-CVE-2026-43966 / cowlib HTTP response splitting,
# EEF-CVE-2026-43969 / GHSA-g2wm-735q-3f56 / cowlib cookie encoder, and
# EEF-CVE-2026-43971 / cowlib Link header directive smuggling have no patched
# Hex release as of cowlib 2.19.0. That release fixed only the HPACK/QPACK
# decoder DoS (EEF-CVE-2026-59248). 43966/43969 still list every release from
# 2.9.0 as affected. Re-verified 2026-09-09: cowlib 2.20.0 ships the 43971 git
# fix (ninenines/cowlib@89da27ee, confirmed in the 2.19.0...2.20.0 compare),
# but the advisory DB still flags 2.20.0 for all three, and 2.20.0 leaves the
# 43966 escape_string/2 and 43969 cookie-encoder paths unchanged (checked
# against the Hex tarball), so no upgrade closes the gate yet. Keep these
# waivers until the advisory DB recognizes a patched release or the
# dependency is replaced.
EEF-CVE-2026-43966
EEF-CVE-2026-43969
EEF-CVE-2026-43971
GHSA-g2wm-735q-3f56

# mix hex.audit and mix deps.audit report the cowlib response-splitting defect
# under different ids, and the mix_audit ignore file matches on the advisory id
# exactly (MixAudit.CLI.Audit rejects `&1.id in ignored_advisory_ids`), so the
# GitHub id has to be listed alongside the EEF id above. It is the same defect:
# the advisory text is verbatim about cowlib, reported here against the
# dependent package gun. Note that gun cannot be upgraded out of it -- the gun
# row carries cowboy's range data (`< 2.16.0`, first patched `2.16.0`) while
# gun's newest release is 2.5.0, so no gun version can ever satisfy it.
GHSA-w4f7-4cxr-rv3c
