defmodule ServiceRadar.Repo.TenantMigrations.RemoveTenantIdColumns do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:edge_sites, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:edge_sites, [:tenant_id, :slug],
                     name: "edge_sites_unique_slug_per_tenant_index"
                   )

    create unique_index(:edge_sites, [:slug], name: "edge_sites_unique_slug_index")

    alter table(:sysmon_profiles, prefix: prefix()) do
      remove :tenant_id
      modify :disk_paths, {:array, :text}, default: []
      add :disk_exclude_paths, {:array, :text}, null: false, default: []
    end

    drop_if_exists unique_index(:sysmon_profiles, [:tenant_id, :name],
                     name: "sysmon_profiles_unique_name_per_tenant_index"
                   )

    create unique_index(:sysmon_profiles, [:name],
             name: "sysmon_profiles_unique_name_per_tenant_index"
           )

    alter table(:alerts, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists index(:health_events, [:tenant_id, :recorded_at])

    alter table(:health_events, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:poll_jobs, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:snmp_targets, prefix: prefix()) do
      remove :community
      remove :tenant_id
      add :community_encrypted, :binary
    end

    alter table(:nats_credentials, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:tenant_cas, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:tenant_cas, [:tenant_id, :status],
                     name: "tenant_cas_unique_active_tenant_ca_index"
                   )

    create unique_index(:tenant_cas, [:status],
             name: "tenant_cas_unique_active_ca_index",
             where: "(status = 'active')"
           )

    alter table(:integration_sources, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:integration_sources, [:tenant_id, :name],
                     name: "integration_sources_unique_name_per_tenant_index"
                   )

    create unique_index(:integration_sources, [:name],
             name: "integration_sources_unique_name_index"
           )

    drop_if_exists index(:agent_config_versions, [:tenant_id, :created_at],
                     name: "agent_config_versions_tenant_created_idx"
                   )

    alter table(:agent_config_versions, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists index(:sweep_host_results, [:tenant_id, :status],
                     name: "sweep_host_results_status_idx"
                   )

    drop_if_exists index(:sweep_host_results, [:tenant_id, :ip],
                     name: "sweep_host_results_ip_idx"
                   )

    drop_if_exists index(:sweep_host_results, [:tenant_id, :execution_id],
                     name: "sweep_host_results_execution_idx"
                   )

    create index(:sweep_host_results, [:status], name: "sweep_host_results_status_idx")

    create index(:sweep_host_results, [:ip], name: "sweep_host_results_ip_idx")

    create index(:sweep_host_results, [:execution_id], name: "sweep_host_results_execution_idx")

    alter table(:sweep_host_results, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:collector_packages, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:collector_packages, [:tenant_id, :user_name],
                     name: "collector_packages_unique_user_name_per_tenant_index"
                   )

    create unique_index(:collector_packages, [:user_name],
             name: "collector_packages_unique_user_name_index"
           )

    create table(:tenant_memberships, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :role, :text, null: false, default: "member"
      add :joined_at, :utc_datetime, null: false, default: fragment("(now() AT TIME ZONE 'utc')")
      add :user_id, :uuid, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:tenant_memberships, [:user_id],
             name: "tenant_memberships_unique_membership_index",
             prefix: "public"
           )

    alter table(:snmp_oid_configs, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:zen_rules, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:zen_rules, [:tenant_id, :subject, :name],
                     name: "zen_rules_unique_name_index"
                   )

    create unique_index(:zen_rules, [:subject, :name], name: "zen_rules_unique_name_index")

    alter table(:device_groups, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:device_groups, [:tenant_id, :name],
                     name: "device_groups_unique_name_per_tenant_index"
                   )

    create unique_index(:device_groups, [:name], name: "device_groups_unique_name_index")

    drop_if_exists index(:sweep_group_executions, [:tenant_id, :status],
                     name: "sweep_group_executions_status_idx"
                   )

    drop_if_exists index(:sweep_group_executions, [:tenant_id, :sweep_group_id, :started_at],
                     name: "sweep_group_executions_group_started_idx"
                   )

    create index(:sweep_group_executions, [:status], name: "sweep_group_executions_status_idx")

    create index(:sweep_group_executions, [:sweep_group_id, :started_at],
             name: "sweep_group_executions_group_started_idx"
           )

    alter table(:sweep_group_executions, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:stateful_alert_rule_templates, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:stateful_alert_rule_templates, [:tenant_id, :name],
                     name: "stateful_alert_rule_templates_unique_name_index"
                   )

    create unique_index(:stateful_alert_rule_templates, [:name],
             name: "stateful_alert_rule_templates_unique_name_index"
           )

    alter table(:ng_users, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:checkers, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:polling_schedules, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:log_promotion_rules, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:log_promotion_rules, [:tenant_id, :name],
                     name: "log_promotion_rules_unique_name_index"
                   )

    create unique_index(:log_promotion_rules, [:name],
             name: "log_promotion_rules_unique_name_index"
           )

    alter table(:nats_leaf_servers, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:snmp_oid_templates, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:snmp_oid_templates, [:tenant_id, :vendor, :name],
                     name: "snmp_oid_templates_unique_name_per_vendor_tenant_index"
                   )

    create unique_index(:snmp_oid_templates, [:vendor, :name],
             name: "snmp_oid_templates_unique_name_per_vendor_index"
           )

    alter table(:agent_config_templates, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:agent_config_templates, [:tenant_id, :name, :config_type],
                     name: "agent_config_templates_unique_name_per_tenant_and_type_index"
                   )

    create unique_index(:agent_config_templates, [:name, :config_type],
             name: "agent_config_templates_unique_name_per_tenant_and_type_index"
           )

    alter table(:edge_onboarding_packages, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:gateways, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:api_tokens, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:snmp_profiles, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:snmp_profiles, [:tenant_id, :name],
                     name: "snmp_profiles_unique_name_per_tenant_index"
                   )

    create unique_index(:snmp_profiles, [:name], name: "snmp_profiles_unique_name_index")

    drop_if_exists index(:sweep_groups, [:tenant_id, :agent_id],
                     name: "sweep_groups_tenant_agent_idx"
                   )

    drop_if_exists index(:sweep_groups, [:tenant_id, :partition],
                     name: "sweep_groups_tenant_partition_idx"
                   )

    create index(:sweep_groups, [:agent_id],
             name: "sweep_groups_agent_idx",
             where: "agent_id IS NOT NULL"
           )

    create index(:sweep_groups, [:partition], name: "sweep_groups_partition_idx")

    alter table(:sweep_groups, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:sweep_groups, [:tenant_id, :name],
                     name: "sweep_groups_unique_name_per_tenant_index"
                   )

    create unique_index(:sweep_groups, [:name], name: "sweep_groups_unique_name_per_tenant_index")

    alter table(:partitions, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:partitions, [:tenant_id, :slug],
                     name: "partitions_unique_slug_per_tenant_index"
                   )

    create unique_index(:partitions, [:slug], name: "partitions_unique_slug_index")

    alter table(:log_promotion_rule_templates, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:log_promotion_rule_templates, [:tenant_id, :name],
                     name: "log_promotion_rule_templates_unique_name_index"
                   )

    create unique_index(:log_promotion_rule_templates, [:name],
             name: "log_promotion_rule_templates_unique_name_index"
           )

    alter table(:ocsf_agents, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:stateful_alert_rule_states, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:stateful_alert_rule_states, [:tenant_id, :rule_id, :group_key],
                     name: "stateful_alert_rule_states_unique_state_index"
                   )

    create unique_index(:stateful_alert_rule_states, [:rule_id, :group_key],
             name: "stateful_alert_rule_states_unique_state_index"
           )

    alter table(:device_alias_states, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:service_checks, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists index(:agent_config_instances, [:tenant_id, :config_type, :agent_id],
                     name: "agent_config_instances_tenant_type_agent_idx"
                   )

    drop_if_exists index(:agent_config_instances, [:tenant_id, :config_type, :partition],
                     name: "agent_config_instances_tenant_type_partition_idx"
                   )

    create index(:agent_config_instances, [:config_type, :agent_id],
             name: "agent_config_instances_type_agent_idx",
             where: "agent_id IS NOT NULL"
           )

    create index(:agent_config_instances, [:config_type, :partition],
             name: "agent_config_instances_type_partition_idx"
           )

    alter table(:agent_config_instances, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(
                     :agent_config_instances,
                     [:tenant_id, :config_type, :partition, :agent_id],
                     name: "agent_config_instances_unique_config_per_agent_index"
                   )

    create unique_index(:agent_config_instances, [:config_type, :partition, :agent_id],
             name: "agent_config_instances_unique_config_per_agent_index"
           )

    alter table(:zen_rule_templates, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:zen_rule_templates, [:tenant_id, :subject, :name],
                     name: "zen_rule_templates_unique_name_index"
                   )

    create unique_index(:zen_rule_templates, [:subject, :name],
             name: "zen_rule_templates_unique_name_index"
           )

    alter table(:sweep_profiles, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:sweep_profiles, [:tenant_id, :name],
                     name: "sweep_profiles_unique_name_per_tenant_index"
                   )

    create unique_index(:sweep_profiles, [:name],
             name: "sweep_profiles_unique_name_per_tenant_index"
           )

    alter table(:stateful_alert_rules, prefix: prefix()) do
      remove :tenant_id
    end

    drop_if_exists unique_index(:stateful_alert_rules, [:tenant_id, :name],
                     name: "stateful_alert_rules_unique_name_index"
                   )

    create unique_index(:stateful_alert_rules, [:name],
             name: "stateful_alert_rules_unique_name_index"
           )
  end

  def down do
    drop_if_exists unique_index(:stateful_alert_rules, [:name],
                     name: "stateful_alert_rules_unique_name_index"
                   )

    create unique_index(:stateful_alert_rules, [:tenant_id, :name],
             name: "stateful_alert_rules_unique_name_index"
           )

    alter table(:stateful_alert_rules, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:sweep_profiles, [:name],
                     name: "sweep_profiles_unique_name_per_tenant_index"
                   )

    create unique_index(:sweep_profiles, [:tenant_id, :name],
             name: "sweep_profiles_unique_name_per_tenant_index"
           )

    alter table(:sweep_profiles, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:zen_rule_templates, [:subject, :name],
                     name: "zen_rule_templates_unique_name_index"
                   )

    create unique_index(:zen_rule_templates, [:tenant_id, :subject, :name],
             name: "zen_rule_templates_unique_name_index"
           )

    alter table(:zen_rule_templates, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:agent_config_instances, [:config_type, :partition, :agent_id],
                     name: "agent_config_instances_unique_config_per_agent_index"
                   )

    create unique_index(
             :agent_config_instances,
             [:tenant_id, :config_type, :partition, :agent_id],
             name: "agent_config_instances_unique_config_per_agent_index"
           )

    alter table(:agent_config_instances, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists index(:agent_config_instances, [:config_type, :partition],
                     name: "agent_config_instances_type_partition_idx"
                   )

    drop_if_exists index(:agent_config_instances, [:config_type, :agent_id],
                     name: "agent_config_instances_type_agent_idx"
                   )

    create index(:agent_config_instances, [:tenant_id, :config_type, :partition],
             name: "agent_config_instances_tenant_type_partition_idx"
           )

    create index(:agent_config_instances, [:tenant_id, :config_type, :agent_id],
             name: "agent_config_instances_tenant_type_agent_idx",
             where: "agent_id IS NOT NULL"
           )

    alter table(:service_checks, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:device_alias_states, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:stateful_alert_rule_states, [:rule_id, :group_key],
                     name: "stateful_alert_rule_states_unique_state_index"
                   )

    create unique_index(:stateful_alert_rule_states, [:tenant_id, :rule_id, :group_key],
             name: "stateful_alert_rule_states_unique_state_index"
           )

    alter table(:stateful_alert_rule_states, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:ocsf_agents, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:log_promotion_rule_templates, [:name],
                     name: "log_promotion_rule_templates_unique_name_index"
                   )

    create unique_index(:log_promotion_rule_templates, [:tenant_id, :name],
             name: "log_promotion_rule_templates_unique_name_index"
           )

    alter table(:log_promotion_rule_templates, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:partitions, [:slug], name: "partitions_unique_slug_index")

    create unique_index(:partitions, [:tenant_id, :slug],
             name: "partitions_unique_slug_per_tenant_index"
           )

    alter table(:partitions, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:sweep_groups, [:name],
                     name: "sweep_groups_unique_name_per_tenant_index"
                   )

    create unique_index(:sweep_groups, [:tenant_id, :name],
             name: "sweep_groups_unique_name_per_tenant_index"
           )

    alter table(:sweep_groups, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists index(:sweep_groups, [:partition], name: "sweep_groups_partition_idx")

    drop_if_exists index(:sweep_groups, [:agent_id], name: "sweep_groups_agent_idx")

    create index(:sweep_groups, [:tenant_id, :partition],
             name: "sweep_groups_tenant_partition_idx"
           )

    create index(:sweep_groups, [:tenant_id, :agent_id],
             name: "sweep_groups_tenant_agent_idx",
             where: "agent_id IS NOT NULL"
           )

    drop_if_exists unique_index(:snmp_profiles, [:name], name: "snmp_profiles_unique_name_index")

    create unique_index(:snmp_profiles, [:tenant_id, :name],
             name: "snmp_profiles_unique_name_per_tenant_index"
           )

    alter table(:snmp_profiles, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:api_tokens, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:gateways, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:edge_onboarding_packages, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:agent_config_templates, [:name, :config_type],
                     name: "agent_config_templates_unique_name_per_tenant_and_type_index"
                   )

    create unique_index(:agent_config_templates, [:tenant_id, :name, :config_type],
             name: "agent_config_templates_unique_name_per_tenant_and_type_index"
           )

    alter table(:agent_config_templates, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:snmp_oid_templates, [:vendor, :name],
                     name: "snmp_oid_templates_unique_name_per_vendor_index"
                   )

    create unique_index(:snmp_oid_templates, [:tenant_id, :vendor, :name],
             name: "snmp_oid_templates_unique_name_per_vendor_tenant_index"
           )

    alter table(:snmp_oid_templates, prefix: prefix()) do
      add :tenant_id, :uuid
    end

    alter table(:nats_leaf_servers, prefix: prefix()) do
      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "nats_leaf_servers_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    drop_if_exists unique_index(:log_promotion_rules, [:name],
                     name: "log_promotion_rules_unique_name_index"
                   )

    create unique_index(:log_promotion_rules, [:tenant_id, :name],
             name: "log_promotion_rules_unique_name_index"
           )

    alter table(:log_promotion_rules, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:polling_schedules, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:checkers, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:ng_users, prefix: prefix()) do
      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "ng_users_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    drop_if_exists unique_index(:stateful_alert_rule_templates, [:name],
                     name: "stateful_alert_rule_templates_unique_name_index"
                   )

    create unique_index(:stateful_alert_rule_templates, [:tenant_id, :name],
             name: "stateful_alert_rule_templates_unique_name_index"
           )

    alter table(:stateful_alert_rule_templates, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:sweep_group_executions, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists index(:sweep_group_executions, [:sweep_group_id, :started_at],
                     name: "sweep_group_executions_group_started_idx"
                   )

    drop_if_exists index(:sweep_group_executions, [:status],
                     name: "sweep_group_executions_status_idx"
                   )

    create index(:sweep_group_executions, [:tenant_id, :sweep_group_id, :started_at],
             name: "sweep_group_executions_group_started_idx"
           )

    create index(:sweep_group_executions, [:tenant_id, :status],
             name: "sweep_group_executions_status_idx"
           )

    drop_if_exists unique_index(:device_groups, [:name], name: "device_groups_unique_name_index")

    create unique_index(:device_groups, [:tenant_id, :name],
             name: "device_groups_unique_name_per_tenant_index"
           )

    alter table(:device_groups, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:zen_rules, [:subject, :name],
                     name: "zen_rules_unique_name_index"
                   )

    create unique_index(:zen_rules, [:tenant_id, :subject, :name],
             name: "zen_rules_unique_name_index"
           )

    alter table(:zen_rules, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:snmp_oid_configs, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:tenant_memberships, [:user_id],
                     name: "tenant_memberships_unique_membership_index",
                     prefix: "public"
                   )

    drop table(:tenant_memberships, prefix: prefix())

    drop_if_exists unique_index(:collector_packages, [:user_name],
                     name: "collector_packages_unique_user_name_index"
                   )

    create unique_index(:collector_packages, [:tenant_id, :user_name],
             name: "collector_packages_unique_user_name_per_tenant_index"
           )

    alter table(:collector_packages, prefix: prefix()) do
      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "collector_packages_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    alter table(:sweep_host_results, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists index(:sweep_host_results, [:execution_id],
                     name: "sweep_host_results_execution_idx"
                   )

    drop_if_exists index(:sweep_host_results, [:ip], name: "sweep_host_results_ip_idx")

    drop_if_exists index(:sweep_host_results, [:status], name: "sweep_host_results_status_idx")

    create index(:sweep_host_results, [:tenant_id, :execution_id],
             name: "sweep_host_results_execution_idx"
           )

    create index(:sweep_host_results, [:tenant_id, :ip], name: "sweep_host_results_ip_idx")

    create index(:sweep_host_results, [:tenant_id, :status],
             name: "sweep_host_results_status_idx"
           )

    alter table(:agent_config_versions, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    create index(:agent_config_versions, [:tenant_id, :created_at],
             name: "agent_config_versions_tenant_created_idx"
           )

    drop_if_exists unique_index(:integration_sources, [:name],
                     name: "integration_sources_unique_name_index"
                   )

    create unique_index(:integration_sources, [:tenant_id, :name],
             name: "integration_sources_unique_name_per_tenant_index"
           )

    alter table(:integration_sources, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:tenant_cas, [:status], name: "tenant_cas_unique_active_ca_index")

    create unique_index(:tenant_cas, [:tenant_id, :status],
             name: "tenant_cas_unique_active_tenant_ca_index",
             where: "(status = 'active')"
           )

    alter table(:tenant_cas, prefix: prefix()) do
      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "tenant_cas_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    alter table(:nats_credentials, prefix: prefix()) do
      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "nats_credentials_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    alter table(:snmp_targets, prefix: prefix()) do
      remove :community_encrypted
      add :tenant_id, :uuid, null: false
      add :community, :text
    end

    alter table(:poll_jobs, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    alter table(:health_events, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    create index(:health_events, [:tenant_id, :recorded_at])

    alter table(:alerts, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:sysmon_profiles, [:name],
                     name: "sysmon_profiles_unique_name_per_tenant_index"
                   )

    create unique_index(:sysmon_profiles, [:tenant_id, :name],
             name: "sysmon_profiles_unique_name_per_tenant_index"
           )

    alter table(:sysmon_profiles, prefix: prefix()) do
      remove :disk_exclude_paths
      modify :disk_paths, {:array, :text}, default: nil
      add :tenant_id, :uuid, null: false
    end

    drop_if_exists unique_index(:edge_sites, [:slug], name: "edge_sites_unique_slug_index")

    create unique_index(:edge_sites, [:tenant_id, :slug],
             name: "edge_sites_unique_slug_per_tenant_index"
           )

    alter table(:edge_sites, prefix: prefix()) do
      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "edge_sites_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end
  end
end
