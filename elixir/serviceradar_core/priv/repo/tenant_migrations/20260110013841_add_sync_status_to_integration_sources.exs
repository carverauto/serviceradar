defmodule ServiceRadar.Repo.TenantMigrations.AddSyncStatusToIntegrationSources do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:ocsf_devices, prefix: prefix()) do
      remove :tenant_id
    end

    alter table(:integration_sources, prefix: prefix()) do
      add :sync_status, :text, null: false, default: "idle"
    end
  end

  def down do
    alter table(:integration_sources, prefix: prefix()) do
      remove :sync_status
    end

    alter table(:ocsf_devices, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
    end
  end
end
