defmodule ServiceRadar.Repo.TenantMigrations.AddZenRules do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:zen_rules, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :description, :text
      add :enabled, :boolean, default: true
      add :order, :bigint, default: 100
      add :stream_name, :text, null: false, default: "events"
      add :subject, :text, null: false
      add :format, :text, null: false, default: "json"
      add :template, :text, null: false
      add :builder_config, :map, default: %{}
      add :compiled_jdm, :map, default: %{}
      add :kv_revision, :bigint
      add :agent_id, :text, null: false, default: "default-agent"
      add :tenant_id, :uuid, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:zen_rules, [:tenant_id, :name],
             name: "zen_rules_unique_name_index"
           )
  end

  def down do
    drop_if_exists unique_index(:zen_rules, [:tenant_id, :name],
                     name: "zen_rules_unique_name_index"
                   )

    drop table(:zen_rules, prefix: prefix())
  end
end
