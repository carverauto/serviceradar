defmodule ServiceRadar.Repo.TenantMigrations.AddRuleTemplates do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:zen_rule_templates, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :description, :text
      add :enabled, :boolean, default: true
      add :order, :bigint, default: 100
      add :stream_name, :text, null: false, default: "events"
      add :subject, :text, null: false
      add :template, :text, null: false
      add :builder_config, :map, default: %{}
      add :agent_id, :text, null: false, default: "default-agent"
      add :tenant_id, :uuid, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:zen_rule_templates, [:tenant_id, :name],
             name: "zen_rule_templates_unique_name_index"
           )

    create table(:log_promotion_rule_templates, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :description, :text
      add :enabled, :boolean, default: true
      add :priority, :bigint, default: 100
      add :match, :map, default: %{}
      add :event, :map, default: %{}
      add :tenant_id, :uuid, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:log_promotion_rule_templates, [:tenant_id, :name],
             name: "log_promotion_rule_templates_unique_name_index"
           )

    create table(:stateful_alert_rule_templates, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :description, :text
      add :enabled, :boolean, default: true
      add :priority, :bigint, default: 100
      add :signal, :text, null: false, default: "log"
      add :match, :map, default: %{}
      add :group_by, {:array, :text}, default: ["serviceradar.sync.integration_source_id"]
      add :threshold, :bigint, null: false, default: 5
      add :window_seconds, :bigint, null: false, default: 600
      add :bucket_seconds, :bigint, null: false, default: 60
      add :cooldown_seconds, :bigint, null: false, default: 300
      add :renotify_seconds, :bigint, null: false, default: 21_600
      add :event, :map, default: %{}
      add :alert, :map, default: %{}
      add :tenant_id, :uuid, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:stateful_alert_rule_templates, [:tenant_id, :name],
             name: "stateful_alert_rule_templates_unique_name_index"
           )
  end

  def down do
    drop_if_exists unique_index(:stateful_alert_rule_templates, [:tenant_id, :name],
                     name: "stateful_alert_rule_templates_unique_name_index"
                   )

    drop table(:stateful_alert_rule_templates, prefix: prefix())

    drop_if_exists unique_index(:log_promotion_rule_templates, [:tenant_id, :name],
                     name: "log_promotion_rule_templates_unique_name_index"
                   )

    drop table(:log_promotion_rule_templates, prefix: prefix())

    drop_if_exists unique_index(:zen_rule_templates, [:tenant_id, :name],
                     name: "zen_rule_templates_unique_name_index"
                   )

    drop table(:zen_rule_templates, prefix: prefix())
  end
end
