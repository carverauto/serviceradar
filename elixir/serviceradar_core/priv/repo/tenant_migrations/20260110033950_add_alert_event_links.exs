defmodule ServiceRadar.Repo.TenantMigrations.AddAlertEventLinks do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:alerts, prefix: prefix()) do
      add :event_id, :uuid
      add :event_time, :utc_datetime_usec
    end
  end

  def down do
    alter table(:alerts, prefix: prefix()) do
      remove :event_time
      remove :event_id
    end
  end
end
