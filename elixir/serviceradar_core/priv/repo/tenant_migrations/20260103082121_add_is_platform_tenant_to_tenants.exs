defmodule ServiceRadar.Repo.Migrations.AddIsPlatformTenantToTenants do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:tenants) do
      add_if_not_exists :is_platform_tenant, :boolean, null: false, default: false
    end

    create_if_not_exists index(:tenants, [:is_platform_tenant],
             name: "tenants_unique_platform_tenant_index",
             unique: true,
             where: "is_platform_tenant = true"
           )
  end

  def down do
    drop_if_exists index(:tenants, [:is_platform_tenant],
                     name: "tenants_unique_platform_tenant_index"
                   )

    alter table(:tenants) do
      remove_if_exists :is_platform_tenant
    end
  end
end
