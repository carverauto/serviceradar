defmodule ServiceRadar.Repo.TenantMigrations.AddSnmpProfiles do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create_if_not_exists table(:sysmon_profiles, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :tenant_id, :uuid, null: false
      add :name, :text, null: false
      add :description, :text
      add :sample_interval, :text, null: false, default: "10s"
      add :collect_cpu, :boolean, null: false, default: true
      add :collect_memory, :boolean, null: false, default: true
      add :collect_disk, :boolean, null: false, default: true
      add :collect_network, :boolean, null: false, default: false
      add :collect_processes, :boolean, null: false, default: false
      add :disk_paths, {:array, :text}, null: false
      add :thresholds, :map, null: false, default: %{}
      add :is_default, :boolean, null: false, default: false
      add :enabled, :boolean, null: false, default: true
      add :target_query, :text
      add :priority, :bigint, null: false, default: 0

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create_if_not_exists unique_index(:sysmon_profiles, [:tenant_id, :name],
             name: "sysmon_profiles_unique_name_per_tenant_index"
           )

    create table(:snmp_targets, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
      add :tenant_id, :uuid, null: false
      add :name, :text, null: false
      add :host, :text, null: false
      add :port, :bigint, null: false, default: 161
      add :version, :text, null: false, default: "v2c"
      add :community_encrypted, :binary
      add :username, :text
      add :security_level, :text
      add :auth_protocol, :text
      add :auth_password_encrypted, :binary
      add :priv_protocol, :text
      add :priv_password_encrypted, :binary

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :snmp_profile_id, :uuid, null: false
    end

    create table(:snmp_oid_configs, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
      add :tenant_id, :uuid, null: false
      add :oid, :text, null: false
      add :name, :text, null: false
      add :data_type, :text, null: false, default: "gauge"
      add :scale, :float, null: false, default: 1.0
      add :delta, :boolean, null: false, default: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :snmp_target_id,
          references(:snmp_targets,
            column: :id,
            name: "snmp_oid_configs_snmp_target_id_fkey",
            type: :uuid,
            prefix: prefix(),
            on_delete: :delete_all
          ), null: false
    end

    create unique_index(:snmp_oid_configs, [:snmp_target_id, :oid],
             name: "snmp_oid_configs_unique_oid_per_target_index"
           )

    create table(:snmp_oid_templates, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
      add :tenant_id, :uuid
      add :name, :text, null: false
      add :description, :text
      add :vendor, :text, null: false
      add :category, :text
      add :oids, {:array, :map}, null: false, default: []
      add :is_builtin, :boolean, null: false, default: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:snmp_oid_templates, [:tenant_id, :vendor, :name],
             name: "snmp_oid_templates_unique_name_per_vendor_tenant_index"
           )

    create table(:snmp_profiles, primary_key: false, prefix: prefix()) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
    end

    alter table(:snmp_targets, prefix: prefix()) do
      modify :snmp_profile_id,
             references(:snmp_profiles,
               column: :id,
               name: "snmp_targets_snmp_profile_id_fkey",
               type: :uuid,
               prefix: prefix(),
               on_delete: :delete_all
             )
    end

    create unique_index(:snmp_targets, [:snmp_profile_id, :name],
             name: "snmp_targets_unique_name_per_profile_index"
           )

    alter table(:snmp_profiles, prefix: prefix()) do
      add :tenant_id, :uuid, null: false
      add :name, :text, null: false
      add :description, :text
      add :poll_interval, :bigint, null: false, default: 60
      add :timeout, :bigint, null: false, default: 5
      add :retries, :bigint, null: false, default: 3
      add :is_default, :boolean, null: false, default: false
      add :enabled, :boolean, null: false, default: true
      add :target_query, :text
      add :priority, :bigint, null: false, default: 0

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:snmp_profiles, [:tenant_id, :name],
             name: "snmp_profiles_unique_name_per_tenant_index"
           )

    alter table(:ocsf_agents, prefix: prefix()) do
      add_if_not_exists :config_source, :text
    end
  end

  def down do
    alter table(:ocsf_agents, prefix: prefix()) do
      remove_if_exists :config_source
    end

    drop_if_exists unique_index(:snmp_profiles, [:tenant_id, :name],
                     name: "snmp_profiles_unique_name_per_tenant_index"
                   )

    alter table(:snmp_profiles, prefix: prefix()) do
      remove :updated_at
      remove :inserted_at
      remove :priority
      remove :target_query
      remove :enabled
      remove :is_default
      remove :retries
      remove :timeout
      remove :poll_interval
      remove :description
      remove :name
      remove :tenant_id
    end

    drop_if_exists unique_index(:snmp_targets, [:snmp_profile_id, :name],
                     name: "snmp_targets_unique_name_per_profile_index"
                   )

    drop constraint(:snmp_targets, "snmp_targets_snmp_profile_id_fkey")

    alter table(:snmp_targets, prefix: prefix()) do
      modify :snmp_profile_id, :uuid
    end

    drop table(:snmp_profiles, prefix: prefix())

    drop_if_exists unique_index(:snmp_oid_templates, [:tenant_id, :vendor, :name],
                     name: "snmp_oid_templates_unique_name_per_vendor_tenant_index"
                   )

    drop table(:snmp_oid_templates, prefix: prefix())

    drop_if_exists unique_index(:snmp_oid_configs, [:snmp_target_id, :oid],
                     name: "snmp_oid_configs_unique_oid_per_target_index"
                   )

    drop constraint(:snmp_oid_configs, "snmp_oid_configs_snmp_target_id_fkey")

    drop table(:snmp_oid_configs, prefix: prefix())

    drop table(:snmp_targets, prefix: prefix())

    drop_if_exists unique_index(:sysmon_profiles, [:tenant_id, :name],
                     name: "sysmon_profiles_unique_name_per_tenant_index"
                   )

    drop table(:sysmon_profiles, prefix: prefix())
  end
end
