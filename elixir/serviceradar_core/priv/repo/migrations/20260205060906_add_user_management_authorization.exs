defmodule ServiceRadar.Repo.Migrations.AddUserManagementAuthorization do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:ng_users, prefix: "platform") do
      add :status, :text, null: false, default: "active"
      add :last_login_at, :utc_datetime
      add :last_auth_method, :text
    end

    execute("CREATE SCHEMA IF NOT EXISTS platform")

    create table(:authorization_settings, primary_key: false, prefix: "platform") do
      add :key, :text, null: false, default: "default", primary_key: true
      add :default_role, :text, null: false, default: "viewer"
      add :role_mappings, {:array, :map}, null: false, default: []

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end
  end

  def down do
    drop table(:authorization_settings, prefix: "platform")

    alter table(:ng_users, prefix: "platform") do
      remove :last_auth_method
      remove :last_login_at
      remove :status
    end
  end
end
