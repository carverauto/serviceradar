defmodule ServiceRadar.Repo.Migrations.AddInterfaceAvailableMetrics do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:discovered_interfaces) do
      add :available_metrics, {:array, :map}
    end
  end

  def down do
    alter table(:discovered_interfaces) do
      remove :available_metrics
    end
  end
end
