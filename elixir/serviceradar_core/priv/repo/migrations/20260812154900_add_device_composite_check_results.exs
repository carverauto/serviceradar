defmodule ServiceRadar.Repo.Migrations.AddDeviceCompositeCheckResults do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    execute("CREATE SCHEMA IF NOT EXISTS platform")

    create table(:device_composite_check_results, primary_key: false, prefix: "platform") do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :device_uid, :text, null: false
      add :verdict, :text, null: false
      add :status, :text, null: false
      add :matched_rule_id, :uuid
      add :inputs, :map, null: false, default: %{}
      add :evaluated_at, :utc_datetime_usec, null: false
      add :changed_at, :utc_datetime_usec, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :check_id,
          references(:composite_checks,
            column: :id,
            name: "device_composite_check_results_check_id_fkey",
            type: :uuid,
            prefix: "platform",
            on_delete: :delete_all
          ),
          null: false
    end

    create index(:device_composite_check_results, [:check_id, :evaluated_at],
             name: "device_composite_check_results_check_eval_idx",
             prefix: "platform"
           )

    create index(:device_composite_check_results, [:check_id, :status],
             name: "device_composite_check_results_check_status_idx",
             prefix: "platform"
           )

    create index(:device_composite_check_results, [:check_id, :verdict],
             name: "device_composite_check_results_check_verdict_idx",
             prefix: "platform"
           )

    create index(:device_composite_check_results, [:device_uid],
             name: "device_composite_check_results_device_uid_idx",
             prefix: "platform"
           )

    create unique_index(:device_composite_check_results, [:device_uid, :check_id],
             name: "device_composite_check_results_unique_device_check_index",
             prefix: "platform"
           )
  end

  def down do
    drop constraint(
           :device_composite_check_results,
           "device_composite_check_results_check_id_fkey", prefix: "platform")

    drop_if_exists unique_index(:device_composite_check_results, [:device_uid, :check_id],
                     name: "device_composite_check_results_unique_device_check_index",
                     prefix: "platform"
                   )

    drop_if_exists index(:device_composite_check_results, [:device_uid],
                     name: "device_composite_check_results_device_uid_idx",
                     prefix: "platform"
                   )

    drop_if_exists index(:device_composite_check_results, [:check_id, :verdict],
                     name: "device_composite_check_results_check_verdict_idx",
                     prefix: "platform"
                   )

    drop_if_exists index(:device_composite_check_results, [:check_id, :status],
                     name: "device_composite_check_results_check_status_idx",
                     prefix: "platform"
                   )

    drop_if_exists index(:device_composite_check_results, [:check_id, :evaluated_at],
                     name: "device_composite_check_results_check_eval_idx",
                     prefix: "platform"
                   )

    drop table(:device_composite_check_results, prefix: "platform")
  end
end
