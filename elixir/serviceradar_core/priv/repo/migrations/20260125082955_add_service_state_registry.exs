defmodule ServiceRadar.Repo.Migrations.AddServiceStateRegistry do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:service_state, primary_key: false, prefix: "platform") do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :agent_id, :text, null: false
      add :gateway_id, :text, null: false
      add :partition, :text, null: false
      add :service_type, :text, null: false
      add :service_name, :text, null: false
      add :available, :boolean, null: false
      add :state, :text, null: false, default: "active"
      add :message, :text
      add :details, :text
      add :last_observed_at, :utc_datetime_usec, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(
             :service_state,
             [:agent_id, :gateway_id, :partition, :service_type, :service_name],
             name: "service_state_unique_service_identity_index",
             prefix: "platform"
           )
  end

  def down do
    drop_if_exists unique_index(
                     :service_state,
                     [:agent_id, :gateway_id, :partition, :service_type, :service_name],
                     name: "service_state_unique_service_identity_index",
                     prefix: "platform"
                   )

    drop table(:service_state, prefix: "platform")
  end
end
