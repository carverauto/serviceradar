defmodule ServiceRadar.Repo.Migrations.AddNatsServiceAccounts do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:nats_service_accounts, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :account_public_key, :text
      add :account_jwt, :text
      add :user_public_key, :text
      add :status, :text, null: false, default: "pending"
      add :error_message, :text
      add :provisioned_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :encrypted_account_seed_ciphertext, :binary
      add :encrypted_user_creds_ciphertext, :binary
    end

    create unique_index(:nats_service_accounts, [:name],
             name: "nats_service_accounts_unique_name_index"
           )
  end

  def down do
    drop_if_exists unique_index(:nats_service_accounts, [:name],
                     name: "nats_service_accounts_unique_name_index"
                   )

    drop table(:nats_service_accounts)
  end
end
