defmodule ServiceRadar.Repo.Migrations.AddCompositeChecks do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    execute("CREATE SCHEMA IF NOT EXISTS platform")

    create table(:composite_checks, primary_key: false, prefix: "platform") do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :slug, :text, null: false
      add :description, :text
      add :scope_query, :text, null: false
      add :evaluation_interval_seconds, :bigint, null: false, default: 300
      add :state, :text, null: false, default: "draft"
      add :last_evaluated_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create index(:composite_checks, [:state],
             name: "composite_checks_state_idx",
             prefix: "platform"
           )

    create index(:composite_checks, ["lower(name)"],
             name: "composite_checks_name_uidx",
             unique: true,
             prefix: "platform"
           )

    create unique_index(:composite_checks, [:slug],
             name: "composite_checks_unique_slug_index",
             prefix: "platform"
           )
  end

  def down do
    drop_if_exists unique_index(:composite_checks, [:slug],
                     name: "composite_checks_unique_slug_index",
                     prefix: "platform"
                   )

    drop_if_exists index(:composite_checks, ["lower(name)"],
                     name: "composite_checks_name_uidx",
                     prefix: "platform"
                   )

    drop_if_exists index(:composite_checks, [:state],
                     name: "composite_checks_state_idx",
                     prefix: "platform"
                   )

    drop table(:composite_checks, prefix: "platform")
  end
end
