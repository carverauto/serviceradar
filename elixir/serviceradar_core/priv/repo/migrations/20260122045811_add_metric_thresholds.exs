defmodule ServiceRadar.Repo.Migrations.AddMetricThresholds do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:interface_settings) do
      add :metric_thresholds, :map, default: %{}
    end
  end

  def down do
    alter table(:interface_settings) do
      remove :metric_thresholds
    end
  end
end
