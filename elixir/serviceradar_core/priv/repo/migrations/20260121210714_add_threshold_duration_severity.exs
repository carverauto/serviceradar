defmodule ServiceRadar.Repo.Migrations.AddThresholdDurationSeverity do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:interface_settings) do
      add :threshold_duration_seconds, :bigint, default: 0
      add :threshold_severity, :text, default: "warning"
    end
  end

  def down do
    alter table(:interface_settings) do
      remove :threshold_severity
      remove :threshold_duration_seconds
    end
  end
end
