defmodule ServiceRadar.Repo.Migrations.AddMapperMikrotikControllers do
  @moduledoc """
  Adds MapperMikrotikController resource.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:mapper_mikrotik_controllers, primary_key: false, prefix: "platform") do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text
      add :base_url, :text, null: false
      add :username, :text, null: false
      add :encrypted_password, :binary
      add :insecure_skip_verify, :boolean, null: false, default: false
      add :mapper_job_id, :uuid, null: false

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create index(:mapper_mikrotik_controllers, [:mapper_job_id],
             prefix: "platform",
             name: "mapper_mikrotik_controllers_job_idx"
           )

    create unique_index(:mapper_mikrotik_controllers, [:mapper_job_id, :base_url],
             prefix: "platform",
             name: "mapper_mikrotik_controllers_unique_base_url_per_job_index"
           )
  end

  def down do
    drop_if_exists unique_index(:mapper_mikrotik_controllers, [:mapper_job_id, :base_url],
                     prefix: "platform",
                     name: "mapper_mikrotik_controllers_unique_base_url_per_job_index"
                   )

    drop_if_exists index(:mapper_mikrotik_controllers, [:mapper_job_id],
                     prefix: "platform",
                     name: "mapper_mikrotik_controllers_job_idx"
                   )

    drop_if_exists table(:mapper_mikrotik_controllers, prefix: "platform")
  end
end
