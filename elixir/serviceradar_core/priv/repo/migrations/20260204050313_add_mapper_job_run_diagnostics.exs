defmodule ServiceRadar.Repo.Migrations.AddMapperJobRunDiagnostics do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:mapper_jobs, prefix: "platform") do
      add :last_run_status, :text
      add :last_run_interface_count, :bigint
      add :last_run_error, :text
    end
  end

  def down do
    alter table(:mapper_jobs, prefix: "platform") do
      remove :last_run_error
      remove :last_run_interface_count
      remove :last_run_status
    end
  end
end
