defmodule ServiceRadar.Repo.Migrations.AddPluginEngineLimits do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:ocsf_agents) do
      add :plugin_engine_max_memory_mb, :bigint
      add :plugin_engine_max_cpu_ms, :bigint
      add :plugin_engine_max_concurrent, :bigint
      add :plugin_engine_max_open_connections, :bigint
    end
  end

  def down do
    alter table(:ocsf_agents) do
      remove :plugin_engine_max_open_connections
      remove :plugin_engine_max_concurrent
      remove :plugin_engine_max_cpu_ms
      remove :plugin_engine_max_memory_mb
    end
  end
end
