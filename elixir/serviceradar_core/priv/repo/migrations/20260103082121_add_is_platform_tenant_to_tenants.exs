defmodule ServiceRadar.Repo.Migrations.AddIsPlatformTenantToTenants do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:edge_sites, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "edge_sites_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :name, :text, null: false
      add :slug, :text, null: false
      add :status, :text, null: false, default: "pending"
      add :nats_leaf_url, :text
      add :last_seen_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:edge_sites, [:tenant_id, :slug],
             name: "edge_sites_unique_slug_per_tenant_index"
           )

    alter table(:collector_packages) do
      add :tls_cert_pem, :text
      add :ca_chain_pem, :text

      add :edge_site_id,
          references(:edge_sites,
            column: :id,
            name: "collector_packages_edge_site_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :encrypted_nats_creds_ciphertext, :binary
      add :encrypted_tls_key_pem_ciphertext, :binary
    end

    create table(:nats_leaf_servers, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true

      add :tenant_id,
          references(:tenants,
            column: :id,
            name: "nats_leaf_servers_tenant_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :edge_site_id,
          references(:edge_sites,
            column: :id,
            name: "nats_leaf_servers_edge_site_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false

      add :status, :text, null: false, default: "pending"
      add :upstream_url, :text, null: false
      add :local_listen, :text, null: false, default: "0.0.0.0:4222"
      add :leaf_cert_pem, :text
      add :server_cert_pem, :text
      add :ca_chain_pem, :text
      add :config_checksum, :text
      add :cert_expires_at, :utc_datetime_usec
      add :provisioned_at, :utc_datetime_usec
      add :connected_at, :utc_datetime_usec
      add :disconnected_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :encrypted_leaf_key_pem_ciphertext, :binary
      add :encrypted_server_key_pem_ciphertext, :binary
    end

    create unique_index(:nats_leaf_servers, [:tenant_id, :edge_site_id],
             name: "nats_leaf_servers_unique_per_edge_site_index"
           )

    alter table(:tenants) do
      add :is_platform_tenant, :boolean, null: false, default: false
    end

    # Set the Default Tenant as the platform tenant
    execute """
    UPDATE tenants
    SET is_platform_tenant = true
    WHERE id = '00000000-0000-0000-0000-000000000000'
    """

    create index(:tenants, [:is_platform_tenant],
             name: "tenants_unique_platform_tenant_index",
             unique: true,
             where: "is_platform_tenant = true"
           )
  end

  def down do
    drop_if_exists index(:tenants, [:is_platform_tenant],
                     name: "tenants_unique_platform_tenant_index"
                   )

    alter table(:tenants) do
      remove :is_platform_tenant
    end

    drop_if_exists unique_index(:nats_leaf_servers, [:tenant_id, :edge_site_id],
                     name: "nats_leaf_servers_unique_per_edge_site_index"
                   )

    drop constraint(:nats_leaf_servers, "nats_leaf_servers_tenant_id_fkey")

    drop constraint(:nats_leaf_servers, "nats_leaf_servers_edge_site_id_fkey")

    drop table(:nats_leaf_servers)

    drop constraint(:collector_packages, "collector_packages_edge_site_id_fkey")

    alter table(:collector_packages) do
      remove :encrypted_tls_key_pem_ciphertext
      remove :encrypted_nats_creds_ciphertext
      remove :edge_site_id
      remove :ca_chain_pem
      remove :tls_cert_pem
    end

    drop_if_exists unique_index(:edge_sites, [:tenant_id, :slug],
                     name: "edge_sites_unique_slug_per_tenant_index"
                   )

    drop constraint(:edge_sites, "edge_sites_tenant_id_fkey")

    drop table(:edge_sites)
  end
end
