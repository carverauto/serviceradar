defmodule ServiceRadar.Repo.Migrations.CreateNatsOperatorAndPlatformTokens do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:nats_platform_tokens, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :token_hash, :text, null: false
      add :purpose, :text, null: false, default: "nats_bootstrap"
      add :expires_at, :utc_datetime_usec, null: false
      add :used_at, :utc_datetime_usec
      add :used_by_ip, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:nats_platform_tokens, [:token_hash],
             name: "nats_platform_tokens_unique_hash_index"
           )

    create table(:nats_operators, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false, default: "serviceradar"
      add :public_key, :text
      add :operator_jwt, :text
      add :system_account_public_key, :text
      add :status, :text, null: false, default: "pending"
      add :error_message, :text
      add :bootstrapped_at, :utc_datetime_usec

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end

    create unique_index(:nats_operators, [:name], name: "nats_operators_unique_name_index")
  end

  def down do
    drop_if_exists unique_index(:nats_operators, [:name],
                     name: "nats_operators_unique_name_index"
                   )

    drop table(:nats_operators)

    drop_if_exists unique_index(:nats_platform_tokens, [:token_hash],
                     name: "nats_platform_tokens_unique_hash_index"
                   )

    drop table(:nats_platform_tokens)
  end
end
