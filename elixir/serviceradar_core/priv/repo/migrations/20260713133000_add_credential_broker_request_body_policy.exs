defmodule ServiceRadar.Repo.Migrations.AddCredentialBrokerRequestBodyPolicy do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:credential_broker_grants, prefix: "platform") do
      add :request_body_policy, :map, null: false, default: %{}
    end
  end

  def down do
    alter table(:credential_broker_grants, prefix: "platform") do
      remove :request_body_policy
    end
  end
end
