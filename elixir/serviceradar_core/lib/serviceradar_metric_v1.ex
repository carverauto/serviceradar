defmodule Serviceradar.Metric.V1 do
  @moduledoc false

  # Boundary shell for generated metric v1 protobuf modules
  # (Serviceradar.Metric.V1.*), matching the add-on v1 proto boundary.
  use Boundary,
    top_level?: true,
    exports: :all
end
