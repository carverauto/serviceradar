{
  "type": "object",
  "properties": {
    "host": {
      "type": "string",
      "x-serviceradar-ui-hidden": true,
      "x-serviceradar-credential-materialized": true,
      "description": "UniFi Protect controller host or IP. Injected per target at runtime from credential rules / SRQL-scoped targets or derived from the relay source URL; not configured per assignment."
    },
    "scheme": {
      "type": "string",
      "description": "http or https",
      "default": "https"
    },
    "username": {
      "type": "string",
      "description": "Protect local account username"
    },
    "password": {
      "type": "string",
      "description": "Protect local account password"
    },
    "api_key": {
      "type": "string",
      "description": "Optional Protect API key"
    },
    "cookie": {
      "type": "string",
      "description": "Optional pre-provisioned controller Cookie header value"
    },
    "timeout_ms": {
      "type": "integer",
      "description": "Request timeout in milliseconds",
      "default": 30000
    },
    "insecure_skip_verify": {
      "type": "boolean",
      "description": "Allow self-signed or otherwise untrusted TLS certificates for controller APIs and RTSPS stream transport",
      "default": false
    },
    "bootstrap_path": {
      "type": "string",
      "description": "Protect bootstrap path",
      "default": "/proxy/protect/api/bootstrap"
    },
    "login_path": {
      "type": "string",
      "description": "UniFi OS login path",
      "default": "/api/auth/login"
    },
    "rtsp_port": {
      "type": "integer",
      "description": "Protect RTSP relay port",
      "default": 7447
    },
    "relay": {
      "type": "object",
      "description": "Injected relay session context from the agent streaming runtime"
    }
  }
}
