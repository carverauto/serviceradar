{
  "type": "object",
  "properties": {
    "base_url": {
      "type": "string",
      "x-serviceradar-ui-hidden": true,
      "description": "Agent-local direct target override, for example https://pve.example.com:8006. Central assignments should use credential rules and SRQL-scoped targets."
    },
    "credential_broker": {
      "type": "object",
      "x-serviceradar-internal": true,
      "description": "Internal credential broker grant generated from network credential rules. Do not configure manually."
    },
    "targets": {
      "type": "array",
      "x-serviceradar-ui-hidden": true,
      "description": "Credential-rule compiled Proxmox targets. Credentials are resolved by the edge credential broker or by agent-local direct configuration.",
      "items": {
        "type": "object",
        "properties": {
          "base_url": {
            "type": "string"
          },
          "device_id": {
            "type": "string"
          },
          "hostname": {
            "type": "string"
          },
          "partition": {
            "type": "string"
          }
        },
        "required": [
          "base_url"
        ]
      }
    },
    "timeout_ms": {
      "type": "integer",
      "default": 30000,
      "minimum": 1000,
      "maximum": 300000
    },
    "include_guests": {
      "type": "boolean",
      "default": true
    },
    "insecure_skip_verify": {
      "type": "boolean",
      "default": false
    }
  }
}
