SERVICERADAR_PLUGIN_CONFIG_FILE=testdata/local-config.json
SERVICERADAR_CREDENTIAL_USERNAME=
SERVICERADAR_CREDENTIAL_PASSWORD=
SERVICERADAR_LOCAL_TLS_INSECURE=true
# Required for the opentext-nom.config.retrieve action. Keep it outside the repo:
# the staged config is NA-masked but can still contain sensitive data.
SERVICERADAR_LOCAL_ARTIFACT_DIR=
