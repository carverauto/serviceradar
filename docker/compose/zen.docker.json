{
  "nats_url": "tls://nats:4222",
  "nats_creds_file": "/etc/serviceradar/creds/platform.creds",
  "stream_name": "events",
  "consumer_name": "zen-consumer",
  "subjects": ["logs.syslog", "logs.snmp", "logs.otel", "logs.internal"],
  "discover_rules_from_kv": true,
  "decision_groups": [
    {
      "name": "syslog",
      "subjects": ["logs.syslog"],
      "rules": [],
      "format": "json"
    },
    {
      "name": "snmp",
      "subjects": ["logs.snmp"],
      "rules": [],
      "format": "json"
    },
    {
      "name": "otel_logs",
      "subjects": ["logs.otel"],
      "rules": [],
      "format": "protobuf"
    },
    {
      "name": "internal_logs",
      "subjects": ["logs.internal"],
      "rules": [],
      "format": "json"
    }
  ],
  "agent_id": "docker-zen-consumer",
  "kv_bucket": "serviceradar-datasvc",
  "listen_addr": "0.0.0.0:50040",
  "result_subject_suffix": ".processed",
  "security": {
    "cert_file": "/etc/serviceradar/certs/zen.pem",
    "key_file": "/etc/serviceradar/certs/zen-key.pem",
    "ca_file": "/etc/serviceradar/certs/root.pem"
  },
  "grpc_security": {
    "cert_file": "/etc/serviceradar/certs/zen.pem",
    "key_file": "/etc/serviceradar/certs/zen-key.pem",
    "ca_file": "/etc/serviceradar/certs/root.pem"
  }
}
