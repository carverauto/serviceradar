[linux_ssh_targets]
# Replace these with Linux hosts, Proxmox VE hosts, or VMs reachable by Ansible.
# pve01.example.com ansible_user=admin
# vm-linux-01.example.com ansible_user=ubuntu

[linux_ssh_targets:vars]
# RHEL-like systems usually use sshd. Debian and Ubuntu often use ssh.
serviceradar_sshd_service=sshd
