# Tasks: overhaul-anomaly-engine-reliability

Phases are independently shippable and revertible. Do not start a phase's merge until its verification tasks pass.

## 0. Demo mitigation (config-only, no release)

- [x] 0.1 Audit deployed anomaly addon artifact versions across demo agents. Read-only demo CNPG audit on 2026-07-04 20:52Z: source `ADDON_VERSION` is now 0.2.0; demo packages still approved/imported only through 0.1.20 (`b9a1069f` 0.1.0, `f0e217c6` 0.1.1, `2a15d187` 0.1.19, `c043115c` 0.1.20). Two enabled anomaly profiles target `in:agents` at priority 100 (`66d0746b` legacy 0.1.19 with params bytes 48; `e89a5f67` 0.1.20 with params bytes 266 and `cusum_enabled=true`). Assignments: 13 enabled profile assignments for 0.1.19, 13 enabled profile assignments for 0.1.20, and 2 enabled manual 0.1.1 assignments (`agent-k8s-cp2-worker1`, `k8s-agent`). Live statuses: 11 active 0.1.20 instances plus one active 0.1.1 instance; no 0.2.0 addon is deployed yet.
- [x] 0.2 Set `cusum_enabled=false` in the anomaly AddonProfile params on demo; verify via the 60s config poll that drift events stop: `kubectl exec -n demo cnpg-23 -c postgres -- psql -U postgres -d serviceradar -c "select count(*) from platform.ocsf_events where log_provider='anomaly_detection' and message like 'sustained%' and time > now() - interval '30 minutes'"` returns ~0 within an hour. Applied on demo at 2026-07-04 20:56Z to the enabled 0.1.20 profile and all enabled 0.1.20 profile assignments. Post-settle check at 2026-07-04 21:05Z: `created_at >= '2026-07-04 21:02:00Z'` returned 0 sustained drift rows and the latest sustained row remained 2026-07-04 21:01:07Z. The exact rolling 30m task query returned 0 at 2026-07-04 21:33:23Z; a second check at 21:34Z also returned 0 by event time and created_at.
- [ ] 0.3 Optionally set `n_sigma=4`, `confirm_slots=8` in profile params to damp per-core cpu spike noise; record before/after 24h spike-open counts. Applied on demo at 2026-07-04 20:56Z to the enabled 0.1.20 profile and assignments (`n_sigma=4.0`, `confirm_slots=8`). Before count: 2,220 `breach confirmed%` spike opens in the prior 24h. Interim checks: 2026-07-04 21:34Z had 23 spike opens since the change and 17 in the last 30m; 2026-07-04 23:05Z had 33 spike opens in the last 30m. Post-change 24h count pending.
- [x] 0.4 Delete historical anomaly_detection rows on demo (disposable data): batched `DELETE ... WHERE log_provider='anomaly_detection'` with ctid batching (follow the #4329 retention-prune pattern); confirm hypertable size reduction. Include the 1,898 pre-lifecycle cpu.usage_percent findings (state='anomalous', 2026-06-26..30) that render as permanently-open on device pages. Completed on demo at 2026-07-05 01:56Z after all 12 agents were direct-canaried to the 0.2.0 add-on binary: deleted 455,620 legacy anomaly rows with `producer_version` absent, preserving all producer-versioned 0.2.0 rows. Post-delete verification: `platform.ocsf_events where log_provider='anomaly_detection'` contains 181 rows, all `producer_version=0.2.0`; zero legacy `<none>` anomaly rows remain.
- [x] 0.5 Profile hygiene (RC15): disable or delete the legacy "Default Edge Anomaly Detection" profile (66d0746b, pkg 0.1.19) so exactly one enabled anomaly profile targets `in:agents`; delete the two manual assignments pinning agent-k8s-cp2-worker1 and k8s-agent to stale v0.1.1 (pkg f0e217c6) and verify the reconciler upgrades them to 0.1.20. Completed on demo at 2026-07-04 20:56Z by disabling the legacy 0.1.19 profile plus its 13 enabled profile assignments, and disabling (not deleting, to preserve audit history) the two enabled 0.1.1 manual pins. Reconciler created 0.1.20 profile assignments for `agent-k8s-cp2-worker1` and `k8s-agent`; current state is 15 enabled 0.1.20 profile assignments, zero enabled 0.1.19/0.1.1 assignments, and active statuses all 0.1.20.
- [x] 0.6 Confirm addon 0.1.20 checkpoint behavior without `checkpoint_path` (likely disabled); decide whether to add `checkpoint_path` to the active profile before the 0.2.0 roll so fleet restarts don't cold-start baselines; note the fleet-wide "resource limits not enforced" cgroup degradation for the rollout plan. Read-only demo CNPG audit on 2026-07-04 20:52Z: neither enabled anomaly profile nor any enabled profile assignment has `checkpoint_path`; delivered `seasonal_baselines` count is 0 everywhere. Decision: add `checkpoint_path` to the single surviving 0.2.0 profile before demo rollout. All 11 active 0.1.20 statuses report `resource limits not enforced` due cgroup permission denied; the single active 0.1.1 status has no degradation reason.

## 1. Edge correctness (addon 0.1.20 → 0.2.0)

- [x] 1.1 Apply the near-zero scale guard to the CUSUM anchor/baseline scale: replace `effective_scale(...).max(f64::EPSILON)` at `rust/anomaly-addon/src/engine/mod.rs:400-404` with the `effective_scoring_scale` guard (`rust/anomaly-core/src/stats.rs:283-313`); add `min_cv=0.05` to the counter-class drift profile
- [x] 1.2 Add `drift_mode ∈ {off, deseasonalized_only, always}` per metric class in `metrics_classify.rs` SeriesProfile; defaults: interface/counter=deseasonalized_only, cpu.usage_percent/memory.used_percent=deseasonalized_only, disk/other=off; remove the global runtime `cusum_enabled` gate; the 0.2.0 schema/UI no longer exposes the legacy global knob, and stale profile data carrying it is scrubbed/ignored
- [x] 1.3 Gate CUSUM updates in `deseasonalized_only` mode on a resolved seasonal center (`engine/mod.rs:412-416`); count skipped series in addon telemetry as `drift_inactive_no_baseline`
- [x] 1.4 Implement latch-and-confirm: crossing h latches drift-pending (no emit, no reset); confirm at `h_confirm = h * h_confirm_mult` (default 1.5) within `drift_confirm_window` samples else silent decay; raise default h 5.0 → 8.0
- [x] 1.5 Compute bounded shift estimate δ̂ = k + S/N at confirm; require `drift_min_effect` (default 2.0); expose δ̂ in the finding payload as the drift score (cap all stored scores at 50)
- [x] 1.6 Implement the drift episode state machine (idle→pending→open→clearing→cleared) modeled on `engine/mod.rs:532-568`: one `anomaly_drift_open`, severity-escalation update, `episode_update_interval_secs` heartbeat (default 1800), clear on recovery (`drift_clear_slots`, default 30) or adoption (`drift_adopt_after_samples`, default 600) with reason "level adopted as new baseline"; block adoption inside saturation bands; `reopen_cooldown_secs` (600) reuses episode identity with `reopen_count`
- [x] 1.7 Re-anchor (center and scale) after every clear/adoption; add `anchor_max_age_secs` (86400) idle refresh for `always` mode; scale refreshes from the current rolling window each evaluation
- [x] 1.8 Apply per-class saturation gates and dispersion floors to the drift path (gate parity with the z path)
- [x] 1.9 Severity: replace `severity_id_from_score` (`verdict.rs:269-281`) with per-class banded mapping (Low<4, Medium 4–8, High ≥8, Critical = High + impact test + persisted ≥`critical_min_duration_secs` 600); practical-significance gates (≥5pp percent gauges, ≥30% relative counters); drift caps at High, opens Medium, escalates after `drift_escalate_after_secs` (3600); per-core (`tag_core_id`) caps at High; delete drift `confirmed=true`-by-construction (`verdict.rs:152-155`)
- [x] 1.10 Emission governance: per-series `emission.cooldown_secs` (300, clears exempt, coalesce to terminal state); per-addon `emission.budget_per_tick` (100) with storm-mode hysteresis, ~20% reserved sub-budget for novel opens, latest-state-wins queue compaction, priority ordering, and `anomaly_emission_shed` rollup records via `shed.rs`; CI invariant: detected transitions == emitted + rollup-accounted
- [x] 1.11 Add `metric_denylist` (default `["cpu.frequency_hz"]`); add `pid`/`start_time` to excluded volatile keys in `identity.rs:13-28`
- [x] 1.12 Stamp `producer_version` (ADDON_VERSION) and `episode_uid`/`transition` fields on every emitted record; transition row event_id is deterministic from (episode_uid, transition, severity_band) — remove ts_nano from row identity (`verdict.rs:164`)
- [x] 1.22 Host-aggregate CPU (design D14): addon-side synthesis per identity+metric with core_id stripped (mean utilization + count/fraction of cores above gate) on the 30s slot cadence, handling partial-core slots; host aggregate is the Critical-eligible alerting series; per-core series stay detected but cap at High; consider dual max+mean slot statistics so a 1s spike per slot cannot sustain an episode
- [x] 1.23 Harness scenario: slow leak (0.05σ/hr ramp) — opens within ARL bound, Medium→High escalation, ≤4 event rows total (drift recall preserved)
- [x] 1.24 Harness scenario: adversarial flapper (series oscillating at threshold) — converges to one flap-annotated episode, bounded rows/day per the cooldown math
- [x] 1.25 Harness scenario: severity calibration corpus — replay captured demo payload distribution; assert Critical share <1% and no stored score above the bound; assert genuine sustained single-core saturation still opens (spike recall — the current gate+MAD combination produced ZERO cpu opens in 48h, the opposite failure)
  - [x] 1.25.a Synthetic local severity corpus covers the bad demo classes (per-core CPU, memory practical-significance, interface counters, disk, one true sustained saturation) and asserts Critical share <1% plus stored score ≤50
  - [x] 1.25.b Sustained single-core CPU saturation recall is pinned by the frame harness (`one_busy_core_does_not_emit_host_cpu_aggregate_open`)
  - [x] 1.25.c Captured demo protobuf payload replay uses committed MetricBatch fixtures sampled from the demo JetStream `metrics` stream (`rust/anomaly-addon/src/tests/demo_metric_corpus_data.rs`) and passes `cargo test --manifest-path rust/anomaly-addon/Cargo.toml --lib captured_demo_metric_corpus -- --nocapture`, asserting Critical share <1% and stored anomaly scores ≤50
- [x] 1.13 Persist episode + drift state (anchor, S±, pending/open, started_ns) in the checkpoint (`engine/checkpoint.rs`); restarts must not fork episodes
- [x] 1.14 Extend `addons/anomaly-addon/config.schema.json` additively (metric_classes, emission, seasonal.max_baselines, metric_denylist); serde-default all new fields; two-direction compat test (old config→new addon, new config→old addon)
- [x] 1.15 Bump addon to 0.2.0; update `addons/anomaly-addon/addon.yaml`
- [x] 1.16 Harness scenario: diurnal sinusoid (14d, 10× day/night amplitude, 30s cadence) — 0 drift episodes with delivered baseline; ≤2 episodes/day in `always` mode via adoption; legacy raw mode kept as regression sentinel documenting the 42–74% FP
- [x] 1.17 Harness scenario: quiet/pinned interface + traffic burst — no record with score >50, no Critical, ≤1 episode (`harness_quiet_interface_burst_bounds_score_severity_and_episode_count`)
- [x] 1.18 Harness scenario: regime change (permanent +3σ step) — exactly one open + one clear ("level adopted"), then ≥7 simulated days of silence (`harness_regime_change_adopts_once_then_stays_silent_for_seven_days`)
- [x] 1.19 Harness scenario: restart storm (checkpoint-less restart mid-cycle) — zero episodes from re-warm on baseline-covered series; bounded ≤4/day otherwise (`harness_checkpointless_restart_storm_is_silent_with_baseline_and_bounded_without`)
- [ ] 1.20 Harness invariant across all scenarios: min inter-record gap ≥ cooldown; ≤6 drift rows/series/day; every shed accounted in a rollup record; wire scorecard floors (spike precision ≥0.98, deseasonalized drift FP ≤1%, drift recall ≥ current 216/300, zero unbounded scores) as blocking CI
  - [x] 1.20.a Emission cooldown and shed accounting are covered by frame-path tests (`process_frame_cooldown_suppresses_non_clear_and_rolls_up`, `process_frame_budget_sheds_with_accounted_rollup_and_storm_entry`)
  - [x] 1.20.b Drift-row/day bounds are covered by the diurnal, slow-leak, flapper, regime-change, and restart-storm harnesses
  - [x] 1.20.c Full historical scorecard floors (precision/recall against the 216/300 labeled corpus). Historical note: the 2026-07-04 repo audit found only the unlabeled demo MetricBatch replay fixture (`rust/anomaly-addon/src/tests/demo_metric_corpus_data.rs`) and no checked-in labeled corpus. DONE 2026-07-12: the "216/300 corpus" was identified as the deterministic `tools/anomaly-proof/gen.py` synthetic corpus (seed 1234, 3 weeks @ 60s, byte-reproducible); it is now committed gzipped at `rust/anomaly-core/testdata/scorecard/` (3.2 MB, regeneration command + sha256 in its README). A Rust port of plot.py's span-matching scorer landed as `serviceradar_anomaly_core::scorecard` (shared by a new `anomaly-backtest --truth <csv>` mode and the gate; the CLI replay was refactored onto the same `SeriesState::step` and verified byte-identical across plain/cusum/gated replays). CI gate `//rust/anomaly-core:scorecard_gate_test` runs in the plain `bazel test tests(//...)` sweep and enforces floors just below the re-measured baseline: spike precision 1.000 (floor 0.98), z-catchable span recall 4/4, blip 0/1 (hysteresis), deseasonalized CUSUM drift recall 230/300=0.767 (floor 0.75; the historical 216/300 predates current kernels — re-baselined), drift FP cpu 0.44% / rate 0.99% (ceiling 1%; mem 38.1% and disk 1.08% measured but deliberately not gated, rationale in the test), max |score| 44.65 all-finite (bound 50), gated disk 0 FP + 1/1. Honesty caveat: this proves the anomaly-core kernels + CLI deseasonalization plumbing, NOT the addon frame path (cooldown/shed/rollup) — production-path coverage remains the 1.20.a/1.20.b in-crate tests.
- [x] 1.21 `bazel test` targets updated for new/changed Rust files (BUILD drift gotcha). Verified native add-on packaging on 2026-07-04 with `bazel build //build/native_addons:anomaly_addon_bundle` and `bazel test //build/native_addons:build_gates_test`; the build gates passed inventory consistency, manifest validation, publish-negative, and dependency isolation (portability/size skipped on macOS). Important caveat found during demo canary: the macOS-built `anomaly_addon_bundle.linux.amd64.tar.gz` metadata said Linux/amd64 but contained a host Mach-O arm64 Rust binary, because Rust native add-ons are packaged directly rather than cross-compiled in `build/native_addons/defs.bzl`. Fixed by adding an assembler-level ELF header/machine validator and Bazel test; post-fix `python3 build/native_addons/assemble_addon_bundle_test.py`, `bazel test //build/native_addons:assemble_addon_bundle_test`, and `bazel test //build/native_addons:build_gates_test` pass. After the fix, `bazel build //build/native_addons:anomaly_addon_bundle` on macOS fails closed with `expected linux/amd64 ELF executable ... not ELF`. The demo canary used a Linux x86_64 host-built ELF instead.
  - [x] 1.21.a Live canary found a second host-build portability trap: an Ubuntu/glibc-2.39 Linux ELF (`sha256=c0d4e90f3cf8ae4ae3990f0e919717f1c1cf7f8c8f8794c4dd4be5f5359ae05b`) required `GLIBC_2.38` and failed on Debian Bookworm agents. Direct operational canaries must build on the lowest supported glibc baseline (or use the normal Bazel/package portability gate) rather than an arbitrary host checkout. The replacement Bookworm-built ELF (`sha256=950175adb75354b0762ed9c4b6677d28e52802bcbfccce784a3887c615f599ac`) was smoke-tested on `ns01` before fleet normalization.

## 2. Core ingest episodes (core-elx)

- [x] 2.1 Close the class-1008 fallthrough for anomaly-shaped payloads: extend `anomaly_detection_signal?` (`analytics_signals.ex:~1339`) to match legacy `signal_type=causal` with anomaly event_types; escape hatch env `SERVICERADAR_ANOMALY_LEGACY_CLASS1008` (default off)
- [x] 2.2 Harden the confirmation gate: withhold any payload whose reason starts with "breach pending" regardless of claimed state (`analytics_signals.ex:1777-1814`)
- [x] 2.3 Create `ServiceRadar.Observability.AnomalyEpisode` Ash resource → `platform.anomaly_episodes` (attrs per design D3) with SystemActor-compatible policies. AshPostgres dry-run was too broad because this repo has no existing snapshots, so the checked migration is hand-scoped to the episode table
- [x] 2.4 Episode registry in AnalyticsSignals: parse `transition`/`episode_uid` (fallback inference for legacy producers); open → episode insert + one OCSF Create row; update → episode update + OCSF row only on severity-band escalation; clear → close episode + OCSF Close row; duplicate open → fold (occurrence_count), no row. Implemented with one DB upsert CTE that returns prior state, so there is no separate episode lookup to cache.
- [x] 2.5 Per-finding_uid rate guard: max 12 persisted OCSF rows/hour/finding; overflow folds into the episode + `[:serviceradar, :anomaly, :governor]` telemetry
- [x] 2.6 Stale sweep: close episodes unseen 30min as `stale_closed` (reuse state_machine.ex sweep pattern)
- [x] 2.7 Payload slimming for anomaly rows: `raw_data` nil; single canonical payload copy in `unmapped`; summary-only `metadata.detection_finding`; target ≤2KB/row
  - [x] 2.7.a Class-2004 anomaly/capacity finding rows now store the decoded producer payload once in `unmapped` and set `raw_data` to nil; focused AnalyticsSignals test locks the contract
  - [x] 2.7.b Verified persisted anomaly row payload ≤2KB in the srql-fixtures scratch DB episode-path test
- [x] 2.8 Transition-only alert enqueue at `analytics_signals.ex:113-114`; add `anomaly_drift_open`/`anomaly_drift_clear` to the seeded rule open/recovery matchers plus a forward-only seeded-rule data migration
- [x] 2.9 Ingest severity backstops in `normalize_severity`: `edge-drift` ≤ High; "breach pending" ≤ Low
- [x] 2.10 VerdictEmitter (central-seasonal): drop (dow,hod) from finding identity (`verdict_emitter.ex:123-145`); align severity to the shared band table (`:237-245`)
- [x] 2.11 Core flood tripwire: operational event when anomaly upserts/min exceed threshold
- [x] 2.12 Feature flag `EVENT_WRITER_ANOMALY_EPISODES` (default on for demo after soak); legacy per-report path retained one release
- [x] 2.13 Tests via srql-fixtures scratch DB: classification (no anomaly payload reaches 1008), pending-reason gate, episode upsert idempotency (same payload twice = 1 episode, 1 row), stale-producer replay (≤1 row + folded episode), payload slimming, transition-only enqueue, rate guard, stale-sweep close semantics
  - [x] 2.13.a Focused AnalyticsSignals tests cover legacy causal anomaly classification (confirmed → class 2004; pending withheld instead of falling through to class 1008)
  - [x] 2.13.b Focused AnalyticsSignals tests cover the breach-pending gate and ingest severity backstops (breach pending ≤ Low, edge drift ≤ High)
  - [x] 2.13.c Focused AnalyticsSignals test covers anomaly raw-payload slimming (`raw_data` nil, canonical payload retained in `unmapped`)
  - [x] 2.13.d Focused fake-repo tests cover episode open/upsert, duplicate-open folding, severity-escalation emission, drift-clear emission, rate-guard folding/telemetry, transition identity stamping, and stale-sweep SQL semantics
  - [x] 2.13.e srql-fixtures DB coverage passed on a freshly migrated scratch database: legacy duplicate delivery persists once and enqueues once, episode replay folds into one transition row with occurrence_count updates and stale-close, persisted payload stays under 2KB, and the DB-bound rate guard folds over-limit transitions after episode upsert
- [x] 2.14 Verify `MIX_ENV=prod mix release` builds clean (hackney/h2 duplicate-module gotcha)
- [x] 2.15 Alert pipeline restoration (design D13, RC14): root-cause why the stateful alert engine stopped matching anomaly events at the 2026-06-30 02:40Z signals.analytics.* cutover (rule histories flatline; ingestion consumer healthy) — fixed legacy anomaly Detection Finding canonicalization (`signal_type=prediction` for alert metadata/evaluation), added `anomaly_drift_open`/`anomaly_drift_clear` matcher support, and covered seeded-rule open/clear matching
- [x] 2.16 Alert persistence: traced dangling `alert_ids` to retention pruning active alerts by `triggered_at`; fixed `AlertsRetentionWorker` to prune terminal alerts only (`resolved`/`suppressed`) so active anomaly alerts remain visible in the alerts UI
- [x] 2.17 Post-deploy alert liveness check (command-level, blocking): added `ServiceRadar.Observability.AnomalyAlertLivenessCheck` and `mix serviceradar.anomaly_alert_liveness` (synthetic confirmed drift open → seeded rule fire → persisted alert → drift clear recovery); live demo execution remains the Phase 6.7 gate

## 3. Capacity soundness + interface rate integrity + UI legibility

- [x] 3.1 Capacity eligibility: default source list restricted to monotone consumables (disk, memory working set); `cpu.usage_percent` and interface utilization excluded by default with per-source opt-in (`capacity_forecasting/source.ex:44-105`); if CPU/interface forecasting is opted in, it forecasts daily p95 of busy-hour utilization against an 80–90% threshold, never raw hourly-avg against 100%
  - [x] 3.1.a `Source.defaults/1` now includes only memory + disk monotone consumables by default; CPU, interface, and flow sources require explicit `include_sources` / worker `default_source_opt_ins`
  - [x] 3.1.b Opted-in CPU/interface sources use a `daily_p95` sustained statistic over hourly buckets with a 90% threshold; worker tests verify the model sees daily p95 samples, not raw hourly averages
- [x] 3.2 Capacity significance gates (`rust/anomaly-disposition/src/disposition/capacity/`): robust slope (Theil–Sen or HAC-adjusted CI) excluding zero; trend persisted m-of-n consecutive runs; extrapolation capped at ~1–2× observed history span (demo fit 7d, extrapolated 90d); ETA gated on the PI **lower-bound** crossing (PI already computed in `linear.rs:112-135`/`holt_winters.rs:252-298`, today display-only); seasonality autodetect uses all complete periods, not the first 24h (`holt_winters.rs:199-241`)
  - [x] 3.2.a Linear ETA projection now caps threshold crossings to the smaller of the legacy 10×-horizon bound and ~2× the observed history span; regression coverage documents the intentional legacy parity divergence when a captured ETA exceeds the new cap
  - [x] 3.2.b Worker gates `projected` findings on the PI lower bound crossing the threshold; skipped rows retain lower/upper bounds in diagnostics so the no-projection decision is auditable
  - [x] 3.2.c Forecast verdict state must persist across consecutive worker runs before it opens or clears; the worker computes confirmed state from bounded prior `capacity_forecasts` snapshots, so a single noisy run is persisted but cannot flip alert state
  - [x] 3.2.d Linear runway findings now require a positive robust Theil-Sen slope with MAD residual CI excluding zero (unless already above threshold); auto-seasonality detection builds its seasonal profile from all complete periods and ignores incomplete tails
- [x] 3.3 Capacity emission becomes transition-only with hysteresis (projected↔cleared state changes), replacing per-series-per-run re-emission (`worker.ex:546-575`, 3,067 events/run, ingest backlogged 2–4 days); runway findings follow episode semantics (one open, refreshed, cleared on retreat)
  - [x] 3.3.a Worker persists every forecast snapshot but emits NATS verdicts only on at-risk `projected` opens or projected→cleared transitions (`inactive`/`skipped`), using the latest prior persisted forecast for the same resource/metric/horizon; unchanged projected state is folded, and first-run cleared state does not mint synthetic clears
  - [x] 3.3.b Multi-run hysteresis is enforced with `capacity_transition_confirm_runs` (default 2) and a bounded prior-state scan, so opens and clears require confirmed state rather than one sample
- [x] 3.4 Capacity attribution + config honoring: interface-source resource_id = device (or device:if_index), not the partition key field (`worker.ex:873-879` — 97% of findings land on device 'default'); `maybe_put_forecast_model` forwards `:linear` (`anomaly_config_runtime.ex:290-293`); worker reads config transactionally at run start (kills the per-node 80↔100 threshold flap)
  - [x] 3.4.a `AnomalyConfigRuntime` forwards the Settings UI `:linear` forecast model to the worker instead of silently dropping it
  - [x] 3.4.b Interface-source `resource_id` now prefers device identity plus `if_index`, so placeholder partitions such as `default` do not become the forecast attribution
  - [x] 3.4.c Worker reads capacity settings at run start through a transactional `CapacityForecastConfig` load; tests keep cache mode explicit for db-free runs and prove direct fetch wins over a stale cache
- [x] 3.5 Interface rate integrity — single conversion site (design D10, commit 7955a1a0a regression): device-page panels (`device_live/interface_data.ex:429`) stop stamping `rate_mode: :counter` on `agg:rate` responses; interface detail page (`interface_live/show.ex` + `metrics_query.ex`) restored to a consistent contract (either `agg:max`+`:counter` or `agg:rate`+label-only); delete the stale "client-side rate deltas" comment; verify tonka01 eth9 renders ~344 KB/s continuous, not 1.8 KB/s dashes
- [x] 3.6 SRQL `agg:rate` hardening (`rust/srql/src/query/downsample/sql.rs:193-216`): partition LAG by polling series (series_key/agent), aggregate per-series rates into the bucket; bound the 2⁶⁴ wrap branch with a plausibility ceiling (mirror the edge normalizer's `plausible_counter_delta`)
- [x] 3.7 Drop accounting: UI `counter_rates` and edge normalizer drop rules (gap/reset/decrease) gain counted-reason telemetry; codify 64-bit polling preference + `counter_width` stamping (verified working) as spec regression locks. UI emits `[:serviceradar, :web_ng, :timeseries, :counter_rate, :dropped]` by reason; edge health includes `counter_rate_drop_*_total`; focused Go tests lock Counter64 and `counter_width` propagation.
- [x] 3.8 web-ng finding modals: decode v2 hex series keys (encoder `serviceradar_core .../anomaly_detection/series_key.ex`; no decoder exists — add one), demote raw key to tooltip; populate INTERFACE/RESOURCE from `metadata.source_identity` tags (core_id, label, mount_point)
- [x] 3.9 web-ng markers: capacity focus timestamp leads with `forecasted_at` (`anomaly_capacity_components.ex:565-583`); out-of-range markers clamp to chart edge with an explicit "outside this window" caption (`series_data.ex:329-337` currently returns nil silently); marker helper text renders only when a marker exists; "Observed" shows `forecasted_at` and the crossing gets a separate "Projected crossing" field (`:435,1160-1164`); drop/rename the constant "PI coverage 95%" display; capacity modal reflects the latest run's state per series (`anomaly_capacity_data.ex:286-299`)
- [x] 3.10 web-ng charts: fix the `@chart_pad` undeclared-assign crash in `chart_card.ex:84-86` (detection-window bands have never rendered — they raise); compute left pad from widest formatted tick (labels >62 SVG units are viewport-clipped — the garbled ladder); render single-point segments as visible dots (`paths.ex:120-121` emits bare `M x,y`); position points by timestamp, not array index
- [x] 3.11 web-ng events page: debounce/coalesce the per-broadcast refresh (`event_live/index.ex:94-101` re-runs list + both summaries on every EventWriter batch per client); align stat-tile click-through queries with the rollup predicates that produced the counts (`:262,269`)
- [x] 3.12 web-ng: device anomaly panel reads `anomaly_episodes` (bounded) instead of paging raw ocsf_events; verify locally (mix test + local phx.server + Playwright per project conventions)
  - [x] 3.12.a `AnomalyCapacityData` defaults anomaly rows to the bounded `ServiceRadar.Observability.AnomalyEpisode` Ash resource, keeps legacy SRQL as an injectable compatibility source for tests, and preserves `episode_uid` through row projection
  - [x] 3.12.b Focused db-free coverage verifies the device panel uses a bounded episode source rather than `in:events`, keeps legacy fallback behavior, and renders cleared episode rows when `episode_uid` is present
  - [x] 3.12.c srql-fixtures scratch DB coverage passed on a freshly migrated database containing `platform.anomaly_episodes`: seeded episode rows are read through the Ash resource with a viewer scope, projected into the device-panel row contract, and the raw `in:events` anomaly path is not called
  - [x] 3.12.d Local Phoenix + Playwright passed against a migrated srql-fixtures scratch database containing `platform.anomaly_episodes`: `/devices/router-1` rendered the seeded "Host CPU saturation" episode with `cpu.usage_percent` and the episode reason, and did not render the seeded `router-2` control row

## 4. Seasonal coverage for interface counters

- [x] 4.1 Pre-flight: srql-fixtures scratch DB verification passed on a freshly migrated database. The generic `platform.timeseries_metrics_hourly` CAGG has no interface identity/rate columns (`partition`, `target_device_ip`, `if_index`, `series_key`, `avg_rate_per_second` absent), so it is not suitable for interface seasonal baselines. The existing `platform.timeseries_metrics_interface_hourly` CAGG has `partition`, `device_id`, `target_device_ip`, `if_index`, `metric_type`, `metric_name`, `series_key`, and `avg_rate_per_second`; a synthetic two-interface counter replay refreshed into two distinct rows (`if_index=1 rate=2.00`, `if_index=2 rate=3.50`). Decision: no new CAGG; Phase 4 uses the existing interface hourly CAGG through a dedicated interface profile path.
- [x] 4.2 Add `interface_seasonal` sources for `ifInOctets`, `ifOutOctets`, `ifInUcastPkts`, and `ifOutUcastPkts` rates to `seasonal_disposition/source.ex` for baseline DELIVERY only; central disposition verdict scope unchanged. Implemented SRQL `profile_hour_of_week(value)` support over `timeseries_metrics_interface_hourly`, profiling `avg_rate_per_second` by `(device_id, if_index, metric_name)` and returning `if_index` in the payload. `EdgeBaselineProducer` now uses a baseline-delivery predicate instead of the central-disposition predicate and keys interface baselines as `<device_uid>|<metric_name>|<if_index>` when rows carry `if_index`. Verified by focused Elixir seasonal-disposition tests plus SRQL unit/translation tests.
- [x] 4.3 EdgeBaselineProducer: 3-segment keys `<device_uid>|<metric_name>|<if_index>`; per-agent scoping; top-K interfaces per device (default 16 by traffic); ≥3 weeks history gate; compact f32 array encoding; hard per-agent cap (1,000 series) + truncation telemetry. Implemented a governed delivery plan: CPU/memory baselines remain profile-wide; interface rows carry `partition`/agent metadata through hydration, are history-gated, ranked by traffic, encoded as 168-slot compact arrays, capped per scope, and written only onto matching `AddonAssignment.params`. `AddonProfileReconciler` preserves assignment-scoped three-segment interface baselines across periodic profile reconciles while profile-owned host baselines still override. Verified by focused seasonal-disposition/reconciler tests and Rust config-parser tests.
- [x] 4.4 Edge: `seasonal_series_key` appends if_index (`identity.rs:172-188`); 3-segment lookup with 2-segment fallback. Implemented `seasonal_series_key` as `<device_uid>|<metric_name>|<if_index>` when `MetricPoint.if_index > 0`; `DetectorEngine` resolves exact keys first and falls back to the legacy two-segment key only when the last segment is numeric. Verified by focused Rust identity/seasonal tests, including exact-key-wins and legacy-fallback cases.
- [ ] 4.5 Measure delivered params payload size on demo before enabling; enable interface drift (deseasonalized_only activates automatically as baselines arrive). Read-only demo check on 2026-07-04 20:52Z: enabled anomaly profiles are still `0.1.20` and legacy `0.1.19` tied at priority 100, plus one active stale `0.1.1` status; delivered `seasonal_baselines` are 0 on all anomaly profiles/assignments, max params payload is 266 bytes. Do not enable/check off until the 0.2.0 addon + interface baseline producer are deployed and one enabled profile owns anomaly delivery.
- [x] 4.6 Absorb align-edge-anomaly-series-key remaining parity tasks: core re-key now emits the Rust edge fallback key shape (`v2|partition|identity|metric|interface_uid|if_index|tag_*`) with matching volatile tag exclusions (`pid`/`process_id`/`start_time`/`start_time_unix_nano`); byte-parity tests pin host CPU and SNMP interface fixtures; analytics structured-key detection accepts both legacy colon and edge pipe v2 keys; the DB-backed stateful alert engine tuple-grouping test passes with an edge-style pipe key while grouping on `device_id`, `anomaly.metric_name`, and `anomaly.if_index`
- [x] 4.7 Harness scenario: seasonal interface series with delivered baseline — Rust engine harness derives the delivered three-segment interface key (`<device_uid>|<metric_name>|<if_index>`), proves clean diurnal interface data produces zero CUSUM drift rows, then injects a sustained 2× shift and verifies exactly one upward drift open with episode metadata and no lifecycle annuity; covered by `cargo test --manifest-path rust/anomaly-addon/Cargo.toml --lib`

## 5. Config unification + docs + OpenSpec debt

- [x] 5.1 `AnomalyAddonConfigProjector` Oban worker (pattern: EdgeBaselineProducer): projects the Settings singleton + edge-safe `metric_class_overrides` into the `managed` sub-key of anomaly AddonProfile params; flag `SERVICERADAR_ANOMALY_EDGE_CONFIG_PROJECTION` + cron override; add-on schema/parser accepts `managed`; add-on merge order is `managed` < operator-explicit top-level params
- [x] 5.2 Settings > Anomaly Detection UI: replaces the dead raw edge-override JSON textarea/stale copy with structured per-class controls (`enabled`, `drift_mode`, CUSUM/drift lifecycle knobs, severity cap/bands), metric denylist, and emission governance; singleton gains first-class `metric_denylist` + `emission` fields with additive migration and Helm seeding; projector writes these into `managed`; add-on schema/parser resolves managed denylist/emission below explicit profile params
- [x] 5.3 Reconciler property test: baseline producer and projector write disjoint keys; operator params never clobbered. Projector tests preserve `seasonal_baselines` + explicit top-level overrides while writing only `managed`; baseline producer tests preserve `managed` while writing only `seasonal_baselines`
- [x] 5.4 Docs rewrite (`docs/docs/anomaly-detection.md`, `docs/docs/anomaly-engine.md`): drift glossary incl. reason strings, score semantics and bounds, severity contract (what Critical means), episode lifecycle, expected healthy event volume, tuning guide mapping UI knobs to edge behavior, cold-start posture
- [x] 5.5 Created `openspec/specs/anomaly-detection/spec.md` baseline from the overhaul deltas; archived completed changes with spec application (`add-causal-anomaly-detection`, `move-anomaly-detection-to-edge`, `refactor-anomaly-engine-rigor`, `refactor-anomaly-reasoner-deepcausality`); archived obsolete/superseded/absorbed changes with `--skip-specs` so stale deltas do not overwrite the new baseline (`update-anomaly-evaluation-cadence`, `optimize-anomaly-production-path-2m`, `add-core-causal-disposition-nif`, `add-anomaly-finding-disposition`, `add-seasonal-anomaly-detection`, `fix-anomaly-engine-semantics-and-delivery`, `align-edge-anomaly-series-key`); `openspec validate --all --strict` passes

## 6. Live verification (blocking gates between phases)

- [ ] 6.1 Post-Phase-0 (demo, 24h): drift reasons ≈0/day; total anomaly events <10k/day. Current pre-mitigation demo baseline on 2026-07-04 20:52Z is not passing: 81,459 anomaly rows in 24h, 77,076 sustained drift rows, 44,580 Critical rows, 192 distinct series keys. Phase-0 mitigation applied 2026-07-04 20:56Z; post-settle sustained drift stopped after 2026-07-04 21:01:07Z (`created_at >= 21:02Z` returned 0 at 21:05Z). Interim sample at 2026-07-04 23:05Z: last 30m had 0 sustained drift rows and 98 total anomaly rows. Full 24h soak remains pending.
- [ ] 6.2 Post-Phase-1 (demo, 48h soak, addon version pinned and verified on 100% of rows via producer_version): total anomaly events <1,000/day fleet-wide; Critical share <5%; max events/series/day ≤20; zero rows with score >50; median per-series re-fire gap >1h. Direct-agent canary started on 2026-07-04/05 UTC before a signed package import: built `serviceradar-anomaly-addon` on Linux x86_64 `sr-test-pve04` (`sha256=c0d4e90f3cf8ae4ae3990f0e919717f1c1cf7f8c8f8794c4dd4be5f5359ae05b`, ELF x86-64), atomically replaced the child binary under the existing `0.1.20` slot on `agent-sr-test-pve04` and `agent-dusk01`, preserved timestamped backups of the old `0.1.20` binary (`sha256=0a0b5a87e76057156059af9a6373fee9030ab72af5efc495268e063c8f88577c`), and restarted only those agents. Both hosts reported `platform.addon_statuses.version='0.2.0'`, `state='running'`, `active=true` by 2026-07-05 00:02Z; the only degradation reason remained the pre-existing cgroup resource-limit warning. Fleet checks: 2026-07-05 00:02Z rolling 30m anomaly rows=55, sustained drift rows=0; 2026-07-05 00:28Z rolling 30m anomaly rows=40, sustained drift rows=0, spike rows=0. Full package rollout + 48h soak remains open. The clean native-add-on publish/import path is blocked in this session because `publish_addon.sh` requires `SERVICERADAR_AGENT_RELEASE_PRIVATE_KEY` to sign per-arch artifacts, and that key is not present in the local environment or visible cluster secrets; do not replace the fleet via unsigned package rows.
  - [x] 6.2.a Direct canary was expanded to all 12 demo agents by replacing the existing `0.1.20` child binary in place, with timestamped backups and agent restarts. The first fleet expansion proved the Ubuntu/glibc-2.39 binary worked on Ubuntu worker hosts but failed on Debian Bookworm `ns*` agents (`GLIBC_2.38 not found` behind plugin `circuit_open`); those four hosts were immediately rolled back to the old `0.1.20` binary, then redeployed with a Bookworm/glibc-2.36 build.
  - [x] 6.2.b Built the replacement add-on in `rust:1.90-bookworm` on `sr-test-pve04`; artifact `sha256=950175adb75354b0762ed9c4b6677d28e52802bcbfccce784a3887c615f599ac`, ELF x86-64, glibc 2.36 baseline. Smoke-tested `--help` on `ns01`, deployed first to `agent-ns01`, `agent-ns02`, `agent-ns03`, and `agent-ns05`, then normalized `agent-dusk01`, `agent-sr-test-pve04`, and the six `agent-k8s-*` workers to the same hash.
  - [x] 6.2.c Post-normalization checks on 2026-07-05 00:48Z: all 12 demo hosts have the exact `950175adb75354b0762ed9c4b6677d28e52802bcbfccce784a3887c615f599ac` file hash; `platform.addon_statuses` reports every anomaly add-on at `version='0.2.0'`, `state='running'`, `active=true`; rolling 30m counts are 26 anomaly rows and 0 sustained drift rows. A narrower producer-version check found six `0.2.0` rows, all `verdict_source=edge-spike` SNMP interface opens from `agent-dusk01` (four High, two Medium), zero Critical, max score exactly 50. The old message-like `%spike%` query is not a valid spike counter because spike messages read `breach confirmed...`. The only reported degradation remains the pre-existing cgroup resource-limit warning. This is still a direct-host canary, not a signed package rollout, so the 48h soak and package path remain open.
- [ ] 6.3 Post-Phase-2 (demo, 48h): `platform.anomaly_episodes` bounded (open episodes ≈ active conditions); zero class-1008 anomaly rows; avg anomaly row payload ≤2KB; StatefulAlertEvaluationQueue overflow counter = 0. Still pending: demo `serviceradar-core` deployment is on published `v1.4.0`, so the episode registry, payload slimming, alert liveness, and ingest backstops are not live in the namespace yet.
- [ ] 6.4 Post-Phase-3: capacity findings exist only for eligible sources with passing significance gates and emit only on transitions (per-run event count ≈ state changes, not fleet size); interface charts render continuous rates at true magnitudes (tonka01 eth9 ≈ 344 KB/s avg, zero double-conversion gaps); UI checks (decoded series, marker note/clamp, projected-crossing label, no @chart_pad crash) verified via Playwright against local web-ng. Still pending: demo `serviceradar-core`/`serviceradar-web-ng` deployments are on published `v1.4.0`. Branch backstop added 2026-07-05 for the observed ns03 capacity row shape: core skips forecasts whose projected crossing is outside `horizon_ends_at`, and web-ng suppresses already-persisted out-of-horizon projected rows; focused core and web-ng tests passed locally.
- [ ] 6.7 Post-Phase-2 alert liveness (blocking): synthetic episode → rule fire → `platform.alerts` row → recovery on clear, all verified on demo; standing check added to the soak procedure
- [ ] 6.5 Post-Phase-4 (demo, 7d): interface drift active only on baseline-covered series; zero "sustained ... drift" events on clean diurnal series; anomaly share of OCSF ingest <5%
- [ ] 6.6 Standing tripwire verified: simulate a flood (replay tool or stale-addon fixture) → core flood operational event fires; shed rollups appear; no silent loss
- [ ] 6.8 Demo agent host rollout preflight: SSH audit on 2026-07-04 23:07Z found both sampled hosts running agent `1.4.0` with anomaly add-on `0.1.20` current. `dusk01` still has Debian package `serviceradar-agent 1.2.83`; its `/usr/local/bin/serviceradar-agent-updater` lacked `--addon-id`/systemd flags and was not setuid, so systemd add-on delivery failed with `flag provided but not defined: -addon-id`. Temporary demo-host repair at 2026-07-04 23:20Z replaced `/usr/local/bin/serviceradar-agent-updater` on `dusk01` and `sr-test-pve04` with the current branch Linux/amd64 helper (`sha256=888e744993dbe872c2566460e8ae37fe49e8b0482898312532f41c86d846cf70`), preserving timestamped backups and setting `root:serviceradar 4750`; verified the `serviceradar` service user sees the full add-on/systemd/resource flag set. The stale `dusk01` service unit also bounded child processes to only `CAP_NET_RAW`, causing the repaired helper to fail setcap with `unable to set CAP_SETFCAP effective capability`; live demo repair added `/etc/systemd/system/serviceradar-agent.service.d/addon-updater-capabilities.conf` with `CAP_NET_RAW CAP_SETFCAP CAP_DAC_OVERRIDE CAP_FOWNER CAP_CHOWN` and restarted only that agent. Demo CNPG had 12 enabled manual netprobe assignments with legacy Settings-UI string params (`enabled="true"`, `capture_interfaces=""`, boolean toggles as strings); normalized those rows in place to typed JSON (`enabled=true`, `capture_interfaces=[]`, booleans false). Post-repair logs: `sr-test-pve04` successfully installs systemd add-ons and applies netprobe visibility config (`netprobe-v1:d859190110853048`), while `dusk01` installs the netprobe systemd add-on and applies visibility config (`netprobe-v1:2265ff88c5683903`). `sr-test-pve04` then still deferred config ACKs for an unrelated Bumblebee apply failure: `/var/lib/serviceradar/bumblebee/tmp` was `root:serviceradar 0750`, so the non-root agent could not create catalog temp files. Live repair at 2026-07-05 00:26Z changed `/var/lib/serviceradar/bumblebee` ownership recursively to `serviceradar:serviceradar`, matching `dusk01`; after agent restart it staged the Bumblebee catalog and wrote the runtime profile, with no new skipped config ACK/deferral log lines in the post-repair window. Code hardening now uses `ValidatedPrivilegedAgentUpdaterPath` for add-on setcap/systemd operations, captures helper stderr/stdout into the parent error (so `CAP_SETFCAP` bounding-set failures are visible in the agent warning), tolerates the legacy netprobe string/array overlay shape, and fails stale helpers fast with actionable missing-setuid/missing-flag reasons. Packaging hardening was verified on 2026-07-04 with `bazel build //build/packaging/agent:agent_deb`; the generated `serviceradar-agent__amd64.deb` contains `usr/local/bin/serviceradar-agent-updater` as `-rwsr-x---` (`4750`, root/root payload, postinstall still assigns group `serviceradar`). Do not close this gate until a package/release reconciles the helper/service-unit/runtime-dir layer fleet-wide and sampled hosts stay clear of config ACK failures for the new 0.2.0 rollout path.
  - [x] 6.8.a Root-caused the fleet-wide `resource limits not enforced` anomaly add-on degradation. The live demo service unit is stale (`Slice=system.slice`, `Delegate=no`, no packaged `ExecStartPre`), and the deployed agent's child-process limiter prefers the manifest `slice: serviceradar-addons.slice` over any explicit `addon_cgroup_root`, so it tries to create `/sys/fs/cgroup/serviceradar.slice/serviceradar-addons.slice` as the non-root `serviceradar` user. A one-host runtime repair attempt on `dusk01` showed manual `mkdir/chown` of the parent cgroup only moves the failure to `memory.max`/`cpu.max`/`pids.max` writes; the kernel control files remain unwritable without a delegated service cgroup. The empty manual cgroup was removed and the agent stayed active.
  - [x] 6.8.b Code/package fix: process-supervised add-ons now prefer an explicit delegated `addon_cgroup_root` over the manifest slice; the packaged agent config sets `addon_cgroup_root=/sys/fs/cgroup/serviceradar.slice/serviceradar-agent.service/addons`; the packaged systemd unit sets `Slice=serviceradar.slice` and `Delegate=yes`. Focused verification passed with `go test ./go/pkg/agent/addon ./go/pkg/agent`; `bazel build //build/packaging/agent:agent_deb` passed; package inspection confirmed `addon_cgroup_root`, `Delegate=yes`, the explicit `/run/serviceradar` `ExecStartPre`, widened `CapabilityBoundingSet`, and setuid `serviceradar-agent-updater` are present. Live demo still needs an agent package rollout before this degradation clears fleet-wide.
