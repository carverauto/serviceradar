## 1. Baseline and schema

- [ ] 1.1 Merge the truthful AWX inventory-sync and verb-bridge archive changes and reconcile the remaining `add-ansible-integration` proposal so this child has one canonical baseline.
- [ ] 1.2 Add AWX membership resources keyed by `(controller_id, inventory_id, awx_host_id)` with generation, current name/address, canonical-device link/evidence/disposition, and multiple memberships per device.
- [ ] 1.3 Add parent operation, child execution, immutable launch snapshot, target tuple, `AutomationExecutionDelegation` with issuer/owner/principal/ceilings, approval/actor snapshot, reserved dispatch marker/state, `dispatch_partial`, and controller-scoped job identity resources/constraints; add administrator-only `ansible.delegations.manage` for binding a separate owned service principal.
- [ ] 1.4 Add versioned authenticated mutation-phase resources and canonical-device-wide holds recording triggering membership/transaction/generation/evidence; add exact `ansible.targets.holds.clear` to RBAC/profile UI/API, administrator-only by default, with approval/current-policy checks and secret-free reconciliation audit.
- [ ] 1.5 Backfill current memberships and legacy runs conservatively; purge/redact active run/schedule raw variables and PaperTrail/action inputs without secret hashes, require scrub-before-traffic on restore, document bounded backup expiry/tombstones and affected-credential rotation, quarantine ambiguity, and never synthesize missing host IDs, actor authority, or provenance.
- [x] 1.6 Split each AWX controller into sync/execution/callback credential references, backfill existing rows from the legacy all-purpose reference, retain a one-release sync-only rolling-upgrade bridge, and map every AWX verb to exactly one purpose with no execution/callback fallback.
- [x] 1.7 Bind every AWX bearer grant to non-empty verb-specific method/host/normalized-port/exact-or-argument-derived-path ACLs, reject malformed or unsupported scope before issuance/dispatch, and refuse credential-backed redirects.
- [x] 1.8 Move scheduled AWX inventory-sync bearers into dedicated protobuf `host_params_json` field 23 with sentinel-only public params and mixed-version fail-closed behavior, strictly reject unknown host fields/trailing JSON/empty controller identities, bind injection to exact origin/TLS plus empty-body inventory-subtree GETs, rotate fingerprints on host-only material, and deny ambiguity, public-envelope smuggling, plaintext fallback, traversal, and redirects.
- [x] 1.9 Add v2 request-body policies for every mutating on-demand AWX grant, retain exact launch bytes outside Wasm, enforce one-use substitution/empty/trusted-rewrite modes before resolver/transport, reject opaque secret/redaction bypasses, and preserve safe v1 reads.
- [x] 1.10 Add host-owned private-CA trust for credential-bearing plugin HTTP, augment system roots from bounded absolute PEM files outside Wasm memory, and fail all plugin HTTP closed when configured trust cannot be loaded.

## 2. Authorization, planning, and inputs

- [ ] 2.1 Implement a single planner used by interactive, API, northbound, schedule, check, verification, commit, retirement, removal, and cancel paths; preserve the initiating human or exact expiring/revocable fixed-ceiling `AutomationExecutionDelegation`, prevent higher-privilege principal selection, and intersect issuer-delegable scope/principal ceiling/current authority/approval/deployment maximum.
- [ ] 2.2 Reauthorize launch permission, tenant, target policy, action, approval, template/revision/credential binding, and holds before plan creation and again at dispatch; make later expansion non-enlarging and contraction cancel/deny.
- [ ] 2.3 Partition exact child jobs by controller/inventory/template/immutable project commit/execution environment/approved machine credential/reviewed static or dynamic custom-credential slot/check mode and re-fetch all AWX template/host state before persistence; reject moving refs and `update_on_launch`.
- [ ] 2.4 Require explicit compatible `inventory_id`, literal-only host-token grammar, parse-round-tripped non-empty limit, exact tuple/name uniqueness, requested/planned/persisted/command equality, exact accepted inventory/limit, and post-start job-host-ID reconciliation; remove hostname/IP/fact fallback and every empty-limit path.
- [ ] 2.5 Create parent/children/all target rows atomically before dispatch and fail the entire plan on validation, uniqueness, or persistence error.
- [ ] 2.6 Replace raw `extra_vars`/schedule variables with reviewed typed non-secret schemas, canonical public/internal values/digests, and prebound AWX credential IDs; reject ServiceRadar-collected secrets, secret-value hashes, sensitive surveys, and undeclared inputs on every launch surface.
- [ ] 2.7 Require native AWX check mode or a separately bound check template and reject variable-simulated dry run.

## 3. Dispatch, callback seam, and lifecycle

- [x] 3.1 Bind every command to parent/child, controller, inventory, template/revision, target digest, actor/approval snapshot, and reserved typed non-secret AWX `extra_vars` `serviceradar_dispatch_id`/`serviceradar_snapshot_digest`; deliver them through the exact restricted AWX survey contract with `ask_variables_on_launch=false`, and require template acceptance plus exact job echo with no user override.
- [ ] 3.2 Verify accepted template/inventory/limit/project commit/execution environment/credential IDs+types/check mode/dispatch marker before controller-local job binding; implement bounded recent-job exact-marker reconciliation and non-retrying `dispatch_ambiguous` cancellation.
- [ ] 3.3 Add callback-owned `prepare(snapshot, actor)`, `materialize_attach(ref, child)`, `bind_activate(ref, controller, job, full_snapshot)`, and `revoke_cleanup(ref, outcome)` calls; keep callbacks pending until job-host equality marks `scope_verified`, pin type/slot in authority, append only the instance ID after local plan, and clean up on scope/create/launch/ambiguity/partial/cancel/consumption/all terminal paths.
- [ ] 3.4 Detect RBAC/membership/service-principal/delegation/approval/target-policy/binding contraction through change invalidation plus bounded pulse/watchdog reauthorization; revoke callbacks, cancel all children, persist `cancel_failed`, hold unproven mutations, and prevent revoked/canceled reactivation.
- [ ] 3.5 Derive parent status from every child/target; on partial dispatch stop undispatched children, cancel accepted jobs, revoke prepared callback refs, and persist `dispatch_partial` plus any `cancel_failed` rather than continuing as ordinary partial success.
- [ ] 3.6 Derive `automation.mutation_phase.v1` only from exact authenticated bound job/action/template/revision lifecycle, implement the specified transition/idempotency/conflict rules, reject stdout/facts/set-stats as advancement evidence, and hold the canonical device across memberships on critical/unknown/uncommitted outcomes.

## 4. Event identity and audit

- [ ] 4.1 Scope run lookup, polling, watchdog, cancel, links, and OCSF events by `(controller_id, awx_job_id)` plus child execution.
- [ ] 4.2 Correlate host events by AWX host ID within the child target snapshot; enrich name-only AWX events through job-scoped host data or fail visibly without fallback.
- [ ] 4.3 Persist initiator/delegation owner and ceiling, transport worker, exact template/project commit/inventory/limit/check mode, credential references/types/instances, declared non-secret inputs/classifications/plan digests, target tuples, approvals, commands, jobs, and mutation phases in secret-free audit.
- [ ] 4.4 Update UI confirmation/run detail/device panels to display controller, inventory, duplicate-name context, exact targets, actor, revision, check mode, holds, drift, dispatch ambiguity, and cancellation failure.

## 5. Verification

- [ ] 5.1 Add unit/property tests for duplicate farm01/tonka01 hostnames, multiple memberships, safe literal limit grammar/round-trip, stale links, empty/inexact limits, fixed inventory mismatch, target-row rollback, job-host reconciliation mismatch, and check-mode enforcement.
- [ ] 5.2 Add authorization tests for human attribution, higher-privilege delegation rejection, issuer/principal/owner issuance-current intersection/expiry/revocation changes, invalidation/pulse contraction, SystemActor non-escalation, callback scope-pending lifecycle/cleanup, approval expiry, and device-wide hold clearance.
- [ ] 5.3 Add secret-retention tests covering DB, Oban args, command context, logs/events, PaperTrail, PubSub, OCSF, support output, retries, cancel, backup, and UI.
- [ ] 5.4 Add AWX integration tests for accepted-job full-field drift, moving SCM/update-on-launch rejection, reserved-extra-var ignore/override, controller-local duplicate job IDs, marker-based ambiguity, copied/relaunched jobs, host-ID scope verification, callback pending-before-scope, partial compensation, cancel failure, exact mutation-phase graph/replays/forgery/timeout, and hold bypass.
- [ ] 5.5 Run scoped Elixir quality/Bazel tests, `openspec validate harden-ansible-awx-targeting --strict`, and `openspec validate --all --strict`; keep live AWX and host mutation disabled until reviewed canary approval.
- [x] 5.6 Add source/ingress AWX result projections with a shared 3 MiB aggregate budget, tracked-command-only gateway allowance, versioned ten-by-ten event windows, and a secret-free bounded v0.1.5 rolling compatibility test matrix.
- [x] 5.7 Add cross-language request-body-policy tests for exact-byte launch substitution, malicious scope mutation, missing/digest-mismatched bytes, v1 mutation denial, safe/empty-body enforcement, replay prevention, buffer clearing, strict policy decoding, redaction bypasses, migration default, and DB roundtrip.
