## 1. Reconcile active specifications and capture the baseline

- [x] 1.1 Read this change's proposal, design, and delta specs together with `openspec/changes/refactor-netprobe-onto-generic-addon-contract`, `openspec/changes/add-host-network-visibility-sidecar`, `openspec/changes/add-netprobe-fleet-attribution`, `openspec/changes/refactor-agent-nats-credential-provisioning`, `openspec/changes/scale-netflow-ingest-isolation`, `openspec/changes/restore-anomaly-alerting-and-surfacing`, the supporting `add-workload-identity-enrichment` discovery note, and the current `ingestion-routing` specification before changing production code.
- [x] 1.2 Reconcile `refactor-netprobe-onto-generic-addon-contract/proposal.md` and `design.md` with its audited task 5.6: remove the stale claim that flow attribution will move to the generic OTLP relay, preserve the current agent-owned positive-ack prefix and direct current-state persistence terminus, and link the remaining work to GitHub #4029, #4030, and #4031.
- [x] 1.3 Reconcile overlapping unchecked delivery, observability, correlation, and scale work in `add-host-network-visibility-sidecar`, `add-netprobe-fleet-attribution`, `refactor-agent-nats-credential-provisioning`, `scale-netflow-ingest-isolation`, and `restore-anomaly-alerting-and-surfacing`; correct the supporting workload-identity discovery note and identify this change as the owner rather than leaving active text that can independently alter the same contract.
- [x] 1.4 Preserve the intentional UDP-client coalescing contract and every deployed correlator candidate family, including wildcard listeners, non-zero exporter ephemeral-port mismatch, node-SNAT, and public-endpoint mapping. Record explicitly that this change does not replace `flow_process_attribution_current` with the generic relay or narrow matching merely to make a strict five-tuple join pass. Also record the current public-endpoint rank-zero collision and make public candidates lower than exact/local fallbacks while retaining relative exposure-class ordering.
- [ ] 1.5 Capture a timestamped pre-change baseline for a controlled agent: reported netprobe add-on version, netprobe process start time, per-protocol attribution producer counters, current attribution rows by protocol, sampled NetFlow rows sharing the host endpoint, correlation candidates, and final `ocsf_payload.event_type = 'attributed_flow'` rows. Give every query an explicit zero/failure branch.
- [x] 1.6 Run `openspec validate --strict` for `harden-flow-attribution-pipeline` and every reconciled active change (`add-host-network-visibility-sidecar`, `add-netprobe-fleet-attribution`, `refactor-netprobe-onto-generic-addon-contract`, `refactor-agent-nats-credential-provisioning`, `scale-netflow-ingest-isolation`, `restore-anomaly-alerting-and-surfacing`, and `add-workload-identity-enrichment`) after reconciliation; resolve every conflict or malformed delta before implementation starts.

## 2. Localize and fix the TCP producer failure (#4029)

- [x] 2.1 Add a Linux-only Bazel integration test target under `rust/netprobe` that loads the real built eBPF object, opens a completed kernel TCP handshake, drains the real eBPF ring, and requires an owner-resolved `FlowAttributionEvent` with protocol TCP, non-zero local and remote ports, PID, and command identity. Do not satisfy this gate with a constructed `FlowAttributionRecord` or a mocked ring reader, and do not add a shell script.
- [x] 2.2 Make the integration target declare the eBPF object and all fixtures as Bazel inputs, state its required Linux/CAP_BPF/CAP_PERFMON/CAP_NET_ADMIN environment, isolate its pinned maps in a unique bpffs directory with failure-safe cleanup, and fail clearly when run in the designated privileged environment without those prerequisites rather than silently passing or skipping the assertion.
- [ ] 2.3 Run the new test against the unfixed producer and capture which boundary fails: `attach_attribution_probes/1`, the `tcp_connect` or `inet_sock_set_state` eBPF hook, tuple construction/ring submission, `drain_ring`, owner-map resolution, or final event construction.
- [x] 2.4 Extend producer-stage evidence so program readiness, `serviceradar_netprobe_attribution_records_total{event_kind="inet_sock_set_state",protocol="tcp",outcome=...}`, owner-cache hit/miss, userspace insert/update, and handoff to the agent distinguish no kernel record, userspace miss, cached/update suppression, and successful new emission; retain bounded labels and do not put PID, tuple, or agent identifiers in metric labels.
- [ ] 2.5 Add focused tests around `rust/netprobe/src/ebpf_runtime.rs::attach_attribution_probes`, `rust/netprobe/ebpf/src/lib.rs::{tcp_connect,inet_sock_set_state}`, and `rust/netprobe/src/attribution.rs::{drain_ring,flow_key_from_record}` that pin the localized failure before changing its implementation.
- [ ] 2.6 Implement the smallest fix at the boundary proven broken by 2.3, then prove both an outbound client connection and an accepted server connection produce owner-resolved TCP events without regressing close handling, PID reuse protection, or process-generation cache keys. If the loaded program cannot safely read the running kernel layout, surface protocol/build attribution as unavailable with the localized failure reason instead of leaving a healthy permanent-zero stream.
- [x] 2.7 Retain regression coverage for intentional UDP ephemeral-client coalescing in `attribution_flow_key_from_record`/`should_coalesce_udp_client_attribution`, including `local_port = 0`, and for both zero-port coalescing and non-zero exporter ephemeral-port mismatch in `ServiceRadar.FlowAttribution.Correlation`; do not trade the TCP fix for renewed high-cardinality UDP churn or silently narrow the deployed fallback ladder.
- [ ] 2.8 Verify the producer metrics on the Linux integration host: a completed TCP handshake must advance a TCP hook/outcome counter and emit an event, while a deliberately unresolvable owner must advance the miss outcome without emitting a falsely attributed event.

## 3. Version and package the corrected netprobe add-on (#4029)

- [x] 3.1 Advance `addons/netprobe/addon.yaml` and `rust/netprobe/BUILD.bazel` `NETPROBE_VERSION` to the same new add-on version in the producer-fix commit. Do not change `rust/netprobe/Cargo.toml`, `Cargo.lock`, or the crate mirror merely to mirror the add-on version.
- [x] 3.2 Run `bazel test //rust/netprobe:netprobe_test //rust/netprobe:static_linkage_test` and build `//build/native_addons:netprobe_addon_bundle.linux.amd64.tar.gz`, proving through a declared Bazel test/runfiles target that the shipped bundle contains the newly built netprobe binary and eBPF object. Do not inspect or copy files from `bazel-out` to establish that proof.
- [ ] 3.3 Run `bazel test //build/native_addons:build_gates_test` and `scripts/check-native-addon-version-bumps.sh origin/staging <producer-fix-commit-sha>` with the actual producer-fix commit SHA; resolve manifest, inventory, portability, size, and version-constant failures rather than bypassing a gate.
- [ ] 3.4 Import the built add-on through the normal catalog path and verify the catalog advertises the new version before assigning it. The #4029 rollout began on stale 0.2.31 because of #4039, so a built artifact or successful importer job is not evidence that the host is running it.
- [ ] 3.5 After assignment, query the agent's reported add-on state and the running process/binary version until both identify the new version; stop the rollout if either still reports the prior version.

## 4. Add correlator and topology diagnostics (#4029)

- [ ] 4.1 Add a bounded diagnostic query/helper alongside `ServiceRadar.FlowAttribution.Correlation` that uses the same partition, recent-flow limit, attribution window, and skew as the production correlation pass; it must not introduce an unbounded scan of either `ocsf_network_activity` or `flow_process_attribution_current`.
- [ ] 4.2 Report, per partition and protocol, recent unattributed flows considered, current attribution rows considered, host endpoints present on both sides, exact/reverse, wildcard-listener, relaxed UDP, node-SNAT, and public-endpoint candidates, and rows actually stamped. Keep raw IPs and tuples out of metric labels.
- [ ] 4.3 Emit an explicit diagnostic outcome that distinguishes at least: no producer rows, no recent sampled flows, no common host endpoint/topology coverage, common endpoint but no tuple candidate, candidate present but no stamped row, and successful attribution.
- [ ] 4.4 Instrument `ServiceRadar.FlowAttribution.Correlator.run_once/0` with bounded pass duration, candidate, stamped, failure, and backlog telemetry while retaining the existing advisory lock, 5,000-flow pass bound, and schedule-after-completion behavior.
- [ ] 4.5 Add database-backed correlation fixtures for exact TCP forward/reverse matches, wildcard TCP/UDP listeners, intentional UDP `local_port = 0` coalescing, non-zero UDP exporter ephemeral-port mismatch, ICMP address-only matching, node-SNAT, public-endpoint mapping, fallback precedence behind an exact candidate, no-common-endpoint topology, tuple mismatch, and a successful final OCSF update; assert the diagnostic classification and persisted artifact in each case. Include a case where a Gateway public endpoint (current raw exposure rank zero) and an exact local candidate are both eligible; normalize public ranks below every local strategy and prove exact wins while Gateway still outranks LoadBalancer/ExternalIP among public candidates.
- [x] 4.6 Document a read-only operator diagnostic sequence that starts with producer counters, then verifies a fresh current attribution row, then common sampled endpoints/tuples, and finally the attributed OCSF row. Treat "correlator ran" or "stamped count logged" as supporting evidence, never as the final success gate.

## 5. Isolate bounded core admission lanes (#4031)

- [x] 5.1 Write live-GenServer regression tests that hold flow persistence open and prove workload-identity, `addon:*`, `plugin:*`, agent capability, and endpoint-inventory admission are handled without waiting for the flow commit; add the inverse test proving slow retained-plugin work does not delay flow admission.
- [x] 5.2 Create separate flow-attribution and retained-plugin admission lanes under core supervision. Each lane must own independent queue state, limits, and timers so saturation of one lane cannot consume or block the other's allowance; a shared queue implementation is acceptable only if the two runtime instances and accounting are isolated. Fix concurrency at one flow worker and two retained-plugin workers for this change. Configure the flow defaults as 16 total admitted items, 64 MiB total retained payload, four admitted items per agent, two-second maximum queue wait, and 20-second worker timeout; configure retained plugin defaults as 32 total admitted items, 64 MiB total retained payload, eight admitted items per agent, two-second maximum queue wait, and 20-second worker timeout. Count and byte totals include queued and in-flight work. Reject deadline configurations unless `queue_wait + worker_timeout + 3s <= gateway_core_call_timeout`; also reject queue wait above two seconds, worker timeout above 20 seconds, a flow gateway call above 25 seconds while the agent RPC is 30 seconds, or a retained-plugin call above its 30-second wave slot. A concurrency change requires a separately reviewed gateway-forwarding and sender-deadline contract.
- [x] 5.3 Make each lane's total-item, total-byte, per-agent-item, queue-wait, and worker-timeout bounds runtime-configurable while rejecting zero/negative configuration; concurrency remains fixed by task 5.2. Account the retained encoded payload plus fixed envelope overhead across queued and in-flight work, and reject admission when the lane's total count, total bytes, or per-agent admitted count would be exceeded. Return distinct, testable reasons for count-full, configured-byte-full, per-agent-full, wire-payload-too-large, admission-timeout, execution-timeout, and worker-crash outcomes. A valid payload within the wire/source size contract but above configured lane capacity is retryable `received: false`; only a wire/source size violation is a terminal hard RPC error.
- [x] 5.4 Route synchronous flow-attribution calls out of `ServiceRadar.StatusHandler.handle_call/3` by enqueuing the original normalized status and `GenServer.from()`, returning `{:noreply, state}` immediately, and decoding/persisting inside the flow lane.
- [x] 5.5 Reply to the original flow caller only after `ServiceRadar.FlowAttribution.persist/3` returns a committed success; propagate decode, persistence, deadlock-exhaustion, timeout, and worker-crash failures without emitting successful batch telemetry or a positive acknowledgement.
- [x] 5.6 Route capability-retained `plugin-result` calls through the independent plugin lane and execute the existing `PluginResultIngestor` validation, handler fan-out, transaction, notification, and publication contract outside both the `StatusHandler` and `ResultsRouter` singleton callbacks. Keep legacy non-retained plugin-result behavior unchanged. Put retained-plugin lane routing behind a compatibility gate that defaults to the legacy path until a #4030 gateway with bounded two-way forwarding is deployed; the flow lane does not require that gate.
- [x] 5.7 Define the retained-plugin durable terminus as both a committed raw result and a durably recorded terminal handler outcome. A terminal handler success or a persisted handler-domain failure may complete the boundary; a committed raw row alone does not. If terminal-outcome persistence fails after the raw row commits, reply uncommitted and prove retry converges idempotently without a duplicate raw logical result. Preserve typed outcome details without treating queue admission as ownership transfer.
- [x] 5.8 Ensure no alternate agent-retained path can run database work inside `StatusHandler.handle_cast/2`: either admit supported internal casts through the bounded lane without a reply or reject an invalid agent-retained cast with explicit telemetry, and pin that behavior with a test.
- [x] 5.9 Define lane deadlines from admission time, including queue wait, and use the specified two-second wait plus 20-second worker defaults under the existing 25-second flow-attribution and 30-second retained-plugin gateway calls. Enforce the task 5.2 deadline invariant on runtime overrides. Require exactly one non-empty chunk for flow attribution so its terminal reply fits the agent's 30-second window. For retained plugin streams, start no more than the lane's two-worker limit at once and start later pairs only when a slot is available. Change the Go agent stream deadline to `max(30s, ceil(chunk_count / 2) * 30s + 15s)`, giving ten chunks 165 seconds, eight seconds of per-wave headroom, and 15 seconds of final RPC grace. Terminate timed-out workers where possible and suppress late replies/results.
- [x] 5.10 Add per-lane telemetry for pending count, pending bytes, in-flight count/bytes, admission wait, execution duration, payload/event count, result, rejection reason, timeout, and crash. Export bounded-cardinality metrics and prove depth/byte gauges return to zero after success, rejection, timeout, and worker failure.
- [x] 5.11 Keep one accepted flow window as one existing atomic persistence call initially; do not split the up-to-4,096-row UPSERT in this change without measurements, because partial commits would alter acknowledgement and retry semantics.
- [ ] 5.12 Measure event count, encoded bytes, queue wait, query duration, Postgrex checkout time, deadlock retries, and total acknowledgement latency at representative table size and churn. If a measured percentile cannot fit the time budget, record the evidence and open a separately reviewed chunk/partial-commit design instead of silently adding chunking here.
- [ ] 5.13 Test independent count/byte/per-agent saturation, a valid single payload above configured lane capacity, a wire/source-contract oversized payload, deterministic admission ordering, task crashes, deadline expiry, caller exit, late completion, and coordinator restart. Verify every accepted live caller gets exactly one terminal reply, no lane exceeds its configured retained-byte budget, configured-capacity rejection is retryable, and wire/source oversize is terminal.

## 6. Return truthful unary and streaming acknowledgements (#4030)

- [x] 6.1 Introduce an internal per-service delivery outcome separate from directives and processed-count bookkeeping, so agent-retained committed work, agent-retained uncommitted downstream work, invalid protocol input, and accepted best-effort buffering cannot collapse into the same `:ok` result. Define the new negative-ack class as exactly `flow-attribution` plus capability-retained `plugin-result`; do not change the existing `otlp-relay` hard-error/durable-spool contract.
- [x] 6.2 Thread that outcome through unary `push_status/2` and `process_push_service/3`; return `%GatewayStatusResponse{received: false, directives: []}` when a valid agent-retained service was not committed, including configured lane-capacity rejection. Continue to raise `INVALID_ARGUMENT` for malformed identity/framing/fields, undecodable source content, or isolation violations, and reserve `RESOURCE_EXHAUSTED` with bounded reason `payload_too_large` for the 16 MiB chunk, 64 MiB stream, or source-contract size limits.
- [x] 6.3 Thread the same outcome through `initial_stream_status_state/0`, `handle_status_chunk/5`, agent-retained stream accumulation, bounded forwarding, and the final `stream_status/2` response. A downstream agent-retained failure must produce an RPC-level negative acknowledgement with no directives, without converting the failure into `GRPC.RPCError :unavailable` or disconnecting the status stream.
- [x] 6.4 Enforce agent-retained source isolation before processing: a unary request containing flow attribution or a capability-retained plugin result must contain exactly one service; every non-empty agent-retained stream chunk must contain exactly one service, every chunk must retain the same source, and the stream must contain no best-effort peer. Require a flow-attribution stream to contain exactly one non-empty chunk; retain the current maximum of ten chunks for retained-plugin streams. Accumulate agent-retained streams only within the existing 16 MiB chunk/64 MiB window bounds, validate the complete stream, and reject mixed, source-changing, multi-service, or multi-chunk flow containers as `:invalid_argument` before any service from the invalid stream is forwarded.
- [x] 6.5 Preserve multi-chunk retained-plugin streams by allowing one isolated retained-plugin service per ordered chunk, pinning the delivery capability for the full stream, processing the validated set with at most two concurrent forwards, starting later work only as slots become available, and making the final response false with no directives if any item was not committed. When all items commit, aggregate directives deterministically in original chunk order. Cover partial-prefix commit and retry idempotence explicitly.
- [x] 6.6 Remove flow attribution and capability-retained plugin results from `StatusProcessor.should_buffer?/1`; keep synchronous core forwarding and its timeouts, and keep legacy best-effort plugin results eligible for the bounded gateway buffer.
- [x] 6.7 Add unary and streaming gateway tests for committed `received: true`, valid-but-uncommitted agent-retained `received: false` with empty directives, no gRPC disconnect on false, invalid payload hard errors, mixed agent-retained/best-effort rejection, source changes, multiple agent-retained services per container, a retained-plugin multi-chunk partial failure, best-effort buffer acceptance retaining its existing behavior, and unchanged OTLP-relay hard errors. Add Go coverage that both local stream-size sentinels terminally drop the complete invalid set without disconnecting or replaying it.
- [x] 6.8 Extend `go/pkg/agent/push_loop_flow_attribution_test.go` and `push_loop_plugin_results_test.go` so a negative response retains the exact pending prefix, a later positive response clears it once, committed prefixes are not double-counted, and a negative application acknowledgement does not mark the gateway connection disconnected.
- [x] 6.9 Treat only `INVALID_ARGUMENT`, `RESOURCE_EXHAUSTED/payload_too_large`, and the Go client's equivalent pre-RPC `ErrStreamStatusChunkTooLarge` or `ErrStreamStatusBudgetExceeded` sentinels as terminal for identical agent-retained bytes. Remove the complete offending flow-attribution window or retained-plugin RPC set from automatic pending replay as an explicit poison drop; do not retain a second payload copy or count the drop as receipt. Record bounded item/byte totals and reason telemetry, and prove the same invalid set is not replayed forever while later valid status work progresses. A local sentinel must not disconnect a healthy connection; a terminal gRPC response may reconnect before later work. Preserve the complete set for every transport error, downstream error, configured lane-capacity NACK, and other retryable outcome.

## 7. Correct the best-effort StatusBuffer (#4030)

- [x] 7.1 Change overflow handling to retain the actual evicted status returned by `enqueue_status/3` and emit drop telemetry from that status, not from the incoming replacement; include bounded source/service-type and reason metadata sufficient to identify which class of work was lost. Export current retained bytes plus dropped-item and dropped-byte totals by bounded reason alongside entry depth.
- [x] 7.2 Document that graceful or abrupt gateway/process loss can discard best-effort buffered entries, correlate the last observed buffer depth with existing process/node restart signals so operators can identify a possible-loss interval, and do not claim an exact per-entry shutdown-loss count that the failed process could not establish.
- [x] 7.3 Prove with distinct old/new fixtures that overflow reports the evicted entry and its encoded bytes, entry depth remains capped, retained-byte depth matches the surviving queue, dropped item/byte totals use the actual evicted entry and reason, monitoring retains the entry/byte-depth and restart evidence required by the documented loss model, and agent-retained flow/plugin statuses can no longer enter the buffer while eligible best-effort statuses still flush normally.

## 8. Focused, Bazel, and database-backed verification

- [x] 8.1 Before any Bazel command in an isolated checkout, symlink the primary checkout's gitignored `.bazelrc.remote` into the worktree (and `.bazelrc.local` only if it exists) and verify `test -f .bazelrc.remote`; never commit either file.
- [ ] 8.2 Run formatting and focused language tests for every touched component, including the new real-kernel TCP producer test, the existing UDP coalescing tests, core queue/correlation tests, gateway acknowledgement/buffer tests, and Go pending-prefix tests; read the emitted assertions and telemetry rather than treating process exit alone as evidence.
- [ ] 8.3 Run `bazel test //rust/netprobe:netprobe_test //rust/netprobe:static_linkage_test`, the new privileged Linux TCP-attribution integration target, `bazel test //go/pkg/agent:agent_test`, `bazel test //elixir/serviceradar_core:unit_tests`, and `bazel test //elixir/serviceradar_agent_gateway:unit_tests`.
- [ ] 8.4 Run the guarded scratch-CNPG integration lifecycle for `//elixir/serviceradar_core:integration_tests` and verify the actual post-test database rows for exact TCP, wildcard listener, both relaxed UDP forms, node-SNAT, public endpoint, topology-miss, and attributed-flow fixtures; never point destructive setup or teardown at demo or production.
- [ ] 8.5 Run fault-injection coverage for slow queries, deadlocks through the final retry, pool checkout delay, lane count/byte/per-agent exhaustion, protocol oversize caught locally and remotely, worker death, gateway-to-core timeout, core unavailability, graceful gateway shutdown, and abrupt gateway restart. Assert the terminal outcome and whether the agent retains or poison-drops the exact pending set for each fault.
- [ ] 8.6 Run `bazel test //build/native_addons:build_gates_test`, `make lint`, and the repository-wide `make test`; do not substitute `go test`, `cargo test`, or `mix test` for the Bazel workspace gate.
- [x] 8.7 Re-run `openspec validate harden-flow-attribution-pipeline --strict` and update checklist state only for items whose commands and artifacts were actually observed.

## 9. Canary and live-rollout verification

- [ ] 9.1 Roll out the #4031 core admission implementation first with the flow lane enabled and retained-plugin routing still behind its compatibility gate. Roll the updated Go agent with the 165-second ten-chunk deadline, terminal gRPC poison-drop handling, and equivalent local oversize-sentinel handling to the controlled cohort before enabling the #4030 gateway behavior. Roll out that gateway's negative-ack and bounded two-way retained-plugin forwarding next, enable retained-plugin lane routing only after it is ready, then roll out the versioned netprobe add-on to one controlled host. Record immutable core/gateway/agent image SHAs, pod and agent readiness timestamps, gate state, add-on catalog version, assignment version, and running netprobe version before generating traffic.
- [ ] 9.2 After rollout completion, establish a positive delivery control by observing a fresh non-TCP attribution row from the canary, then open a known completed TCP handshake and require the TCP producer counter and a `proto = 6` row newer than the stimulus. Fail the producer gate if either artifact is absent.
- [ ] 9.3 Verify sampled NetFlow/IPFIX contains the canary host and the matching direction-normalized tuple inside the correlation window. If there is no common endpoint, record the explicit topology diagnostic and stop: zero attributed flows is then not evidence against the producer or correlator.
- [ ] 9.4 Once producer and topology preconditions pass, wait for a correlation pass that started after the rollout and stimulus, then query for the specific OCSF row stamped `event_type = 'attributed_flow'`; compare candidate/stamped telemetry with the row and fail if they disagree.
- [ ] 9.5 Exercise a controlled slow-persistence window while sending unrelated workload, add-on/plugin telemetry, agent capability, endpoint-inventory, flow, and retained-plugin statuses. Verify unrelated latency stays within its gate, both lane count/byte gauges remain bounded, and flow/plugin replies arrive only after their commits.
- [ ] 9.6 Inject one valid agent-retained delivery failure through unary status and one through `StreamStatus`; verify both return `received: false` with empty directives, keep the gRPC connection usable, leave the exact agent prefix pending, avoid `StatusBuffer`, and later commit and acknowledge the same prefix once after recovery.
- [ ] 9.7 Hold the canary through multiple correlation and agent-push intervals while recording lane wait/execution percentiles, Postgrex checkout/query latency, deadlock retries, WAL/autovacuum indicators, producer drops, topology outcomes, and attributed-row freshness. Do not introduce persistence chunking unless these measurements breach the recorded budget and the follow-up design is approved.
- [ ] 9.8 Re-query all acceptance artifacts after the observation window and after each relevant job has run: running versions, fresh TCP attribution row, common sampled tuple, final attributed OCSF row, zero unexpected agent-retained entries in `StatusBuffer`, truthful acknowledgement counters, and bounded/returned-to-zero lane gauges. Expand rollout only when every check has an explicit passing result.
