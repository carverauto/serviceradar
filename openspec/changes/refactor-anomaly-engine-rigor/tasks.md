# Tasks — refactor-anomaly-engine-rigor

> Approval gate: do not start implementation until this proposal is approved.
> Coordination: land **after** `fix-anomaly-engine-semantics-and-delivery`'s F14 key
> alignment and F15 `profile_hour_of_week` feed deploy; **withdraw**
> `add-anomaly-finding-disposition` #4280 as superseded; build on
> `retire-uasb-causal-disposition`'s UASB naming pass; sequence the BREAKING wire-envelope
> rename (1f) with the BMP (`add-bmp-dual-path-observability`) and topology-overlay
> (`topology-causal-overlays`) producers.

## 0. Proof harness (cross-cutting; gates every later phase)

- [x] 0.1 Harness exists at `tools/anomaly-proof/{gen.py,plot.py}` driving the **real** `target/debug/anomaly-backtest` (built from `rust/anomaly-core/src/bin/anomaly-backtest.rs`); initial scorecard captured (spike/step/burst recall 1/1 @ ~4-sample latency, precision 1.0; recurring nightly 21/21; raw SNMP counter 6956 false alarms vs rate-normalized precision 1.0; drift+leak 0/1; single-blip 0/1).
- [x] 0.2 Generate synthetic **labeled** datasets: bounded percent gauges (cpu/mem/disk) + monotonic SNMP counters, with injected spike / step / drift / off-cycle anomalies and a counter wrap/reset (`gen.py`).
- [x] 0.3 `anomaly-backtest` now exposes the previously-hardcoded `ReasonContext` fields: the saturation gate + dispersion floors (`--saturation-gate-min`/`--min-std-floor`/`--min-cv`), CUSUM (`--cusum`), and the **seasonal signal** (`--seasonal`/`--seasonal-n-sigma`/`--seasonal-min-samples`) fed the REAL hour-of-week (dow×24+hod) baseline — verified to execute on real code (150 verdicts reference the seasonal signal; recurring patterns sit in the baseline so they are not flagged; it catches the drift/leak the rolling z-score misses). Every requirement 0.3 names (stability-gate, MAD [resolved by withhold-from-baseline], CUSUM, seasonal) is now harness-provable. (`trend_enabled` stays off — not a named requirement; the linear-trend path is the capacity tier.)
- [x] 0.4 **Core half validated against the real DB**: ran the seasonal + capacity WORKER tests (43 tests, 0 failures) against an `srql-fixtures` CNPG scratch DB (migrated current branch) through the real `SRQLRunner` + the rebuilt `causal_disposition` NIF — not just the Rust kernel. This DB run CAUGHT A REGRESSION invisible to the non-DB tests: the `confirm_slots` default 1→2 (task 1.16) broke 5 single-breach-surfacing worker tests; fixed by pinning the test suite's default to `confirm_slots: 1` (the production default stays 2; the hysteresis itself is covered by the dedicated `confirm_slots: 1/3` tests). Capacity parity (1e-9) + seasonal source + series_key + disposition also green through the cdylib.
- [x] 0.5 Emit precision / recall / detection-latency scorecards + Twitter-AnomalyDetection Fig-2-style labeled-overlay plots (`plot.py` edge; `plot_seasonal.py` core; capacity scorecard via `disposition-backtest --kind capacity`).
- [~] 0.6 Scenarios wired for every IMPLEMENTED behavioral requirement, harness/test-proven on real code: edge spike/step/burst/blip + disk-saturation-gate + drift + leak + counter-reset + recurring-seasonal (`gen.py` + `anomaly-backtest`), core seasonal suppress/breach/insufficient (`disposition-backtest` + the DB feed end-to-end), capacity band widening, CUSUM drift, S-H-ESD vs raw-ESD, RPCA seasonal/sparse separation, the matched-resolution disposition (`anomaly_disposition_test.exs`), and edge↔central series_key alignment (`series_key_test.exs`). The two requirements still awaiting implementation (the alert-engine join + the edge baseline push 2.6) get their scenarios when those land.
- [x] 0.7 Self-masking scenario added (`gen.py` double-spike `selfmask_a`/`selfmask_b`) and scored: the second spike is DETECTED (z=17.1, recall 1/1) — the withhold-from-baseline rule keeps the first spike's samples out of the window, so it does not inflate the baseline std.

## 1. Phase 1 — Honesty + correctness

### 1a. Strip cosmetic "causal" branding (detector/disposition layer)

- [x] 1.1 Producer value flip `signal_type:"causal"→"prediction"` DONE at the 3 producers (`verdict.rs`, seasonal/capacity `verdict_emitter.ex`) — SAFE because the prep landed (consumers dual-read `["causal","prediction"]` 1.f3; rule defs dual-match, `match_value/2` `Enum.any?`). Producer-output tests → `"prediction"`; back-compat tests kept on `"causal"` (dual-consume coverage verified). cargo test 73/0 + `mix compile --warnings-as-errors` clean; the flipped assertion tests pass in isolation (the 3 suite failures are the pre-existing `align_existing_ocsf_event_times` no-DB family, unrelated). **DEPLOY CAVEAT (1.f4): existing deployments' already-seeded single-`"causal"` rules need a data migration to dual-match before this ships live; fresh deploys are correct (dual-match defs committed).**
- [x] 1.2 The `causal-disposition` modules are already honestly named (`seasonal`/`capacity`); the `CausalFlow` hosting is now documented as a pipeline/state-machine combinator (NOT causal inference) across `anomaly-core` (lib/detector), `anomaly-addon` (engine), `causal-disposition` (lib), the NIF, and `CausalReasoner`. (The `causal-disposition` crate / `CausalReasoner` module identifier renames and the wire `signal_type` value are the BREAKING 1f task.)
- [x] 1.3 `rust/causal-engine` crate moduledoc rewritten as a deterministic dependency/expert-reasoning engine (hand-coded C1–C13 rules + ultragraph centrality/reachability; "causaloids" are largely identity nodes), explicitly NOT causal inference. (The wire `signals.causal.*` subject rename stays in 1f.)
- [x] 1.4 Causal-inference overclaim stripped from the engine code module-docs and the docs site (Phase 3); memory reflects the honest framing. (causal-engine's own docs are 1.3.)

### 1b. Honest capacity uncertainty (valid prediction intervals on both paths)

- [x] 1.5 **OLS prediction interval** (`half-width = Z·s·sqrt(1 + 1/n + (x0 - x̄)² / Sxx)`, widens with horizon, residual standard error `s` over `n-2` df). DONE in the Rust kernel `rust/causal-disposition/src/disposition/capacity/linear.rs` (`ols_prediction_bounds`); the worker calls it through the NIF (`worker.ex:368` is the call site, not the math).
- [x] 1.6 **Residual-bootstrap prediction interval** for the additive Holt-Winters path (off the hot path; bounded deterministic-seeded simulation re-injecting resampled in-sample residuals, 2.5/97.5 quantiles). DONE in `holt_winters.rs` (`bootstrap_prediction_bounds`).
- [x] 1.7 **Removed** the `clamp(1 - rmse/scale)` heuristic; `confidence` now carries the interval's nominal coverage level (`0.95`), web-ng relabeled "PI coverage". Parity gate updated (fit fields keep parity; band/confidence assert the new behaviour), plus a unit test (`ols_prediction_interval_widens_with_horizon`) and the harness (`disposition-backtest --kind capacity`) prove the OLS band widens with horizon (5.89→6.15 across 7d/30d/90d).

### 1f. BREAKING — on-the-wire de-causal envelope rename (D7; dual-publish + dual-consume)

- [x] 1.f1 Honest names landed: the anomaly verdict emits `detector_method:"rolling_robust_zscore"`; the NATS subject was renamed `signals.causal.*`→`signals.analytics.*` (dual-subscribe) and the `"collector":"causal-engine"`→`"correlation-engine"`; and the whole IDENTIFIER de-causal shipped this session (module `CausalSignals`→`AnalyticsSignals`, crates `causal-engine`→`correlation-engine` + `causal-disposition`→`anomaly-disposition`, NIF `causal_disposition_nif`→`anomaly_disposition_nif`, facade `CausalReasoner`→`DispositionKernels`). Only the `signal_type` value flip (1.1) + the deploy cutover (1.f4) remain.
- [x] 1.f2 MOOT — **there are NO external producers** (3-way audit, 2026-06-28): arancini (the BMP collector, `~/src/arancini`) publishes ONLY `arancini.updates.*` — the string "signals" doesn't appear in the repo as a producer; there is NO implemented topology producer (`topology-causal-overlays` is a spec capability in the UNBUILT `add-causal-engine` proposal, where topology is a *consumer* of verdicts, not a publisher; the would-be `rust/causal-engine` emitter never shipped). EVERY producer to the `signals.*` namespace is in-repo and already on `signals.analytics.*` (correlation-engine emitter; the anomaly-addon spine via `status_handler.publish_edge_anomaly_verdict`/`CausalPredictionSubject`; seasonal + capacity `verdict_emitter`; the 2 inventory emitters). So no cross-team dual-publish is needed; the dual-subscribe now only covers the in-repo rolling-deploy window. (`mtr_causal_signal_emitter`→`internal.causal.mtr` and `state_change_publisher`→`signals.state.*` are different namespaces, unaffected.)
- [~] 1.f3 Dual-consume landed in the CORE consumers (accept the honest new routing value `"prediction"` alongside legacy `"causal"`): `causal_evidence.rs` (Rust evidence consumer), `status_handler.ex`, `causal_signals.ex` (×2 routers, `:440`/`:1245`). Additive dead-branch until a producer emits the new value — compiles clean (warnings-as-errors); 36/37 causal_signals tests pass (the 1 failure is a PRE-EXISTING baseline time-alignment test, unrelated — verified by reverting). web-ng dual-consume + the value rename (1.1) + the BMP/topology producers (1.f2) follow.
- [~] 1.f4 DEPLOY CUTOVER — the in-repo CODE is DONE; the rest is deploy-time actions (no cross-team, since 1.f2 is moot): (1) ✅ **data migration WRITTEN + DB-VERIFIED** (`priv/repo/migrations/20260628224553_dualmatch_legacy_signal_type_rules.exs`) — updates existing-deployment seeded `causal_*_health_finding` rules' `match->attribute_equals->signal_type` + the `recovery` block from scalar `"causal"` to `["causal","prediction"]`, idempotent (scalar→array only; array rows byte-unchanged), `down` a documented no-op (fresh deploys are array-seeded). So the 1.1 flip is now deploy-safe for long-lived deployments. REMAINING (deploy-time, not code): (2) deploy the flip; (3) the old `signals.causal.*` subject + `signal_type:"causal"` value reach ZERO traffic by construction after the rolling deploy (no external producers); (4) **drop** the legacy dual-subscribe + `"causal"` dual-read/dual-match branches (pure cleanup). Rollback stays safe (legacy paths live until the cleanup).

### 1c. Close the matched-resolution disposition loop (absorbs #4280)

- [x] 1.8 Edge ALREADY forwards spike **peak + window** (`verdict.rs:108-111`: episode_peak_value / episode_peak_at / episode_started_at / episode_ended_at) — verified, no change needed.
- [x] 1.9 The **peak variant** `build_profile_hour_of_week_peak_query` exists, BUT DB validation found it was NOT reachable via a query: the SRQL translate path routed `bucket:1h` profile queries to the downsample builder (rejecting `timezone`), and `to_sql_and_params` lacked the `_peak` branch. **FIXED in `rust/srql`** (`translate.rs`/`engine.rs` profile-stats guard + the missing peak branch); verified against the real schema that both `profile_hour_of_week` and `_peak` now translate + execute, 335 srql tests pass. (The original "verified exists" was a static check that missed reachability — and the same bug had silently broken the production seasonal feed, F15.)
- [~] 1.10 RESOLVED BY DESIGN (matched-resolution Option B): the disposition is computed ON DEMAND from the queryable hour-of-week PEAK profile (the F19 `profile_hour_of_week_peak` verb) when an edge finding arrives — so the central need NOT emit a verdict for every evaluated series (which would flood `ocsf_events`, the very thing this change fixes). The seasonal worker keeps emitting only its own breach/clear verdicts; the on-demand peak-profile lookup is part of the alert-engine wiring (with 1.11). Re-open only if a verdict-stream join (vs on-demand query) is later required.
- [x] 1.11 LOGIC + ORCHESTRATION implemented + unit-tested (`ServiceRadar.Observability.AnomalyDisposition`, 15 tests green): `dispose/3` (peak-vs-peak → suppress/downgrade/escalate/pass_through) AND `for_finding/3` — the on-demand bridge that re-keys an edge finding via `SeriesKey.from_source_identity` (F14), reads the forwarded `episode_peak_value`, derives the (dow,hod) of `episode_peak_at_unix_nano` (Postgres DOW convention), fetches the central hour-of-week PEAK profile through an injected `fetch_peak_profile.(series_key, dow, hod)`, and disposes. The real SRQL fetcher is now IMPLEMENTED + verified: `AnomalyDisposition.PeakProfile.fetcher/2` runs `stats:profile_hour_of_week_peak(value)` and parses `center`/`p95`/`bucket_count` → `%{center, scale=(p95-center)/z95, sample_count}` (5 unit tests; the query verified executable against the real schema after the rust/srql routing fix). `for_finding/3` calls it with `%{series_key, device_id, metric_class, metric_name, dow, hod}` (matches the query's `device_id AS series`, unwrapping the SRQL `payload` envelope). **SEED-AND-FETCH E2E DONE + VERIFIED against a real CNPG/TimescaleDB**: seeded 6 weeks of Tue-09:00 cpu data, refreshed the hourly CAGG, and the disposition produced peak profile center=59.0/sample_count=5 (latest bucket correctly excluded), a novel spike (92) → `:escalate` (z=20.9), an in-profile spike (60) → `:suppress`. The disposition DATA PATH is proven on real data. **CALL-SITE NOW WIRED (the last 1.11 piece): `report_finding/2` (report-only — builds the SRQL fetcher, disposes, emits telemetry `[:serviceradar,:anomaly,:disposition]`, never mutates an alert) is invoked by a new out-of-band `AnomalyDispositionReporter` GenServer that `AnalyticsSignals.process_batch` fire-and-forget casts each persisted class-2004 anomaly finding to AFTER persistence — supervised alongside (NOT inside) the stateful-alert path, off the hot path, can't block ingest. Forwarded edge peak confirmed on the wire (verdict.rs:116-117 → finding_info.dimensions). 7 reporter tests/0 (novel→escalate, in-profile→suppress, uncovered→pass_through, all actionable==false; safe no-op when down). The disposition RUNS in the live system now, report-only.**
- [~] 1.12 The peak-profile stability gate + report-only kill switch is implemented + tested (`AnomalyDisposition.actionable?/2`): suppression is OFF by default (report-only is the safety default), gated on peak-profile stability (`>= :min_stable_samples` + finite center/scale) and a per-metric-class `:suppression_enabled` flag. The suppression-eligible-mass telemetry is the consumer wiring.
- [~] 1.13 The disposition decision (suppress→off-path / downgrade→lower / escalate→higher / pass_through) is the `AnomalyDisposition.dispose/3` output (operator-tunable thresholds); applying it to a finding's effective severity in the alert engine is the consumer wiring (follows with 1.11).
- [x] 1.14 Edge↔central series_key alignment test added + PASSING (`series_key_test.exs`, 11 tests green): the same logical series re-keys identically regardless of the provisional producer hints the edge varies (agent_id/host_id/host_ip and the `host` tag are excluded; the canonical resource + metric + if_index + partition decide the key). The precondition for the join. (Existing F14 tests already covered device_id-over-host/agent canonicalization.)

### 1d. Robust seasonal statistic + hysteresis

- [x] 1.15 Seasonal robust statistic defaults to `median + MAD`: the live cpu/memory sources were already `:median_mad`; the `Source` struct default + the `from_config` fallback (`source.ex`) now default to `:median_mad` too (the profile verb supplies center/mad). (Peak-profile robust IQR scale lands with the 1c loop.)
- [x] 1.16 Core seasonal `confirm_slots` is tunable (operator-overridable) and supports `> 1`; **default = 2** — Rust `SeasonalConfig` default + Elixir `@default_confirm_slots` (D-Q3). Hysteresis verified: `drift_pending_until_confirm_slots_met`; the e2e escalation test pins `confirm_slots: 1` for the single-bucket immediate-breach case.

### 1e. Dead code + stale docs

- [x] 1.17 `peak_profile` kernel unambiguously marked DORMANT/not-wired (no NIF ABI), with the marker now pointing at the matched-resolution loop closure (1c) that will wire it. Kept rather than deleted because that wiring is the next step.
- [x] 1.18 Corrected the false "capacity phase 2 — NOT yet wired" doc in the NIF (`causal_disposition_nif/src/lib.rs:46`); capacity is live and wired (`dispose_batch(:capacity, ...)`).
- [x] 1.19 Write the "what the engine really is" document (robust detector + seasonal/forecast disposition + deterministic dependency expert system).

## 2. Phase 2 — Statistical rigor

### 2a. Edge

- [x] 2.1 DONE via option (a): the edge dispersion is now a **robust median/MAD (Hampel) identifier** — `deviation = (value − median) / (MAD × 1.4826)` over the clean window — replacing the Welford mean/std as the PRIMARY robustness (`stats.rs` `RobustStats{center,scale}` + `robust_score`; `window.rs compact_rolling_state` rebuilds it from the clean window with a sort; `detector.rs`/`signal.rs` `evaluate_rolling_signal` scores center/scale; engine CUSUM anchored to the robust center). PRESERVED unchanged: dispersion floors (`min_std_floor`+`min_cv`, applied to the MAD-scale so a pinned/discrete window with MAD=0 floors instead of dividing by zero), directional saturation gate (cpu/mem/disk 80/80/85), confirm-slot hysteresis, CUSUM, counter rate-norm, and the withhold-from-baseline (now defense-in-depth behind median/MAD). HARNESS RE-VERIFIED (gen.py seed 1234, 3 weeks; old mean/std → new robust): spike recall **1/1→1/1**, step **1/1→1/1** (cpu+mem), selfmask_a/b **1/1→1/1**, recurring_seasonal **21/21→21/21**, blip correctly unconfirmed (hysteresis held); precision held at **1.000** on cpu/mem/snmp-rate (cpu 468→**692 TP / 0 FP** — the extra flags are all in-truth, no new FP); saturation gate off→on **22→11** (sub-80 FP suppressed, real >80 kept) unchanged; counter_raw garbage by design. BONUS: cpu **drift 0/1→1/1** (the robust median lags a slow ramp where the mean tracks it, so the point score now catches the drift — recall up, precision unchanged). No recall regression anywhere. Rust `anomaly-core` 49/0, `anomaly-addon` 92/0, `anomaly-disposition` 43/0; clippy clean. The `engine_score_matches_anomaly_core_zscore` parity test was updated/renamed to `engine_score_matches_anomaly_core_robust_score` (engine==core still pinned, now against `robust_score`).
- [x] 2.2 **Two-sided CUSUM** primitive implemented + unit-tested in `rust/anomaly-core/src/cusum.rs` (anchored, reset-on-alarm; ETA ≈ h/(δ−k)) and wired into `anomaly-backtest --cusum`. HARNESS-PROVEN: it catches the CPU drift the rolling z-score structurally misses (z-score 0/300 → CUSUM 216/300, +20-sample latency). MUST run on the deseasonalized residual (raw CUSUM floods 42–74% FP on seasonal data — measured). Production wiring into the addon/`ReasonContext` path is the deployment follow-up.
- [~] 2.3 Edge **deseasonalization** demonstrated in the harness (CUSUM over a causal hour-of-week residual): with a robust **median** hour-of-week baseline (latest-excluded) CUSUM FP drops to **0.6%/1.0%** (cpu/snmp) while still catching the drift/leak (+20/+31 latency). FINDING: a *persistent* 2-week leak in a 3-week window still pollutes any SHORT-history edge baseline (memory 38% FP) → the production design must push the core's 180-day robust seasonal profile to the edge (2.6), which a short edge window cannot substitute. anomaly-core/`ReasonContext` integration follows.
- [~] 2.4 Harness now measures slow-leak/drift **detection-latency** (+20/+31 samples) and CUSUM **false-positive rate** per series; the self-masking (0.7) and morning-ramp FP scenarios still to add.

### 2b. Core

- [~] 2.5 S-H-ESD primitive IMPLEMENTED + reference-verified in `rust/anomaly-core/src/esd.rs`: Generalized ESD (Rosner) with a robust median/MAD center/scale (the "Hybrid") + Acklam normal-inverse-CDF + Cornish-Fisher Student-t quantile for the critical values. Unit tests pin Φ⁻¹(0.975)=1.96, t(0.975,10)=2.228, and that GESD finds exactly the injected outliers / nothing on a clean series. Production integration (swap the per-bucket residual-z for GESD over the deseasonalized hour-of-week residual series) + the harness scenario (2.9) follow.
- [x] 2.6 Make the hour-of-week profile the **source of the coarse baseline pushed to the edge** (feeds 2.3). BUILDING BLOCKS DONE + TESTED (back-compat: no baseline → unchanged rolling-only): (a) ADD-ON CONSUMPTION — `anomaly-core/seasonal.rs` (`hour_of_week` UTC, `SeasonalBucket{center,scale,sample_count}.usable()`, `synthetic_baseline` reconstructing a window whose mean==center/std==scale so the existing mean/std signal scores against a delivered robust MEDIAN summary), `anomaly-addon/engine.rs` `SeasonalProfile` (168-slot) + `seasonal_context()` resolver replacing the 4 hardcoded `None`s (median center → a recurring incident hour can't poison the reference), `config.rs`/`addon.rs`/`config.schema.json` deliver optional `seasonal_baselines` over the control-plane configure channel; (b) CORE BUILDER — `seasonal_disposition/edge_baseline.ex` turns the existing `profile_hour_of_week` rows into the add-on JSON (scale per median_mad/p05p95/mean_stddev). **DELIVERY + ALIGNMENT NOW DONE + PROVEN e2e.** (1) PRODUCER: `seasonal_disposition/edge_baseline_producer.ex` (Oban worker, cron-gated) calls `EdgeBaseline.build` over `worker.edge_baseline_rows/2`, re-keys each `device_id` baseline to `<device_uid>|<wire_metric_name>`, writes `AddonProfile.params["seasonal_baselines"]` via `SystemActor`; delivery rides the existing `AddonProfileReconcileWorker` → `configure()`. (2) SERIES-KEY ALIGNMENT SOLVED: central `series:uid profile_hour_of_week` keys by canonical `device_id`; the edge detector keys by the finer `series_key`. The add-on ALREADY computes `anomaly_device_uid` at scoring (verdict.rs), so the seasonal lookup is keyed by `<device_uid>|<metric_name>` (detector state keeps its finer key — per-core separation preserved). New `identity.seasonal_series_key` + `engine.evaluate_with_seasonal_key` (old 4-arg delegates, back-compat). PROOF: Rust `tests/seasonal.rs` (uid-keyed baseline resolves — high-but-in-bucket 72 not flagged, 200 flagged; detector-key mis-key stays inert) + real-DB producer build (10 weekly memory readings → CAGG → correct `<uid>|memory.used_percent` baseline, center 71). addon 81/0, seasonal_disposition 42/0. ALSO fixed a sibling F15-class bug: the Worker DISPOSE path (`profile_rows`) didn't `unwrap_payload` the live SRQLRunner envelope → seasonal disposition hydrated nothing against real data; now unwraps (idempotent; flat test rows pass through). FOLLOW-UPS: (a) per-agent delivery scoping — DEFERRED (profile-wide is acceptable; the add-on only resolves keys for series it scores, so extra entries are harmless); (b) non-UTC `profile_timezone` — FIXED: the edge-baseline fetch forces `timezone:"Etc/UTC"` (`Worker.edge_baseline_query/1`) so the (dow,hod) buckets align with the edge detector's UTC `hour_of_week`, while the central VERDICT pass keeps the configured tz via `source.query` (tested: a non-UTC profile still fetches the edge baseline in UTC).
- [~] 2.7 RPCA primitive IMPLEMENTED + rigorously verified in `rust/anomaly-core/src/rpca.rs`: PCP via inexact-ALM with a one-sided Jacobi SVD. The SVD is verified DIRECTLY (reconstruction `||A−UΣVᵀ|| < 1e-8` + orthonormal U/V), and RPCA is verified to separate a rank-1 seasonal `L` from injected sparse spikes (the top-|S| entries are exactly the spike locations). V1 = single-series reshape (the caller supplies the `slots × periods` matrix; a host-stacked fleet matrix is the same primitive with hosts as columns). Feature-flag + core integration + harness scenario (2.9) follow.
- [x] 2.8 Verified: Holt-Winters is capacity-only — the seasonal validator is the residual-z kernel; `holt_winters`/`seasonal_forecast` appear only under `disposition/capacity/`. No change needed.
- [~] 2.9 S-H-ESD vs raw-ESD comparison VERIFIED in-test (S-H-ESD ignores recurring seasonal peaks a raw ESD flags; finds only genuine off-pattern anomalies); RPCA verified to separate seasonal-`L` from sparse-`S` (recovers the exact spike locations); CUSUM drift recall + FP verified in the streaming harness. Full harness wiring over the `gen.py` synthetic series + the RPCA host-stacked fleet demo follow.

## 3. Phase 3 — Documentation overhaul (single source of truth)

- [x] 3.1 Stripped the "causal"/causal-inference overclaim from the engine code module-docs + inline comments (`anomaly-core` lib/detector, `anomaly-addon` engine, `causal-disposition` lib, `causal-engine` lib/god_view/domain_model/reasoner, `causal_disposition_nif`, `CausalReasoner`) and the docs site (Phase 3). Archived proposal history untouched. (The wire `signal_type`/`signals.causal.*` value renames remain in 1f.)
- [x] 3.2 Author the new end-to-end engine doc set under `docs/docs/` (the single source of truth) and register it in `docs/sidebars.ts`, covering: two-tier architecture (edge robust spike detector + core seasonal/capacity disposition + deterministic dependency expert system); data contract (gauges vs monotonic counters, rate normalization, counter wrap/reset, directional saturation gate, series keying); actual statistics (rolling robust z-score; hour-of-week residual-z / S-H-ESD; OLS + Holt-Winters capacity); honest naming (what is and is NOT causal); the disposition loop; operations/tuning knobs; how to run the proof harness (`tools/anomaly-proof`).
- [x] 3.3 Overhauled the stale `docs/docs/anomaly-detection.md` (retitled "Anomaly Detection (Tuning & Operations)", points to the new engine doc, dropped the duplicated stale section; operator content preserved).

## 4. Validation, coordination + rollout

- [~] 4.1 Rollout directive — the CODE side is in place: suppression is report-only by default (`AnomalyDisposition.actionable?/2` defaults `suppression_enabled: false`) and the proof harness exists (`tools/anomaly-proof/`, edge + core + DB-feed). The live "verify on harness before enabling suppression" gate is a deploy-time action.
- [~] 4.2 Rollout directive — the CODE side is in place: Phase-2 detectors (CUSUM/S-H-ESD/RPCA) are reference-verified and the per-metric-class kill switch exists (`actionable?/2`). Calibrating constants against real per-cell distributions is a deploy-time activity guarded by the invariant tests.
- [~] 4.3 Code-doc de-causal VERIFIED complete: a workspace grep finds no causal-INFERENCE claims left in engine code docs (the survivors are the honest "not causal inference" framing, crate-NAME references like `causal-engine`/`causal_disposition_nif`, or the wire subject). The `signal_type:"causal"` wire value + `signals.causal.*` subject rename and the zero-traffic check are 1f (BREAKING).
- [x] 4.4 `add-anomaly-finding-disposition` (#4280, already merged) marked **SUPERSEDED** by this change — a banner at the top of its `proposal.md` points at `refactor-anomaly-engine-rigor` as the consolidation of record (its Option-B disposition is implemented here as `AnomalyDisposition`).
- [x] 4.5 `openspec validate refactor-anomaly-engine-rigor --strict` passes ("Change 'refactor-anomaly-engine-rigor' is valid").
