## 1. Implementation
- [x] 1.1 Normalize SNMP profile `target_query` defaults to `in:devices` for empty values (seed + resolver + UI fallback).
- [x] 1.2 Implement target count evaluation that returns distinct device counts for device or interface SRQL queries.
- [x] 1.3 Update the SNMP profiles list and editor to render computed counts and show "Unknown" on SRQL errors.
- [x] 1.4 Add/adjust tests covering default targeting behavior and UI count rendering.
