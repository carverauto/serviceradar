## 1. Implementation
- [ ] 1.1 Define a per-tenant sysmon payload limit setting and a documented default (15MB).
- [ ] 1.2 Plumb the limit into the agent gateway from tenant config and enforce it for sysmon payloads.
- [ ] 1.3 Document the configuration key, default, and enforcement behavior.
- [ ] 1.4 Add tests covering default fallback and configured limit enforcement.
