## 1. Proposal
- [x] 1.1 Review existing agent, device inventory, datasvc object storage, Bumblebee, and CTI OpenSpec surfaces.
- [x] 1.2 Draft focused endpoint SBOM inventory proposal, design, tasks, and spec deltas.
- [x] 1.3 Validate with `openspec validate add-endpoint-sbom-inventory --strict`.
- [x] 1.4 Get proposal approval before implementation.
- [x] 1.5 Revise architecture for large-fleet scale: local cache, deterministic hashes, upload-on-change, and on-demand query path.
- [x] 1.6 Adopt the full golden-path architecture (TimescaleDB history + continuous-aggregate rollups, bounded cohort scatter-gather, freshness/coverage contract, command-bus safety, backpressure, observability, identity reconciliation) as in-scope now.
- [x] 1.7 Reorient around automation/signals/prediction: inventory + CVE coordinate-match → vulnerability findings → events/alerts → device risk-state enrichment → causal prediction. Topology/god_view is one optional renderer, not the spine.

## 2. Agent And Collector
- [x] 2.1 Add endpoint inventory configuration structs, validation, profile delivery, and local override handling.
- [x] 2.2 Add a signed native add-on or scoped agent subcommand that collects Linux OS package inventory.
- [x] 2.3 Generate CycloneDX JSON with OS package components, OS metadata, collector provenance, and redaction metadata.
- [x] 2.4 Add bounded local spool handling, schema validation, size checks, and last-good artifact behavior.
- [x] 2.5 Add focused Go tests with dpkg, rpm, and apk fixture data.
- [x] 2.6 Add deterministic `package_set_hash` over normalized sorted package identities (`package_manager`, `name`, `version`, `architecture`, canonical `purl`), with a 1-byte algorithm-version prefix and volatile CycloneDX metadata excluded; add a separate `artifact_hash` over canonicalized CycloneDX component payload bytes with scan provenance excluded.
- [x] 2.7 Persist the local last-known-good inventory cache (manifest, `package_set_hash`, `artifact_hash`, source summaries, last-seen package-DB mtimes, last uploaded hashes).
- [x] 2.8 Gate scheduled scans on source-database mtime so unchanged hosts skip parsing entirely (with a forced full re-parse every Nth cycle); change reporting so unchanged inventories send lightweight status/hash summaries instead of full uploads.
- [x] 2.9 Add typed endpoint inventory on-demand command handling that evaluates bounded predicates against the local cache and returns compact COUNT/EXISTS and DETAIL results; support an authorized device-scoped fresh scan behind a per-agent single-flight semaphore.
- [x] 2.10 Add jittered upload scheduling (runtime-profile upload jitter, distinct from the scan-timer randomized delay) so correlated fleet changes spread over a window.
- [x] 2.11 Add bounded retry/buffering of changed uploads when the control plane is unavailable, without losing previous uploaded-hash metadata.

## 3. Transport, Hashing, And Command Bus
(Storage/ingestion items previously numbered 3.3–3.5 were relocated to section 4 during the architecture revision.)
- [x] 3.1 Define protobuf/API contracts for endpoint inventory scan metadata, normalized package summaries, and SBOM artifact references.
- [x] 3.2 Add durable object upload flow for raw SBOM artifacts through datasvc or an agent-gateway relay.
- [x] 3.3 Extend API/protobuf ingestion contracts with `package_set_hash`, `artifact_hash`, `upload_reason`, unchanged-scan status, source summaries, and a typed freshness verdict + cohort coverage envelope.
- [x] 3.4 Add typed inventory command types (cache-query vs force-fresh-scan) and COUNT/EXISTS vs DETAIL response modes; route cohort results over a per-command topic (`agent:commands:{query_id}`) instead of the shared `agent:commands` topic.
- [x] 3.5 Add a bounded cohort scatter-gather in the Elixir agent command bus (`agent_command_bus.ex`): resolve online + capable + authorized targets once, dispatch with bounded concurrency, batch result writes, and enforce a hard cohort cap with SRQL/persisted fallback above it.
- [x] 3.6 Add command-bus safety: `ensure_dispatch_capacity` clauses for the inventory command types (bounded concurrency for cache-query, force-fresh dispatch/rate limits), server-side `endpoint_inventory.force_fresh_scan` RBAC authorization before dispatch, agent-side per-agent single-flight for force-fresh, a `CommandResult.payload_json` byte cap, and a guarantee that SBOM artifact bytes never traverse the command/result stream (route agent→gateway relay→datasvc).
- [x] 3.7 Persist on-demand query command lifecycle/results using the existing agent command bus semantics.

## 4. Storage, Coordinate, Ontology, And Findings
- [x] 4.1 Add Elixir migrations for endpoint inventory scan runs, SBOM artifacts, and normalized package/component rows in the `platform` schema.
- [x] 4.2 Add ingestion code that validates artifact hashes and replaces the current inventory view only after normalized rows commit.
- [x] 4.3 Add retention cleanup for historical scans and raw artifacts.
- [x] 4.4 Make ingestion check the stored current `package_set_hash` first and no-op package-row replacement for unchanged hashes while still updating scan freshness metadata; recompute the hash server-side on changed uploads and flag mismatches; and, when an agent has sent no changed upload within the reconcile window (N scans or M days, whichever first), return a reconcile-floor directive in the scan acknowledgement so the agent performs its next upload as a full changed-path upload (server-directed, not agent self-counting).
- [x] 4.5 Make raw SBOM component payload storage content-addressed by `artifact_hash` and deduplicated across hosts (golden-image dedup); store scan-specific provenance in metadata rows and record reused artifact references.
- [x] 4.6 Move historical changed scans and server-computed package add/remove events into TimescaleDB hypertables (`maybe_create_hypertable` convention), independent of current-state retention, with native compression tuned for repetitive package strings (`compress_segmentby='agent_id'`, `compress_orderby='scan_time DESC, package_manager, name'`) + `add_compression_policy` + `add_retention_policy`, all guarded by the TimescaleDB-availability check.
- [x] 4.7 Compute package diffs server-side at ingest and materialize them into the history hypertable.
- [x] 4.8 Add maintained current-count tables updated from package diffs plus TimescaleDB continuous aggregates for historical fleet rollups (host counts per package/version/ecosystem/CPE over time) that include offline/last-known hosts.
- [x] 4.9 Add a GIN index on `endpoint_inventory_packages.cpes` (and ensure an indexed canonical PURL) so structural "which devices run coordinate X" queries are index scans over the current-state table, not seqscans — this is the accepted cohort/membership path (never an AGE traversal, never the deferred roaring index).
- [x] 4.10 Route endpoint-inventory ingest through a bounded admission queue instead of inline-synchronous ingestion; emit all downstream events AFTER `Repo.transaction` commit via that queue (never inside the transaction).
- [x] 4.11 Add server-side canonical PURL: compute `purl_canonical` (per the PURL spec — lowercased type, namespace for deb/rpm/apk, arch qualifier, sorted/encoded qualifiers) in `normalize_package`, store it in a new `purl_canonical text NOT NULL` column (keep raw agent `purl` for provenance), and make it the PRIMARY dedup/coordinate term with `{package_manager, name, version, architecture}` as the deterministic fallback. Land this BEFORE 2.6 locks PURL into `package_set_hash`.
- [x] 4.12 Add a normalized `Package` entity keyed on the canonical PURL/CPE coordinate; model `Device HAS_PACKAGE` as a CNPG current-state relation + SRQL projection (NOT an AGE edge).
- [x] 4.13 Implement the endpoint-side feed-agnostic coordinate input contract: expose endpoint package canonical PURL/CPE — and the `{package_manager, name, version, architecture}` fallback tuple for feeds without PURL (e.g. Trivy) — with host-scope kept distinct from image-scope. The advisory feed, advisory-side `Package AFFECTED_BY CVE` population, matcher policy, and CVSS scoring are owned by `add-cti-signal-coverage`; this change owns the endpoint-side `Package` entity, device/package relation, and match input contract.
- [x] 4.13b Derive and populate candidate CPE(s) per package in normalization (server-side where the collector's CycloneDX `cpe` is absent/sparse), so CPE-indexed feeds (VulnCheck NVD++) can match; record that CPE matching is `vendor:product` + version-range and that the range evaluation, PURL↔CPE mapping, and distro-backport false-positive handling are matcher-side (`add-cti-signal-coverage`).
- [x] 4.14 Make `inventory` a first-class `CausalSignals` domain (`normalize_domain('inventory')`, `domain_rank('inventory')`, `type_uid_for('inventory')`) BEFORE any inventory event is emitted, and freeze the versioned change-event schema with a deterministic `event_id` (`inventory:<agent_id>:<scan_id>:<event_kind>:<purl-or-coord-hash>`) so redelivery does not duplicate `ocsf_events` rows.
- [x] 4.15 Publish package-change events (`added`/`removed`/`version_changed`; canonical PURL/CPE; prev/new version; `package_set_hash`; canonical device UID; observed-at) to `signals.causal.inventory.*` (`signal_type='inventory'`, routed by the existing `bmp_causal_subject?` path into `CausalSignals`); these are inventory deltas, NOT `class_uid=2004`.
- [x] 4.16 Add a `CausalSignals` `signal_type=='inventory'` vuln-match branch that emits OCSF Vulnerability Findings (`class_uid=2004`, `category_uid=2`, `type_uid=200401`, `activity_id=1`), maps CVSS→`severity_id`, populates `device={"uid": device_uid}` directly (the existing `normalize_device` does not read `device_uid`), sets `primary_domain='security'`, and SUPPRESSES the finding when `device_uid` is nil (pre-reconciliation) so it is never emitted with `device={}`.
- [x] 4.17 Enrich `ocsf_devices` risk state at vuln-match ingest via `DeviceRiskReducer.upsert_contribution(source: "endpoint_inventory", source_ref: device_uid, score: CVSS*10 clamped 0–100)` (MAX-wins multi-source arbitration, recompute on package add/remove). Drive this from the ingestor (NOT a `Device` `:update` Ash action) so `suppress_operational_event?` cannot block risk on inactive/decommissioned devices. Handle `is_compliant` via a separate write or defer to `add-cti-signal-coverage` (KEV requires the advisory feed).
- [x] 4.18 Project ONLY a bounded per-device risk summary into AGE: `MERGE (d:Device {id: $uid}) SET` ≤5 scalars (`pkg_worst_severity`, `pkg_critical_count`, `pkg_kev_count`, `pkg_has_unpatched_rce`, `pkg_risk_summary_at`) on the EXISTING `Device` vertex — no new vertex labels, no `Package` vertex, no `HAS_PACKAGE`/`AFFECTED_BY` edges. Conditional on advisory scoring (set `none`/`unknown` when absent, never leave stale); mark topology-structure-invariant so the causal hydrator's unfreeze excludes it.
- [x] 4.19 Ensure the inventory history hypertable is excluded from the CDC allowlist (history is queried on-demand via SRQL; only current-state tables may be CDC'd).

## 5. Identity, Backpressure, And Observability
- [x] 5.1 Add `platform.device_fleet_ordinals` dictionary (`uid text PK REFERENCES ocsf_devices(uid)`, `ordinal integer UNIQUE NOT NULL` from a sequence, `tombstoned boolean DEFAULT false`, `allocated_at timestamptz`) + lazy first-seen allocation at ingest + backfill for existing active devices (skip `serviceradar:`-prefixed service-component ids). This is the derived `uid→u32` surrogate the deferred fleet index needs; `ocsf_devices.uid` is unchanged and gains no ordinal column.
- [x] 5.2 Extend `IdentityReconciler.merge_devices` to reassign inventory scan/package rows to the surviving canonical UID, tombstone the dead UID's ordinal, ensure the survivor has an ordinal, and backfill NULL `device_uid` inventory rows when an agent first acquires a canonical UID.
- [x] 5.3 Enforce the `sr:`-canonical `device_uid` invariant: add a DB CHECK (`device_uid LIKE 'sr:%' OR device_uid IS NULL`) on the inventory tables + an ingestor guard that rejects/NULLs non-`sr:` values; fix the ingestor test fixture that currently uses a non-`sr:` uid and assert `String.starts_with?(uid, "sr:")`.
- [x] 5.4 Add a `record_field_value(record, "device_uid") => device["uid"]` clause and a `group_sources` entry to `stateful_alert_engine.ex` (no device-uid clause exists today; `group_by: device` silently falls back to `global` otherwise).
- [x] 5.5 Add cost/volume observability: upload-reason counters, changed-vs-unchanged ratio (hash-flapping detection), object-store bytes, current-row counts, and autovacuum/compression lag for inventory tables.
- [x] 5.6 Design the agent status heartbeat to carry operator-defined standing-question result counts (forward-compatible for continuous-aggregate-backed fleet answers).

## 6. Query, Alerting, Automation, And UI
- [x] 6.1 Add Ash resources/API reads for scan status, artifact metadata, and package rows.
- [x] 6.2 Add SRQL fields or query support for package name, version, package manager, PURL, CPE, and current-vs-historical inventory state.
- [x] 6.3 Add a device/asset detail surface showing latest scan status, installed package inventory, and device risk posture.
- [x] 6.4 Add operator controls for enabling sources, cadence, retention, and redaction.
- [x] 6.5 Add API/SRQL-visible freshness fields: `package_set_hash`, last scan time, last changed scan time, unchanged scan count, and the typed freshness verdict.
- [x] 6.6 Answer fleet membership/rollup ("which devices run coordinate X", "how many hosts have nginx") via maintained current-count tables, the GIN-indexed current-state `SELECT`, standing-question result aggregates, and TimescaleDB continuous aggregates — never an AGE traversal and never the deferred roaring index.
- [x] 6.7 Add an on-demand endpoint software query surface: a device-scoped check/refresh action and a bounded cohort/fleet query form that displays compact live results with freshness and the cohort coverage envelope.
- [x] 6.8 Seed an endpoint-inventory vulnerability `StatefulAlertRule` (`signal: :event`, subject-prefix `signals.causal.inventory`, `attribute_equals signal_type`, `group_by: device`, threshold 1) AND add an `evaluate_events` call inside `CausalSignals` gated on `signal_type=='inventory'`, dispatched async/bounded (it is a synchronous 15s `GenServer.call` — inline on fleet-patch volume back-pressures Broadway).
- [x] 6.9 Emit vuln findings via the Ash `OcsfEvent.record` path (Path A) so `EventHandlerRunner` northbound automations (ticket/quarantine/webhook) fire; document that alert evaluation needs explicit `evaluate_events` wiring and that risk enrichment is ingestor-driven and independent of the event write.
- [x] 6.10 Expose for the causal engine via SRQL/`EmbeddedSrql`: current-state package rows + `Device HAS_PACKAGE` (CNPG relation), endpoint-side `Package`, advisory/vulnerability findings from `add-cti-signal-coverage` keyed on canonical UID + canonical PURL/CPE, plus the bounded Device risk attributes via `graph_cypher` (Device risk attributes ONLY — there is no package subgraph to traverse).

## 7. Validation
- [x] 7.1 Run focused Go tests for collector, mtime gate, spool validation, hashing determinism/version, and local cache fallback.
- [x] 7.2 Run Elixir migration/resource tests for ingestion and current inventory replacement.
- [x] 7.3 Run SRQL tests for endpoint package predicates and projections.
- [x] 7.4 Run Go tests for hash-gated upload decisions, jittered scheduling, and typed on-demand query command handling (COUNT/EXISTS and DETAIL, device-scoped force-fresh authorization).
- [x] 7.5 Run Elixir tests for: unchanged `package_set_hash` no-op + server-side recompute/mismatch flag + reconcile-floor; canonical-PURL determinism + fallback-tuple dedup; content-addressed component-payload dedupe with separate provenance metadata; maintained current counts; hypertable history + compression + continuous aggregates; GIN index usage; `sr:`-uid CHECK/guard (fix the mis-claiming fixture); `device_fleet_ordinals` allocation + device-merge tombstone/reassign + NULL-uid backfill.
- [x] 7.6 Run agent-gateway tests for bounded cohort scatter-gather (cohort cap, per-command topic isolation), dispatch capacity/rate enforcement, server-side force-fresh RBAC denial-before-dispatch, agent-side semaphore behavior, and `CommandResult` payload cap.
- [x] 7.7 Run web-ng tests for inventory status, freshness verdict, package UI, fleet rollup, device risk posture, and on-demand/cohort query UI.
- [x] 7.8 Run automation-pipeline tests: package-change events into `ocsf_events` (deterministic `event_id`, no dup on redelivery); vuln-match OCSF `class_uid=2004` finding with `device.uid` populated + suppressed when nil; `DeviceRiskReducer` MAX-wins enrichment + convergence on package removal; seeded alert rule fires with `group_by: device`; northbound `EventHandlerRunner` dispatch via `OcsfEvent.record`; and a MUST-fail check that no `Package`/`HAS_PACKAGE`/`AFFECTED_BY` vertex or edge is created in `platform_graph`.
- [x] 7.9 Run OpenSpec validation and focused build/test commands before rollout (update bazel BUILD deps for any new Go/Rust imports and test files).

## 8. Deferred: Interactive Fleet Index (Roaring)
Build when an interactive cohort/drill-down consumer ships. Until then, the GIN-indexed current-state `SELECT` (4.9) is the accepted cohort-resolution path (fine at 100k, degrading toward 1M). The `device_fleet_ordinals` dictionary (5.1) — the only irreversible piece — is built now so this is hooked, not forgotten. The causal engine and the automation pipeline consume NONE of this.
- [ ] 8.1 Add a rustler NIF under `elixir/serviceradar_core/native/` (reusing the `god_view_nif` roaring + arrow-ipc pattern) that builds and queries roaring-bitmap inverted indexes over current inventory coordinates (canonical PURL, CPE, name, name+version, package manager, ecosystem) plus online/capability/partition axis bitmaps, keyed on the `device_fleet_ordinals` ordinal.
- [ ] 8.2 Update the inverted index incrementally on hash-gated ingest from the server-side package diff (flip changed devices' ordinal bits; unchanged scans no-op).
- [ ] 8.3 Hold index bitmaps in memory with a generation/epoch marker, rebuild deterministically from current-state tables on boot/corruption, and use portable roaring `serialize_into` only for snapshot/restore — no persisted Postgres bitmap table (the `device_fleet_ordinals` dictionary is the uid→ordinal mapping, not a bitmap table).
- [ ] 8.4 Expose index query ops: membership, boolean AND/OR/ANDNOT across coordinates, cardinality (instant host count including offline), and cohort target resolution; expose `query_and_resolve_to_uids(coordinate) → Vec<device_uid>` so any render/verdict-layer intersection happens in canonical UID space, never a direct cross-space bitmap AND.
- [ ] 8.5 Add inverted-index observability: index memory/byte size per partition, generation/lag vs current state, rebuild duration, and per-coordinate cardinality distribution.
