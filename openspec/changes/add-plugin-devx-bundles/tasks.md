## 1. Implementation
- [ ] 1.1 Define the bundle format in docs and update `docs/docs/wasm-plugins.md`.
- [ ] 1.2 Update web-ng plugin import UI to accept bundle uploads and show validation errors.
- [ ] 1.3 Update core/web-ng import pipeline to extract and validate bundle contents.
- [ ] 1.4 Allow sidecar `display_contract.json` and `config.schema.json` and apply precedence rules.
- [ ] 1.5 Update `tools/wasm-plugin-harness` to generate a bundle artifact (`plugin-bundle.zip`).
- [ ] 1.6 Add example bundle contents and update README for the harness.
- [ ] 1.7 Add tests for bundle validation (missing files, invalid JSON, path traversal).
