suite: web-ng checksum is stable when secret lookup is empty
templates:
  - templates/web.yaml
tests:
  - it: stamps lookup-unavailable on the web-ng deployment
    asserts:
      - equal:
          path: spec.template.metadata.annotations['checksum/db-credentials']
          value: lookup-unavailable
        documentSelector:
          path: kind
          value: Deployment
