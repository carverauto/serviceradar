suite: core checksum is stable when secret lookup is empty
templates:
  - templates/core.yaml
tests:
  - it: stamps lookup-unavailable on the core deployment
    asserts:
      - equal:
          path: spec.template.metadata.annotations['checksum/db-credentials']
          value: lookup-unavailable
        documentSelector:
          path: kind
          value: Deployment
