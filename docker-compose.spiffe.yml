services:
  # The base docker-compose.yml now enables SPIFFE by default. This override is
  # retained only for backwards compatibility so existing scripts that include
  # docker-compose.spiffe.yml keep working without additional changes.
