# ServiceRadar Docker Configuration
# Copy this file to .env and customize as needed

# ============================================
# IMAGE VERSION
# ============================================

# ServiceRadar image version to use
# Options: latest, 1.0.53, 1.0.52, develop, etc.
# Default: latest (automatically uses most recent stable release)
# SERVICERADAR_VERSION=latest
# SERVICERADAR_VERSION=1.0.53

# ============================================
# DATABASE CONFIGURATION  
# ============================================

# Proton database password (auto-generated if not set)
# PROTON_PASSWORD=your-secure-password

# Proton log level
PROTON_LOG_LEVEL=error

# ============================================
# API CONFIGURATION
# ============================================

# API authentication key
API_KEY=changeme

# JWT secret for token generation  
JWT_SECRET=changeme

# Enable/disable authentication
AUTH_ENABLED=true

# ============================================
# BUILD CONFIGURATION (for development)
# ============================================

# Version for local builds (when using docker-compose.dev.yml)
VERSION=dev

# Build ID for local builds
BUILD_ID=docker

# Config source (file, env, consul, etc.)
CONFIG_SOURCE=file