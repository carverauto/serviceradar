# Legacy Next.js UI retained for reference only.
# Bazel build targets are intentionally omitted.

package(default_visibility = ["//visibility:public"])

exports_files(
    [
        ".npmrc",
        "package.json",
        "pnpm-lock.yaml",
    ],
    visibility = ["//visibility:public"],
)
