# Managed by ServiceRadar. Keep the shared runtime tree outside the agent unit lifecycle.
[Service]
RuntimeDirectory=
RuntimeDirectoryMode=0750
ExecStartPre=+/usr/bin/install -d -o serviceradar -g serviceradar -m 0750 /run/serviceradar
