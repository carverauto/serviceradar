# Package placeholder for repository alias helper.
package(default_visibility = ["//visibility:public"])

exports_files(["repo_alias.bzl"])

filegroup(
    name = "hex_cache",
    srcs = ["hex_cache.tar.gz"],
)

filegroup(
    name = "cargo_cache",
    srcs = ["cargo_cache.tar.gz"],
)
